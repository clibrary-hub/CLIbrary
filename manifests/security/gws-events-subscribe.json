{
  "name": "gws-events-subscribe",
  "version": "0.1.0",
  "category": "security",
  "description": "Google Workspace Events: Subscribe to Workspace events and stream them as NDJSON.",
  "intent_triggers": [
    "use the gws events subscribe agent skill",
    "run a workflow for gws events subscribe",
    "help me with gws events subscribe",
    "我需要處理 gws events subscribe 相關工作",
    "幫我啟用 gws events subscribe 這個 skill",
    "用 agent skill 完成 gws events subscribe",
    "use gws events subscribe from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the gws events subscribe skill for this task.",
      "invocation": "codex --skill gws-events-subscribe \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "security"
  ]
}
