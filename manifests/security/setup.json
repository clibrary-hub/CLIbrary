{
  "name": "setup",
  "version": "0.1.0",
  "category": "security",
  "description": "Set up Vercel CLI and project configuration.",
  "intent_triggers": [
    "use the setup agent skill",
    "run a workflow for setup",
    "help me with setup",
    "我需要處理 setup 相關工作",
    "幫我啟用 setup 這個 skill",
    "用 agent skill 完成 setup",
    "use setup from vercel-deploy-claude-code-plugin"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the setup skill for this task.",
      "invocation": "codex --skill setup \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "security"
  ]
}
