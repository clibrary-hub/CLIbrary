{
  "name": "gws-sheets-append",
  "version": "0.1.0",
  "category": "security",
  "description": "Google Sheets: Append a row to a spreadsheet.",
  "intent_triggers": [
    "use the gws sheets append agent skill",
    "run a workflow for gws sheets append",
    "help me with gws sheets append",
    "我需要處理 gws sheets append 相關工作",
    "幫我啟用 gws sheets append 這個 skill",
    "用 agent skill 完成 gws sheets append",
    "use gws sheets append from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the gws sheets append skill for this task.",
      "invocation": "codex --skill gws-sheets-append \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "security"
  ]
}
