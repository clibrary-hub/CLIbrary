{
  "name": "gws-calendar-agenda",
  "version": "0.1.0",
  "category": "security",
  "description": "Google Calendar: Show upcoming events across all calendars.",
  "intent_triggers": [
    "use the gws calendar agenda agent skill",
    "run a workflow for gws calendar agenda",
    "help me with gws calendar agenda",
    "我需要處理 gws calendar agenda 相關工作",
    "幫我啟用 gws calendar agenda 這個 skill",
    "用 agent skill 完成 gws calendar agenda",
    "use gws calendar agenda from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the gws calendar agenda skill for this task.",
      "invocation": "codex --skill gws-calendar-agenda \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "security"
  ]
}
