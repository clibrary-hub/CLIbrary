{
  "name": "gws-sheets-read",
  "version": "0.1.0",
  "category": "security",
  "description": "Google Sheets: Read values from a spreadsheet.",
  "intent_triggers": [
    "use the gws sheets read agent skill",
    "run a workflow for gws sheets read",
    "help me with gws sheets read",
    "我需要處理 gws sheets read 相關工作",
    "幫我啟用 gws sheets read 這個 skill",
    "用 agent skill 完成 gws sheets read",
    "use gws sheets read from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the gws sheets read skill for this task.",
      "invocation": "codex --skill gws-sheets-read \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "security"
  ]
}
