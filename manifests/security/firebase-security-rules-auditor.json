{
  "name": "firebase-security-rules-auditor",
  "version": "0.1.0",
  "category": "security",
  "description": "A skill to evaluate how secure Firestore security rules are.",
  "intent_triggers": [
    "use the firebase security rules auditor agent skill",
    "run a workflow for firebase security rules auditor",
    "help me with firebase security rules auditor",
    "我需要處理 firebase security rules auditor 相關工作",
    "幫我啟用 firebase security rules auditor 這個 skill",
    "用 agent skill 完成 firebase security rules auditor",
    "use firebase security rules auditor from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the firebase security rules auditor skill for this task.",
      "invocation": "codex --skill firebase-security-rules-auditor \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "security"
  ]
}
