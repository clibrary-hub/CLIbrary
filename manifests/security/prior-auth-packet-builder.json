{
  "name": "prior-auth-packet-builder",
  "version": "0.1.0",
  "category": "security",
  "description": "Build a concise prior authorization packet from local case files and payer policy docs.",
  "intent_triggers": [
    "use the prior auth packet builder agent skill",
    "run a workflow for prior auth packet builder",
    "help me with prior auth packet builder",
    "我需要處理 prior auth packet builder 相關工作",
    "幫我啟用 prior auth packet builder 這個 skill",
    "用 agent skill 完成 prior auth packet builder",
    "use prior auth packet builder from openai-agents-python"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the prior auth packet builder skill for this task.",
      "invocation": "codex --skill prior-auth-packet-builder \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "security"
  ]
}
