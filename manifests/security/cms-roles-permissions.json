{
  "name": "cms-roles-permissions",
  "version": "0.1.0",
  "category": "security",
  "description": "Advise developers on designing roles, permissions, teams, and token access in Contentstack.",
  "intent_triggers": [
    "use the cms roles permissions agent skill",
    "run a workflow for cms roles permissions",
    "help me with cms roles permissions",
    "我需要處理 cms roles permissions 相關工作",
    "幫我啟用 cms roles permissions 這個 skill",
    "用 agent skill 完成 cms roles permissions",
    "use cms roles permissions from contentstack-agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the cms roles permissions skill for this task.",
      "invocation": "codex --skill cms-roles-permissions \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "security"
  ]
}
