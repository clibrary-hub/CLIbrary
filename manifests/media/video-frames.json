{
  "name": "video-frames",
  "version": "0.1.0",
  "category": "media",
  "description": "Extract frames or short clips from videos using ffmpeg.",
  "intent_triggers": [
    "use the video frames agent skill",
    "run a workflow for video frames",
    "help me with video frames",
    "我需要處理 video frames 相關工作",
    "幫我啟用 video frames 這個 skill",
    "用 agent skill 完成 video frames",
    "use video frames from openclaw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the video frames skill for this task.",
      "invocation": "codex --skill video-frames \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "media",
    "mit"
  ]
}
