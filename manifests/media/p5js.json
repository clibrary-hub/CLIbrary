{
  "name": "p5js",
  "version": "0.1.0",
  "category": "media",
  "description": "Production pipeline for interactive and generative visual art using p5.js.",
  "intent_triggers": [
    "use the p5js agent skill",
    "run a workflow for p5js",
    "help me with p5js",
    "我需要處理 p5js 相關工作",
    "幫我啟用 p5js 這個 skill",
    "用 agent skill 完成 p5js"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the p5js skill for this task.",
      "invocation": "codex --skill p5js \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "media",
    "mit",
    "skill"
  ]
}
