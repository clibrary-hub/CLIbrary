{
  "name": "openai-image-gen",
  "version": "0.1.0",
  "category": "media",
  "description": "Batch-generate images via OpenAI Images API.",
  "intent_triggers": [
    "use the openai image gen agent skill",
    "run a workflow for openai image gen",
    "help me with openai image gen",
    "我需要處理 openai image gen 相關工作",
    "幫我啟用 openai image gen 這個 skill",
    "用 agent skill 完成 openai image gen",
    "use openai image gen from openclaw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the openai image gen skill for this task.",
      "invocation": "codex --skill openai-image-gen \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "media",
    "mit"
  ]
}
