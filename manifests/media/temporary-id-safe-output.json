{
  "name": "temporary-id-safe-output",
  "version": "0.1.0",
  "category": "media",
  "description": "Add temporary ID support to safe-output jobs end to end.",
  "intent_triggers": [
    "use the temporary id safe output agent skill",
    "run a workflow for temporary id safe output",
    "help me with temporary id safe output",
    "我需要處理 temporary id safe output 相關工作",
    "幫我啟用 temporary id safe output 這個 skill",
    "用 agent skill 完成 temporary id safe output",
    "use temporary id safe output from gh-aw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the temporary id safe output skill for this task.",
      "invocation": "codex --skill temporary-id-safe-output \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "media",
    "mit"
  ]
}
