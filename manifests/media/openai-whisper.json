{
  "name": "openai-whisper",
  "version": "0.1.0",
  "category": "media",
  "description": "Local speech-to-text with the Whisper CLI (no API key).",
  "intent_triggers": [
    "use the openai whisper agent skill",
    "run a workflow for openai whisper",
    "help me with openai whisper",
    "我需要處理 openai whisper 相關工作",
    "幫我啟用 openai whisper 這個 skill",
    "用 agent skill 完成 openai whisper",
    "use openai whisper from openclaw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the openai whisper skill for this task.",
      "invocation": "codex --skill openai-whisper \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "media",
    "mit"
  ]
}
