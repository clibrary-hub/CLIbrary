{
  "name": "sherpa-onnx-tts",
  "version": "0.1.0",
  "category": "media",
  "description": "Local text-to-speech via sherpa-onnx (offline, no cloud)",
  "intent_triggers": [
    "use the sherpa onnx tts agent skill",
    "run a workflow for sherpa onnx tts",
    "help me with sherpa onnx tts",
    "我需要處理 sherpa onnx tts 相關工作",
    "幫我啟用 sherpa onnx tts 這個 skill",
    "用 agent skill 完成 sherpa onnx tts",
    "use sherpa onnx tts from openclaw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sherpa onnx tts skill for this task.",
      "invocation": "codex --skill sherpa-onnx-tts \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "media",
    "mit"
  ]
}
