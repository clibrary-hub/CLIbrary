{
  "name": "rw-generate-audio",
  "version": "0.1.0",
  "category": "media",
  "description": "Generate audio using the Runway API via runnable scripts.",
  "intent_triggers": [
    "use the rw generate audio agent skill",
    "run a workflow for rw generate audio",
    "help me with rw generate audio",
    "我需要處理 rw generate audio 相關工作",
    "幫我啟用 rw generate audio 這個 skill",
    "用 agent skill 完成 rw generate audio",
    "use rw generate audio from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the rw generate audio skill for this task.",
      "invocation": "codex --skill rw-generate-audio \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "media",
    "mit"
  ]
}
