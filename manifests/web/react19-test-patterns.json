{
  "name": "react19-test-patterns",
  "version": "0.1.0",
  "category": "web",
  "description": "Provides before/after patterns for migrating test files to React 19 compatibility, including act() imports, Simulate removal, and StrictMode call count changes.",
  "intent_triggers": [
    "use the react19 test patterns agent skill",
    "run a workflow for react19 test patterns",
    "help me with react19 test patterns",
    "我需要處理 react19 test patterns 相關工作",
    "幫我啟用 react19 test patterns 這個 skill",
    "用 agent skill 完成 react19 test patterns",
    "use react19 test patterns from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the react19 test patterns skill for this task.",
      "invocation": "codex --skill react19-test-patterns \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
