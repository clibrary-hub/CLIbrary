{
  "name": "web-perf",
  "version": "0.1.0",
  "category": "web",
  "description": "Analyzes web performance using Chrome DevTools MCP.",
  "intent_triggers": [
    "use the web perf agent skill",
    "run a workflow for web perf",
    "help me with web perf",
    "我需要處理 web perf 相關工作",
    "幫我啟用 web perf 這個 skill",
    "用 agent skill 完成 web perf",
    "use web perf from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the web perf skill for this task.",
      "invocation": "codex --skill web-perf \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
