{
  "name": "neon-auth-nextjs",
  "version": "0.1.0",
  "category": "web",
  "description": "Sets up Neon Auth in Next.js App Router applications.",
  "intent_triggers": [
    "use the neon auth nextjs agent skill",
    "run a workflow for neon auth nextjs",
    "help me with neon auth nextjs",
    "我需要處理 neon auth nextjs 相關工作",
    "幫我啟用 neon auth nextjs 這個 skill",
    "用 agent skill 完成 neon auth nextjs",
    "use neon auth nextjs from neon-js"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the neon auth nextjs skill for this task.",
      "invocation": "codex --skill neon-auth-nextjs \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
