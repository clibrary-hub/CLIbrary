{
  "name": "mapbox-ios-patterns",
  "version": "0.1.0",
  "category": "web",
  "description": "Official integration patterns for Mapbox Maps SDK on iOS.",
  "intent_triggers": [
    "use the mapbox ios patterns agent skill",
    "run a workflow for mapbox ios patterns",
    "help me with mapbox ios patterns",
    "我需要處理 mapbox ios patterns 相關工作",
    "幫我啟用 mapbox ios patterns 這個 skill",
    "用 agent skill 完成 mapbox ios patterns",
    "use mapbox ios patterns from mapbox-agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mapbox ios patterns skill for this task.",
      "invocation": "codex --skill mapbox-ios-patterns \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
