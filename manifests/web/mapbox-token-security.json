{
  "name": "mapbox-token-security",
  "version": "0.1.0",
  "category": "web",
  "description": "Security best practices for Mapbox access tokens, including scope management, URL restrictions, rotation strategies, and protecting sensitive data.",
  "intent_triggers": [
    "use the mapbox token security agent skill",
    "run a workflow for mapbox token security",
    "help me with mapbox token security",
    "我需要處理 mapbox token security 相關工作",
    "幫我啟用 mapbox token security 這個 skill",
    "用 agent skill 完成 mapbox token security",
    "use mapbox token security from mapbox-agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mapbox token security skill for this task.",
      "invocation": "codex --skill mapbox-token-security \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
