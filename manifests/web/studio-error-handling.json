{
  "name": "studio-error-handling",
  "version": "0.1.0",
  "category": "web",
  "description": "Error display and troubleshooting pattern for Supabase Studio.",
  "intent_triggers": [
    "use the studio error handling agent skill",
    "run a workflow for studio error handling",
    "help me with studio error handling",
    "我需要處理 studio error handling 相關工作",
    "幫我啟用 studio error handling 這個 skill",
    "用 agent skill 完成 studio error handling",
    "use studio error handling from supabase"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the studio error handling skill for this task.",
      "invocation": "codex --skill studio-error-handling \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
