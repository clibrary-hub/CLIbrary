{
  "name": "mapbox-google-maps-migration",
  "version": "0.1.0",
  "category": "web",
  "description": "Migration guide for developers moving from Google Maps Platform to Mapbox GL JS, covering API equivalents, pattern translations, and key differences",
  "intent_triggers": [
    "use the mapbox google maps migration agent skill",
    "run a workflow for mapbox google maps migration",
    "help me with mapbox google maps migration",
    "我需要處理 mapbox google maps migration 相關工作",
    "幫我啟用 mapbox google maps migration 這個 skill",
    "用 agent skill 完成 mapbox google maps migration",
    "use mapbox google maps migration from mapbox-agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mapbox google maps migration skill for this task.",
      "invocation": "codex --skill mapbox-google-maps-migration \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
