{
  "name": "llm-context",
  "version": "0.1.0",
  "category": "web",
  "description": "USE FOR RAG/LLM grounding.",
  "intent_triggers": [
    "use the llm context agent skill",
    "run a workflow for llm context",
    "help me with llm context",
    "我需要處理 llm context 相關工作",
    "幫我啟用 llm context 這個 skill",
    "用 agent skill 完成 llm context",
    "use llm context from brave-search-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the llm context skill for this task.",
      "invocation": "codex --skill llm-context \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
