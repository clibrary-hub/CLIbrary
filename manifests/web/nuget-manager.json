{
  "name": "nuget-manager",
  "version": "0.1.0",
  "category": "web",
  "description": "Manage NuGet packages in .NET projects/solutions.",
  "intent_triggers": [
    "use the nuget manager agent skill",
    "run a workflow for nuget manager",
    "help me with nuget manager",
    "我需要處理 nuget manager 相關工作",
    "幫我啟用 nuget manager 這個 skill",
    "用 agent skill 完成 nuget manager",
    "use nuget manager from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the nuget manager skill for this task.",
      "invocation": "codex --skill nuget-manager \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
