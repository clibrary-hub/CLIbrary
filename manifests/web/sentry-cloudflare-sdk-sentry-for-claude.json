{
  "name": "sentry-cloudflare-sdk-sentry-for-claude",
  "version": "0.1.0",
  "category": "web",
  "description": "Full Sentry SDK setup for Cloudflare Workers and Pages.",
  "intent_triggers": [
    "use the sentry cloudflare sdk agent skill",
    "run a workflow for sentry cloudflare sdk",
    "help me with sentry cloudflare sdk",
    "我需要處理 sentry cloudflare sdk 相關工作",
    "幫我啟用 sentry cloudflare sdk 這個 skill",
    "用 agent skill 完成 sentry cloudflare sdk",
    "use sentry cloudflare sdk from sentry-for-claude"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sentry cloudflare sdk skill for this task.",
      "invocation": "codex --skill sentry-cloudflare-sdk \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
