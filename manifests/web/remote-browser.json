{
  "name": "remote-browser",
  "version": "0.1.0",
  "category": "web",
  "description": "Controls a local browser from a sandboxed remote machine.",
  "intent_triggers": [
    "use the remote browser agent skill",
    "run a workflow for remote browser",
    "help me with remote browser",
    "我需要處理 remote browser 相關工作",
    "幫我啟用 remote browser 這個 skill",
    "用 agent skill 完成 remote browser",
    "use remote browser from browser-use"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the remote browser skill for this task.",
      "invocation": "codex --skill remote-browser \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
