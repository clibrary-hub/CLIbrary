{
  "name": "test-site",
  "version": "0.1.0",
  "category": "web",
  "description": "Agent skill for test site workflows.",
  "intent_triggers": [
    "use the test site agent skill",
    "run a workflow for test site",
    "help me with test site",
    "我需要處理 test site 相關工作",
    "幫我啟用 test site 這個 skill",
    "用 agent skill 完成 test site",
    "use test site from power-platform-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the test site skill for this task.",
      "invocation": "codex --skill test-site \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
