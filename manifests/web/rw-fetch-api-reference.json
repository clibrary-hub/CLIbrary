{
  "name": "rw-fetch-api-reference",
  "version": "0.1.0",
  "category": "web",
  "description": "Retrieve the latest Runway API reference from docs.dev.runwayml.com and use it as the authoritative source before any integration work",
  "intent_triggers": [
    "use the rw fetch api reference agent skill",
    "run a workflow for rw fetch api reference",
    "help me with rw fetch api reference",
    "我需要處理 rw fetch api reference 相關工作",
    "幫我啟用 rw fetch api reference 這個 skill",
    "用 agent skill 完成 rw fetch api reference",
    "use rw fetch api reference from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the rw fetch api reference skill for this task.",
      "invocation": "codex --skill rw-fetch-api-reference \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
