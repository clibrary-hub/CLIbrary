{
  "name": "vercel-deploy",
  "version": "0.1.0",
  "category": "web",
  "description": "Deploy applications and websites to Vercel with a claimable preview.",
  "intent_triggers": [
    "use the vercel deploy agent skill",
    "run a workflow for vercel deploy",
    "help me with vercel deploy",
    "我需要處理 vercel deploy 相關工作",
    "幫我啟用 vercel deploy 這個 skill",
    "用 agent skill 完成 vercel deploy",
    "use vercel deploy from vercel-deploy-codex-skill"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the vercel deploy skill for this task.",
      "invocation": "codex --skill vercel-deploy \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
