{
  "name": "notion-cli",
  "version": "0.1.0",
  "category": "web",
  "description": "Agent skill for notion cli workflows.",
  "intent_triggers": [
    "use the notion cli agent skill",
    "run a workflow for notion cli",
    "help me with notion cli",
    "我需要處理 notion cli 相關工作",
    "幫我啟用 notion cli 這個 skill",
    "用 agent skill 完成 notion cli",
    "use notion cli from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the notion cli skill for this task.",
      "invocation": "codex --skill notion-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
