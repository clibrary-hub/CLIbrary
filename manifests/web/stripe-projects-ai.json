{
  "name": "stripe-projects-ai",
  "version": "0.1.0",
  "category": "web",
  "description": "Agent skill for stripe projects workflows.",
  "intent_triggers": [
    "use the stripe projects agent skill",
    "run a workflow for stripe projects",
    "help me with stripe projects",
    "我需要處理 stripe projects 相關工作",
    "幫我啟用 stripe projects 這個 skill",
    "用 agent skill 完成 stripe projects",
    "use stripe projects from ai"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the stripe projects skill for this task.",
      "invocation": "codex --skill stripe-projects \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
