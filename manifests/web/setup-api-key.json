{
  "name": "setup-api-key",
  "version": "0.1.0",
  "category": "web",
  "description": "Guides users through setting up an ElevenLabs API key for ElevenLabs MCP tools.",
  "intent_triggers": [
    "use the setup api key agent skill",
    "run a workflow for setup api key",
    "help me with setup api key",
    "我需要處理 setup api key 相關工作",
    "幫我啟用 setup api key 這個 skill",
    "用 agent skill 完成 setup api key",
    "use setup api key from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the setup api key skill for this task.",
      "invocation": "codex --skill setup-api-key \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
