{
  "name": "debugging-local-replay",
  "version": "0.1.0",
  "category": "web",
  "description": "Agent skill for debugging local replay workflows.",
  "intent_triggers": [
    "use the debugging local replay agent skill",
    "run a workflow for debugging local replay",
    "help me with debugging local replay",
    "我需要處理 debugging local replay 相關工作",
    "幫我啟用 debugging local replay 這個 skill",
    "用 agent skill 完成 debugging local replay"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the debugging local replay skill for this task.",
      "invocation": "codex --skill debugging-local-replay \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mit",
    "skill",
    "web"
  ]
}
