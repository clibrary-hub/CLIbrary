{
  "name": "gog",
  "version": "0.1.0",
  "category": "web",
  "description": "Google Workspace CLI for Gmail, Calendar, Drive, Contacts, Sheets, and Docs.",
  "intent_triggers": [
    "use the gog agent skill",
    "run a workflow for gog",
    "help me with gog",
    "我需要處理 gog 相關工作",
    "幫我啟用 gog 這個 skill",
    "用 agent skill 完成 gog",
    "use gog from openclaw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the gog skill for this task.",
      "invocation": "codex --skill gog \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
