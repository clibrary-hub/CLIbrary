{
  "name": "azure-messaging-webpubsubservice-py",
  "version": "0.1.0",
  "category": "web",
  "description": "Agent skill for azure messaging webpubsubservice py workflows.",
  "intent_triggers": [
    "use the azure messaging webpubsubservice py agent skill",
    "run a workflow for azure messaging webpubsubservice py",
    "help me with azure messaging webpubsubservice py",
    "我需要處理 azure messaging webpubsubservice py 相關工作",
    "幫我啟用 azure messaging webpubsubservice py 這個 skill",
    "用 agent skill 完成 azure messaging webpubsubservice py",
    "use azure messaging webpubsubservice py from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the azure messaging webpubsubservice py skill for this task.",
      "invocation": "codex --skill azure-messaging-webpubsubservice-py \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
