{
  "name": "shopify-polaris-app-home",
  "version": "0.1.0",
  "category": "web",
  "description": "Build your app's primary user interface embedded in the Shopify admin.",
  "intent_triggers": [
    "use the shopify polaris app home agent skill",
    "run a workflow for shopify polaris app home",
    "help me with shopify polaris app home",
    "我需要處理 shopify polaris app home 相關工作",
    "幫我啟用 shopify polaris app home 這個 skill",
    "用 agent skill 完成 shopify polaris app home",
    "use shopify polaris app home from shopify-ai-toolkit"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the shopify polaris app home skill for this task.",
      "invocation": "codex --skill shopify-polaris-app-home \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
