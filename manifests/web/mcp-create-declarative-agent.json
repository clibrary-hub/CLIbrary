{
  "name": "mcp-create-declarative-agent",
  "version": "0.1.0",
  "category": "web",
  "description": "Skill converted from mcp-create-declarative-agent.prompt.md",
  "intent_triggers": [
    "use the mcp create declarative agent agent skill",
    "run a workflow for mcp create declarative agent",
    "help me with mcp create declarative agent",
    "我需要處理 mcp create declarative agent 相關工作",
    "幫我啟用 mcp create declarative agent 這個 skill",
    "用 agent skill 完成 mcp create declarative agent",
    "use mcp create declarative agent from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mcp create declarative agent skill for this task.",
      "invocation": "codex --skill mcp-create-declarative-agent \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
