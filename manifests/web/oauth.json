{
  "name": "oauth",
  "version": "0.1.0",
  "category": "web",
  "description": "Configure OAuth providers (Google, Apple, Microsoft, Facebook, GitHub, etc.) to work with portless local dev URLs.",
  "intent_triggers": [
    "use the oauth agent skill",
    "run a workflow for oauth",
    "help me with oauth",
    "我需要處理 oauth 相關工作",
    "幫我啟用 oauth 這個 skill",
    "用 agent skill 完成 oauth",
    "use oauth from portless"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the oauth skill for this task.",
      "invocation": "codex --skill oauth \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
