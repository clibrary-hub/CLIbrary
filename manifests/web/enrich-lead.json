{
  "name": "enrich-lead",
  "version": "0.1.0",
  "category": "web",
  "description": "Instant lead enrichment.",
  "intent_triggers": [
    "use the enrich lead agent skill",
    "run a workflow for enrich lead",
    "help me with enrich lead",
    "我需要處理 enrich lead 相關工作",
    "幫我啟用 enrich lead 這個 skill",
    "用 agent skill 完成 enrich lead",
    "use enrich lead from knowledge-work-plugins"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the enrich lead skill for this task.",
      "invocation": "codex --skill enrich-lead \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
