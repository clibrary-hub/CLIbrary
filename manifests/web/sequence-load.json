{
  "name": "sequence-load",
  "version": "0.1.0",
  "category": "web",
  "description": "Find leads matching criteria and bulk-add them to an Apollo outreach sequence.",
  "intent_triggers": [
    "use the sequence load agent skill",
    "run a workflow for sequence load",
    "help me with sequence load",
    "我需要處理 sequence load 相關工作",
    "幫我啟用 sequence load 這個 skill",
    "用 agent skill 完成 sequence load",
    "use sequence load from knowledge-work-plugins"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sequence load skill for this task.",
      "invocation": "codex --skill sequence-load \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
