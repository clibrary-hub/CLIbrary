{
  "name": "sentry-setup-tracing",
  "version": "0.1.0",
  "category": "web",
  "description": "Setup Sentry Tracing (Performance Monitoring) in any project.",
  "intent_triggers": [
    "use the sentry setup tracing agent skill",
    "run a workflow for sentry setup tracing",
    "help me with sentry setup tracing",
    "我需要處理 sentry setup tracing 相關工作",
    "幫我啟用 sentry setup tracing 這個 skill",
    "用 agent skill 完成 sentry setup tracing",
    "use sentry setup tracing from sentry-for-cursor"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sentry setup tracing skill for this task.",
      "invocation": "codex --skill sentry-setup-tracing \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
