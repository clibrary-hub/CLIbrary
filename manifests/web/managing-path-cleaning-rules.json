{
  "name": "managing-path-cleaning-rules",
  "version": "0.1.0",
  "category": "web",
  "description": "Inspects URL paths and proposes, tests, orders, and applies project-level path cleaning rules so dynamic segments (numeric IDs, UUIDs, slugs, dates) collapse into readable aliases.",
  "intent_triggers": [
    "use the managing path cleaning rules agent skill",
    "run a workflow for managing path cleaning rules",
    "help me with managing path cleaning rules",
    "我需要處理 managing path cleaning rules 相關工作",
    "幫我啟用 managing path cleaning rules 這個 skill",
    "用 agent skill 完成 managing path cleaning rules"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the managing path cleaning rules skill for this task.",
      "invocation": "codex --skill managing-path-cleaning-rules \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mit",
    "skill",
    "web"
  ]
}
