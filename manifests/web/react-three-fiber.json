{
  "name": "react-three-fiber",
  "version": "0.1.0",
  "category": "web",
  "description": "React Three Fiber 3D renderer for json-render.",
  "intent_triggers": [
    "use the react three fiber agent skill",
    "run a workflow for react three fiber",
    "help me with react three fiber",
    "我需要處理 react three fiber 相關工作",
    "幫我啟用 react three fiber 這個 skill",
    "用 agent skill 完成 react three fiber",
    "use react three fiber from json-render"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the react three fiber skill for this task.",
      "invocation": "codex --skill react-three-fiber \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
