{
  "name": "prisma-postgres",
  "version": "0.1.0",
  "category": "web",
  "description": "Prisma Postgres setup and operations guidance across Console, create-db CLI, Management API, and Management API SDK.",
  "intent_triggers": [
    "use the prisma postgres agent skill",
    "run a workflow for prisma postgres",
    "help me with prisma postgres",
    "我需要處理 prisma postgres 相關工作",
    "幫我啟用 prisma postgres 這個 skill",
    "用 agent skill 完成 prisma postgres",
    "use prisma postgres from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the prisma postgres skill for this task.",
      "invocation": "codex --skill prisma-postgres \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
