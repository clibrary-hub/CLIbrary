{
  "name": "cloudflare-browser",
  "version": "0.1.0",
  "category": "web",
  "description": "Control headless Chrome via Cloudflare Browser Rendering CDP WebSocket.",
  "intent_triggers": [
    "use the cloudflare browser agent skill",
    "run a workflow for cloudflare browser",
    "help me with cloudflare browser",
    "我需要處理 cloudflare browser 相關工作",
    "幫我啟用 cloudflare browser 這個 skill",
    "用 agent skill 完成 cloudflare browser",
    "use cloudflare browser from moltworker"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the cloudflare browser skill for this task.",
      "invocation": "codex --skill cloudflare-browser \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
