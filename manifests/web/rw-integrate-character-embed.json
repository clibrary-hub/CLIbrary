{
  "name": "rw-integrate-character-embed",
  "version": "0.1.0",
  "category": "web",
  "description": "Help users embed Runway Character avatar calls in React apps using the @runwayml/avatars-react SDK",
  "intent_triggers": [
    "use the rw integrate character embed agent skill",
    "run a workflow for rw integrate character embed",
    "help me with rw integrate character embed",
    "我需要處理 rw integrate character embed 相關工作",
    "幫我啟用 rw integrate character embed 這個 skill",
    "用 agent skill 完成 rw integrate character embed",
    "use rw integrate character embed from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the rw integrate character embed skill for this task.",
      "invocation": "codex --skill rw-integrate-character-embed \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
