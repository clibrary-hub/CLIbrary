{
  "name": "react18-legacy-context",
  "version": "0.1.0",
  "category": "web",
  "description": "Provides the complete migration pattern for React legacy context API (contextTypes, childContextTypes, getChildContext) to the modern createContext API.",
  "intent_triggers": [
    "use the react18 legacy context agent skill",
    "run a workflow for react18 legacy context",
    "help me with react18 legacy context",
    "我需要處理 react18 legacy context 相關工作",
    "幫我啟用 react18 legacy context 這個 skill",
    "用 agent skill 完成 react18 legacy context",
    "use react18 legacy context from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the react18 legacy context skill for this task.",
      "invocation": "codex --skill react18-legacy-context \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
