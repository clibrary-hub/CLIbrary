{
  "name": "mapbox-store-locator-patterns",
  "version": "0.1.0",
  "category": "web",
  "description": "Common patterns for building store locators, restaurant finders, and location-based search applications with Mapbox.",
  "intent_triggers": [
    "use the mapbox store locator patterns agent skill",
    "run a workflow for mapbox store locator patterns",
    "help me with mapbox store locator patterns",
    "我需要處理 mapbox store locator patterns 相關工作",
    "幫我啟用 mapbox store locator patterns 這個 skill",
    "用 agent skill 完成 mapbox store locator patterns",
    "use mapbox store locator patterns from mapbox-agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mapbox store locator patterns skill for this task.",
      "invocation": "codex --skill mapbox-store-locator-patterns \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
