{
  "name": "typespec-create-agent",
  "version": "0.1.0",
  "category": "web",
  "description": "Generate a complete TypeSpec declarative agent with instructions, capabilities, and conversation starters for Microsoft 365 Copilot",
  "intent_triggers": [
    "use the typespec create agent agent skill",
    "run a workflow for typespec create agent",
    "help me with typespec create agent",
    "我需要處理 typespec create agent 相關工作",
    "幫我啟用 typespec create agent 這個 skill",
    "用 agent skill 完成 typespec create agent",
    "use typespec create agent from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the typespec create agent skill for this task.",
      "invocation": "codex --skill typespec-create-agent \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
