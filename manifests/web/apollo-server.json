{
  "name": "apollo-server",
  "version": "0.1.0",
  "category": "web",
  "description": "Agent skill for apollo server workflows.",
  "intent_triggers": [
    "use the apollo server agent skill",
    "run a workflow for apollo server",
    "help me with apollo server",
    "我需要處理 apollo server 相關工作",
    "幫我啟用 apollo server 這個 skill",
    "用 agent skill 完成 apollo server",
    "use apollo server from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the apollo server skill for this task.",
      "invocation": "codex --skill apollo-server \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
