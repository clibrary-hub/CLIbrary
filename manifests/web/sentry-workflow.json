{
  "name": "sentry-workflow",
  "version": "0.1.0",
  "category": "web",
  "description": "Fix production issues and review code with Sentry context.",
  "intent_triggers": [
    "use the sentry workflow agent skill",
    "run a workflow for sentry workflow",
    "help me with sentry workflow",
    "我需要處理 sentry workflow 相關工作",
    "幫我啟用 sentry workflow 這個 skill",
    "用 agent skill 完成 sentry workflow",
    "use sentry workflow from sentry-for-ai"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sentry workflow skill for this task.",
      "invocation": "codex --skill sentry-workflow \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
