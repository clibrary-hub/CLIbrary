{
  "name": "zustand",
  "version": "0.1.0",
  "category": "web",
  "description": "Zustand adapter for json-render's StateStore interface.",
  "intent_triggers": [
    "use the zustand agent skill",
    "run a workflow for zustand",
    "help me with zustand",
    "我需要處理 zustand 相關工作",
    "幫我啟用 zustand 這個 skill",
    "用 agent skill 完成 zustand",
    "use zustand from json-render"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the zustand skill for this task.",
      "invocation": "codex --skill zustand \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
