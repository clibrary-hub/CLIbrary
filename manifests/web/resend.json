{
  "name": "resend",
  "version": "0.1.0",
  "category": "web",
  "description": "Webhook signing secret for verifying event payloads.",
  "intent_triggers": [
    "use the resend agent skill",
    "run a workflow for resend",
    "help me with resend",
    "我需要處理 resend 相關工作",
    "幫我啟用 resend 這個 skill",
    "用 agent skill 完成 resend",
    "use resend from resend-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the resend skill for this task.",
      "invocation": "codex --skill resend \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
