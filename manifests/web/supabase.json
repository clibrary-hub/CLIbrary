{
  "name": "supabase",
  "version": "0.1.0",
  "category": "web",
  "description": "Use when doing ANY task involving Supabase.",
  "intent_triggers": [
    "use the supabase agent skill",
    "run a workflow for supabase",
    "help me with supabase",
    "我需要處理 supabase 相關工作",
    "幫我啟用 supabase 這個 skill",
    "用 agent skill 完成 supabase",
    "use supabase from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the supabase skill for this task.",
      "invocation": "codex --skill supabase \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
