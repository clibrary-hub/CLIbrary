{
  "name": "durable-objects",
  "version": "0.1.0",
  "category": "web",
  "description": "Create and review Cloudflare Durable Objects.",
  "intent_triggers": [
    "use the durable objects agent skill",
    "run a workflow for durable objects",
    "help me with durable objects",
    "我需要處理 durable objects 相關工作",
    "幫我啟用 durable objects 這個 skill",
    "用 agent skill 完成 durable objects",
    "use durable objects from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the durable objects skill for this task.",
      "invocation": "codex --skill durable-objects \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
