{
  "name": "slack-msw-tests",
  "version": "0.1.0",
  "category": "web",
  "description": "Author Slack HTTP integration tests using the repository MSW harness and fixture factories.",
  "intent_triggers": [
    "use the slack msw tests agent skill",
    "run a workflow for slack msw tests",
    "help me with slack msw tests",
    "我需要處理 slack msw tests 相關工作",
    "幫我啟用 slack msw tests 這個 skill",
    "用 agent skill 完成 slack msw tests",
    "use slack msw tests from junior"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the slack msw tests skill for this task.",
      "invocation": "codex --skill slack-msw-tests \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
