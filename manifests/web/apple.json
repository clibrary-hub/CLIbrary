{
  "name": "apple",
  "version": "0.1.0",
  "category": "web",
  "description": "Emulated Sign in with Apple / Apple OIDC for local development and testing.",
  "intent_triggers": [
    "use the apple agent skill",
    "run a workflow for apple",
    "help me with apple",
    "我需要處理 apple 相關工作",
    "幫我啟用 apple 這個 skill",
    "用 agent skill 完成 apple",
    "use apple from emulate"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the apple skill for this task.",
      "invocation": "codex --skill apple \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
