{
  "name": "cms-variants-personalization",
  "version": "0.1.0",
  "category": "web",
  "description": "Advise developers on using Contentstack Variants and Personalize for audience-targeted content.",
  "intent_triggers": [
    "use the cms variants personalization agent skill",
    "run a workflow for cms variants personalization",
    "help me with cms variants personalization",
    "我需要處理 cms variants personalization 相關工作",
    "幫我啟用 cms variants personalization 這個 skill",
    "用 agent skill 完成 cms variants personalization",
    "use cms variants personalization from contentstack-agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the cms variants personalization skill for this task.",
      "invocation": "codex --skill cms-variants-personalization \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
