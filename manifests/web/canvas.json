{
  "name": "canvas",
  "version": "0.1.0",
  "category": "web",
  "description": "Canvas LMS integration — fetch enrolled courses and assignments using API token authentication.",
  "intent_triggers": [
    "use the canvas agent skill",
    "run a workflow for canvas",
    "help me with canvas",
    "我需要處理 canvas 相關工作",
    "幫我啟用 canvas 這個 skill",
    "用 agent skill 完成 canvas",
    "use canvas from hermes-agent-zh"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the canvas skill for this task.",
      "invocation": "codex --skill canvas \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mit",
    "skills-rest",
    "web"
  ]
}
