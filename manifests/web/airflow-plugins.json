{
  "name": "airflow-plugins",
  "version": "0.1.0",
  "category": "web",
  "description": "Build Airflow 3.1+ plugins that embed FastAPI apps, custom UI pages, React components, middleware, macros, and operator links directly into the Airflow UI.",
  "intent_triggers": [
    "use the airflow plugins agent skill",
    "run a workflow for airflow plugins",
    "help me with airflow plugins",
    "我需要處理 airflow plugins 相關工作",
    "幫我啟用 airflow plugins 這個 skill",
    "用 agent skill 完成 airflow plugins",
    "use airflow plugins from agents"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the airflow plugins skill for this task.",
      "invocation": "codex --skill airflow-plugins \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
