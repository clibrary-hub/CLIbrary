{
  "name": "deno-project-templates",
  "version": "0.1.0",
  "category": "web",
  "description": "Use when scaffolding new Deno projects.",
  "intent_triggers": [
    "use the deno project templates agent skill",
    "run a workflow for deno project templates",
    "help me with deno project templates",
    "我需要處理 deno project templates 相關工作",
    "幫我啟用 deno project templates 這個 skill",
    "用 agent skill 完成 deno project templates",
    "use deno project templates from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the deno project templates skill for this task.",
      "invocation": "codex --skill deno-project-templates \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
