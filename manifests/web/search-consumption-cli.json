{
  "name": "search-consumption-cli",
  "version": "0.1.0",
  "category": "web",
  "description": "Agent skill for search consumption cli workflows.",
  "intent_triggers": [
    "use the search consumption cli agent skill",
    "run a workflow for search consumption cli",
    "help me with search consumption cli",
    "我需要處理 search consumption cli 相關工作",
    "幫我啟用 search consumption cli 這個 skill",
    "用 agent skill 完成 search consumption cli",
    "use search consumption cli from skills-for-fabric"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the search consumption cli skill for this task.",
      "invocation": "codex --skill search-consumption-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
