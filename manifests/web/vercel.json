{
  "name": "vercel",
  "version": "0.1.0",
  "category": "web",
  "description": "Emulated Vercel REST API for local development and testing.",
  "intent_triggers": [
    "use the vercel agent skill",
    "run a workflow for vercel",
    "help me with vercel",
    "我需要處理 vercel 相關工作",
    "幫我啟用 vercel 這個 skill",
    "用 agent skill 完成 vercel",
    "use vercel from emulate"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the vercel skill for this task.",
      "invocation": "codex --skill vercel \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
