{
  "name": "shopify-app-store-review",
  "version": "0.1.0",
  "category": "web",
  "description": "Run a pre-submission compliance check against your Shopify app's codebase.",
  "intent_triggers": [
    "use the shopify app store review agent skill",
    "run a workflow for shopify app store review",
    "help me with shopify app store review",
    "我需要處理 shopify app store review 相關工作",
    "幫我啟用 shopify app store review 這個 skill",
    "用 agent skill 完成 shopify app store review",
    "use shopify app store review from shopify-ai-toolkit"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the shopify app store review skill for this task.",
      "invocation": "codex --skill shopify-app-store-review \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
