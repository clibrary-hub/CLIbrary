{
  "name": "react18-batching-patterns",
  "version": "0.1.0",
  "category": "web",
  "description": "Provides exact patterns for diagnosing and fixing automatic batching regressions in React 18 class components.",
  "intent_triggers": [
    "use the react18 batching patterns agent skill",
    "run a workflow for react18 batching patterns",
    "help me with react18 batching patterns",
    "我需要處理 react18 batching patterns 相關工作",
    "幫我啟用 react18 batching patterns 這個 skill",
    "用 agent skill 完成 react18 batching patterns",
    "use react18 batching patterns from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the react18 batching patterns skill for this task.",
      "invocation": "codex --skill react18-batching-patterns \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
