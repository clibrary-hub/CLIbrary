{
  "name": "upstash-qstash-js",
  "version": "0.1.0",
  "category": "web",
  "description": "Work with the QStash TypeScript/JavaScript SDK for serverless messaging, scheduling.",
  "intent_triggers": [
    "use the upstash qstash js agent skill",
    "run a workflow for upstash qstash js",
    "help me with upstash qstash js",
    "我需要處理 upstash qstash js 相關工作",
    "幫我啟用 upstash qstash js 這個 skill",
    "用 agent skill 完成 upstash qstash js",
    "use upstash qstash js from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the upstash qstash js skill for this task.",
      "invocation": "codex --skill upstash-qstash-js \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
