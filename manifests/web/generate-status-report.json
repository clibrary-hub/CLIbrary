{
  "name": "generate-status-report",
  "version": "0.1.0",
  "category": "web",
  "description": "Generate project status reports from Jira issues and publish to Confluence.",
  "intent_triggers": [
    "use the generate status report agent skill",
    "run a workflow for generate status report",
    "help me with generate status report",
    "我需要處理 generate status report 相關工作",
    "幫我啟用 generate status report 這個 skill",
    "用 agent skill 完成 generate status report",
    "use generate status report from atlassian-mcp-server"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the generate status report skill for this task.",
      "invocation": "codex --skill generate-status-report \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
