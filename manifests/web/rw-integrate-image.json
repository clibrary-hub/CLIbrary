{
  "name": "rw-integrate-image",
  "version": "0.1.0",
  "category": "web",
  "description": "Help users integrate Runway image generation APIs (text-to-image with reference images)",
  "intent_triggers": [
    "use the rw integrate image agent skill",
    "run a workflow for rw integrate image",
    "help me with rw integrate image",
    "我需要處理 rw integrate image 相關工作",
    "幫我啟用 rw integrate image 這個 skill",
    "用 agent skill 完成 rw integrate image",
    "use rw integrate image from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the rw integrate image skill for this task.",
      "invocation": "codex --skill rw-integrate-image \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
