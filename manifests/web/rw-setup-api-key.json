{
  "name": "rw-setup-api-key",
  "version": "0.1.0",
  "category": "web",
  "description": "Guide users through obtaining and configuring a Runway API key",
  "intent_triggers": [
    "use the rw setup api key agent skill",
    "run a workflow for rw setup api key",
    "help me with rw setup api key",
    "我需要處理 rw setup api key 相關工作",
    "幫我啟用 rw setup api key 這個 skill",
    "用 agent skill 完成 rw setup api key",
    "use rw setup api key from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the rw setup api key skill for this task.",
      "invocation": "codex --skill rw-setup-api-key \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
