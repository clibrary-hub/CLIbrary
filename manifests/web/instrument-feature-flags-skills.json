{
  "name": "instrument-feature-flags-skills",
  "version": "0.1.0",
  "category": "web",
  "description": "Agent skill for instrument feature flags workflows.",
  "intent_triggers": [
    "use the instrument feature flags agent skill",
    "run a workflow for instrument feature flags",
    "help me with instrument feature flags",
    "我需要處理 instrument feature flags 相關工作",
    "幫我啟用 instrument feature flags 這個 skill",
    "用 agent skill 完成 instrument feature flags",
    "use instrument feature flags from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the instrument feature flags skill for this task.",
      "invocation": "codex --skill instrument-feature-flags \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
