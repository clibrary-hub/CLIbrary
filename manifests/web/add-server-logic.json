{
  "name": "add-server-logic",
  "version": "0.1.0",
  "category": "web",
  "description": "Agent skill for add server logic workflows.",
  "intent_triggers": [
    "use the add server logic agent skill",
    "run a workflow for add server logic",
    "help me with add server logic",
    "我需要處理 add server logic 相關工作",
    "幫我啟用 add server logic 這個 skill",
    "用 agent skill 完成 add server logic",
    "use add server logic from power-platform-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the add server logic skill for this task.",
      "invocation": "codex --skill add-server-logic \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
