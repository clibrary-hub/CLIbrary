{
  "name": "release-dbt-mcp",
  "version": "0.1.0",
  "category": "web",
  "description": "Release a new version of dbt-mcp to PyPi",
  "intent_triggers": [
    "use the release dbt mcp agent skill",
    "run a workflow for release dbt mcp",
    "help me with release dbt mcp",
    "我需要處理 release dbt mcp 相關工作",
    "幫我啟用 release dbt mcp 這個 skill",
    "用 agent skill 完成 release dbt mcp",
    "use release dbt mcp from dbt-mcp"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the release dbt mcp skill for this task.",
      "invocation": "codex --skill release-dbt-mcp \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
