{
  "name": "suggest",
  "version": "0.1.0",
  "category": "web",
  "description": "USE FOR query autocomplete/suggestions.",
  "intent_triggers": [
    "use the suggest agent skill",
    "run a workflow for suggest",
    "help me with suggest",
    "我需要處理 suggest 相關工作",
    "幫我啟用 suggest 這個 skill",
    "用 agent skill 完成 suggest",
    "use suggest from brave-search-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the suggest skill for this task.",
      "invocation": "codex --skill suggest \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
