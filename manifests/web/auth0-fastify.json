{
  "name": "auth0-fastify",
  "version": "0.1.0",
  "category": "web",
  "description": "Use when adding authentication (login, logout, protected routes) to Fastify web applications - integrates @auth0/auth0-fastify for session-based auth.",
  "intent_triggers": [
    "use the auth0 fastify agent skill",
    "run a workflow for auth0 fastify",
    "help me with auth0 fastify",
    "我需要處理 auth0 fastify 相關工作",
    "幫我啟用 auth0 fastify 這個 skill",
    "用 agent skill 完成 auth0 fastify",
    "use auth0 fastify from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the auth0 fastify skill for this task.",
      "invocation": "codex --skill auth0-fastify \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
