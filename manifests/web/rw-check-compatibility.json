{
  "name": "rw-check-compatibility",
  "version": "0.1.0",
  "category": "web",
  "description": "Analyze a user's codebase to verify it can use Runway's public API (server-side requirement)",
  "intent_triggers": [
    "use the rw check compatibility agent skill",
    "run a workflow for rw check compatibility",
    "help me with rw check compatibility",
    "我需要處理 rw check compatibility 相關工作",
    "幫我啟用 rw check compatibility 這個 skill",
    "用 agent skill 完成 rw check compatibility",
    "use rw check compatibility from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the rw check compatibility skill for this task.",
      "invocation": "codex --skill rw-check-compatibility \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
