{
  "name": "rnn-codebase",
  "version": "0.1.0",
  "category": "web",
  "description": "Navigate and work with the react-native-navigation (RNN) codebase.",
  "intent_triggers": [
    "use the rnn codebase agent skill",
    "run a workflow for rnn codebase",
    "help me with rnn codebase",
    "我需要處理 rnn codebase 相關工作",
    "幫我啟用 rnn codebase 這個 skill",
    "用 agent skill 完成 rnn codebase",
    "use rnn codebase from react-native-navigation"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the rnn codebase skill for this task.",
      "invocation": "codex --skill rnn-codebase \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
