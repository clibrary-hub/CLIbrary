{
  "name": "azure-identity-ts-skills",
  "version": "0.1.0",
  "category": "web",
  "description": "Authenticate to Azure services using Azure Identity library for JavaScript (@azure/identity).",
  "intent_triggers": [
    "use the azure identity ts agent skill",
    "run a workflow for azure identity ts",
    "help me with azure identity ts",
    "我需要處理 azure identity ts 相關工作",
    "幫我啟用 azure identity ts 這個 skill",
    "用 agent skill 完成 azure identity ts",
    "use azure identity ts from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the azure identity ts skill for this task.",
      "invocation": "codex --skill azure-identity-ts \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
