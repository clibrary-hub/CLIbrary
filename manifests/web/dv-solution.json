{
  "name": "dv-solution",
  "version": "0.1.0",
  "category": "web",
  "description": "Dataverse solution lifecycle — create, export, import, promote across environments, and validate deployments.",
  "intent_triggers": [
    "use the dv solution agent skill",
    "run a workflow for dv solution",
    "help me with dv solution",
    "我需要處理 dv solution 相關工作",
    "幫我啟用 dv solution 這個 skill",
    "用 agent skill 完成 dv solution",
    "use dv solution from dataverse-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the dv solution skill for this task.",
      "invocation": "codex --skill dv-solution \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
