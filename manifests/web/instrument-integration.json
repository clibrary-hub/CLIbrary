{
  "name": "instrument-integration",
  "version": "0.1.0",
  "category": "web",
  "description": "Agent skill for instrument integration workflows.",
  "intent_triggers": [
    "use the instrument integration agent skill",
    "run a workflow for instrument integration",
    "help me with instrument integration",
    "我需要處理 instrument integration 相關工作",
    "幫我啟用 instrument integration 這個 skill",
    "用 agent skill 完成 instrument integration"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the instrument integration skill for this task.",
      "invocation": "codex --skill instrument-integration \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mit",
    "skill",
    "web"
  ]
}
