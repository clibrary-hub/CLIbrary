{
  "name": "parallel-cli",
  "version": "0.1.0",
  "category": "web",
  "description": "Optional vendor skill for Parallel CLI — agent-native web search, extraction, deep research, enrichment, FindAll, and monitoring.",
  "intent_triggers": [
    "use the parallel cli agent skill",
    "run a workflow for parallel cli",
    "help me with parallel cli",
    "我需要處理 parallel cli 相關工作",
    "幫我啟用 parallel cli 這個 skill",
    "用 agent skill 完成 parallel cli",
    "use parallel cli from hermes-agent-zh"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the parallel cli skill for this task.",
      "invocation": "codex --skill parallel-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mit",
    "skills-rest",
    "web"
  ]
}
