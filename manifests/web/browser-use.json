{
  "name": "browser-use",
  "version": "0.1.0",
  "category": "web",
  "description": "Automates browser interactions for web testing, form filling, screenshots, and data extraction.",
  "intent_triggers": [
    "use the browser use agent skill",
    "run a workflow for browser use",
    "help me with browser use",
    "我需要處理 browser use 相關工作",
    "幫我啟用 browser use 這個 skill",
    "用 agent skill 完成 browser use",
    "use browser use from browser-use"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the browser use skill for this task.",
      "invocation": "codex --skill browser-use \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
