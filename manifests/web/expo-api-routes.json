{
  "name": "expo-api-routes",
  "version": "0.1.0",
  "category": "web",
  "description": "Guidelines for creating API routes in Expo Router with EAS Hosting",
  "intent_triggers": [
    "use the expo api routes agent skill",
    "run a workflow for expo api routes",
    "help me with expo api routes",
    "我需要處理 expo api routes 相關工作",
    "幫我啟用 expo api routes 這個 skill",
    "用 agent skill 完成 expo api routes"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the expo api routes skill for this task.",
      "invocation": "codex --skill expo-api-routes \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mit",
    "skill",
    "web"
  ]
}
