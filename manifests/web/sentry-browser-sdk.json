{
  "name": "sentry-browser-sdk",
  "version": "0.1.0",
  "category": "web",
  "description": "Full Sentry SDK setup for browser JavaScript.",
  "intent_triggers": [
    "use the sentry browser sdk agent skill",
    "run a workflow for sentry browser sdk",
    "help me with sentry browser sdk",
    "我需要處理 sentry browser sdk 相關工作",
    "幫我啟用 sentry browser sdk 這個 skill",
    "用 agent skill 完成 sentry browser sdk",
    "use sentry browser sdk from sentry-for-ai"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sentry browser sdk skill for this task.",
      "invocation": "codex --skill sentry-browser-sdk \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
