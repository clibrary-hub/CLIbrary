{
  "name": "error-tracking-web",
  "version": "0.1.0",
  "category": "web",
  "description": "PostHog error tracking for Web (JavaScript)",
  "intent_triggers": [
    "use the error tracking web agent skill",
    "run a workflow for error tracking web",
    "help me with error tracking web",
    "我需要處理 error tracking web 相關工作",
    "幫我啟用 error tracking web 這個 skill",
    "用 agent skill 完成 error tracking web",
    "use error tracking web from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the error tracking web skill for this task.",
      "invocation": "codex --skill error-tracking-web \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
