{
  "name": "expo-ui-jetpack-compose",
  "version": "0.1.0",
  "category": "web",
  "description": "`@expo/ui/jetpack-compose` package lets you use Jetpack Compose Views and modifiers in your app.",
  "intent_triggers": [
    "use the expo ui jetpack compose agent skill",
    "run a workflow for expo ui jetpack compose",
    "help me with expo ui jetpack compose",
    "我需要處理 expo ui jetpack compose 相關工作",
    "幫我啟用 expo ui jetpack compose 這個 skill",
    "用 agent skill 完成 expo ui jetpack compose"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the expo ui jetpack compose skill for this task.",
      "invocation": "codex --skill expo-ui-jetpack-compose \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mit",
    "skill",
    "web"
  ]
}
