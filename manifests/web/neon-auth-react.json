{
  "name": "neon-auth-react",
  "version": "0.1.0",
  "category": "web",
  "description": "Sets up Neon Auth in React applications (Vite, CRA).",
  "intent_triggers": [
    "use the neon auth react agent skill",
    "run a workflow for neon auth react",
    "help me with neon auth react",
    "我需要處理 neon auth react 相關工作",
    "幫我啟用 neon auth react 這個 skill",
    "用 agent skill 完成 neon auth react",
    "use neon auth react from neon-js"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the neon auth react skill for this task.",
      "invocation": "codex --skill neon-auth-react \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
