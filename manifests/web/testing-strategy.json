{
  "name": "testing-strategy",
  "version": "0.1.0",
  "category": "web",
  "description": "Design test strategies and test plans.",
  "intent_triggers": [
    "use the testing strategy agent skill",
    "run a workflow for testing strategy",
    "help me with testing strategy",
    "我需要處理 testing strategy 相關工作",
    "幫我啟用 testing strategy 這個 skill",
    "用 agent skill 完成 testing strategy",
    "use testing strategy from knowledge-work-plugins"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the testing strategy skill for this task.",
      "invocation": "codex --skill testing-strategy \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
