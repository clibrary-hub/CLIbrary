{
  "name": "spellcheck",
  "version": "0.1.0",
  "category": "web",
  "description": "USE FOR spell correction.",
  "intent_triggers": [
    "use the spellcheck agent skill",
    "run a workflow for spellcheck",
    "help me with spellcheck",
    "我需要處理 spellcheck 相關工作",
    "幫我啟用 spellcheck 這個 skill",
    "用 agent skill 完成 spellcheck",
    "use spellcheck from brave-search-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the spellcheck skill for this task.",
      "invocation": "codex --skill spellcheck \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
