{
  "name": "sentry-ruby-sdk",
  "version": "0.1.0",
  "category": "web",
  "description": "Full Sentry SDK setup for Ruby.",
  "intent_triggers": [
    "use the sentry ruby sdk agent skill",
    "run a workflow for sentry ruby sdk",
    "help me with sentry ruby sdk",
    "我需要處理 sentry ruby sdk 相關工作",
    "幫我啟用 sentry ruby sdk 這個 skill",
    "用 agent skill 完成 sentry ruby sdk",
    "use sentry ruby sdk from sentry-for-ai"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sentry ruby sdk skill for this task.",
      "invocation": "codex --skill sentry-ruby-sdk \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
