{
  "name": "agent-auth-cli",
  "version": "0.1.0",
  "category": "web",
  "description": "Use the Agent Auth CLI (auth-agent) to discover providers, connect agents, manage capabilities, and execute operations.",
  "intent_triggers": [
    "use the agent auth cli agent skill",
    "run a workflow for agent auth cli",
    "help me with agent auth cli",
    "我需要處理 agent auth cli 相關工作",
    "幫我啟用 agent auth cli 這個 skill",
    "用 agent skill 完成 agent auth cli",
    "use agent auth cli from agent-auth"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the agent auth cli skill for this task.",
      "invocation": "codex --skill agent-auth-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
