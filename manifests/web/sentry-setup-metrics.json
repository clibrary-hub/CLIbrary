{
  "name": "sentry-setup-metrics",
  "version": "0.1.0",
  "category": "web",
  "description": "Setup Sentry Metrics in any project.",
  "intent_triggers": [
    "use the sentry setup metrics agent skill",
    "run a workflow for sentry setup metrics",
    "help me with sentry setup metrics",
    "我需要處理 sentry setup metrics 相關工作",
    "幫我啟用 sentry setup metrics 這個 skill",
    "用 agent skill 完成 sentry setup metrics",
    "use sentry setup metrics from sentry-for-cursor"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sentry setup metrics skill for this task.",
      "invocation": "codex --skill sentry-setup-metrics \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
