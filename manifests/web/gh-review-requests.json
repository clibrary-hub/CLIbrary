{
  "name": "gh-review-requests",
  "version": "0.1.0",
  "category": "web",
  "description": "Fetch unread GitHub notifications for open PRs where review is requested from a specified team or opened by a team member.",
  "intent_triggers": [
    "use the gh review requests agent skill",
    "run a workflow for gh review requests",
    "help me with gh review requests",
    "我需要處理 gh review requests 相關工作",
    "幫我啟用 gh review requests 這個 skill",
    "用 agent skill 完成 gh review requests",
    "use gh review requests from sentry-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the gh review requests skill for this task.",
      "invocation": "codex --skill gh-review-requests \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "mcpservers",
    "web"
  ]
}
