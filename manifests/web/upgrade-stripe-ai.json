{
  "name": "upgrade-stripe-ai",
  "version": "0.1.0",
  "category": "web",
  "description": "Guide for upgrading Stripe API versions and SDKs",
  "intent_triggers": [
    "use the upgrade stripe agent skill",
    "run a workflow for upgrade stripe",
    "help me with upgrade stripe",
    "我需要處理 upgrade stripe 相關工作",
    "幫我啟用 upgrade stripe 這個 skill",
    "用 agent skill 完成 upgrade stripe",
    "use upgrade stripe from ai"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the upgrade stripe skill for this task.",
      "invocation": "codex --skill upgrade-stripe \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "mcpservers",
    "mit",
    "web"
  ]
}
