{
  "name": "base",
  "version": "0.1.0",
  "category": "finance",
  "description": "Query Base (Ethereum L2) blockchain data with USD pricing — wallet balances, token info, transaction details, gas analysis, contract inspection, whale detection, and live network stats.",
  "intent_triggers": [
    "use the base agent skill",
    "run a workflow for base",
    "help me with base",
    "我需要處理 base 相關工作",
    "幫我啟用 base 這個 skill",
    "用 agent skill 完成 base",
    "use base from hermes-agent-zh"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the base skill for this task.",
      "invocation": "codex --skill base \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "finance",
    "mit",
    "skills-rest"
  ]
}
