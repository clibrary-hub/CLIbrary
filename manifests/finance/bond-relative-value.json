{
  "name": "bond-relative-value",
  "version": "0.1.0",
  "category": "finance",
  "description": "Perform relative value analysis on bonds by combining pricing, yield curve context, credit spreads, and scenario stress testing.",
  "intent_triggers": [
    "use the bond relative value agent skill",
    "run a workflow for bond relative value",
    "help me with bond relative value",
    "我需要處理 bond relative value 相關工作",
    "幫我啟用 bond relative value 這個 skill",
    "用 agent skill 完成 bond relative value",
    "use bond relative value from financial-services-plugins"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the bond relative value skill for this task.",
      "invocation": "codex --skill bond-relative-value \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "finance",
    "mcpservers"
  ]
}
