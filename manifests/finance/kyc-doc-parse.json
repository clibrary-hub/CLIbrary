{
  "name": "kyc-doc-parse",
  "version": "0.1.0",
  "category": "finance",
  "description": "Parse an investor or client onboarding packet into structured KYC fields — identity, ownership, control, source of funds, and document inventory.",
  "intent_triggers": [
    "use the kyc doc parse agent skill",
    "run a workflow for kyc doc parse",
    "help me with kyc doc parse",
    "我需要處理 kyc doc parse 相關工作",
    "幫我啟用 kyc doc parse 這個 skill",
    "用 agent skill 完成 kyc doc parse",
    "use kyc doc parse from financial-services-plugins"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the kyc doc parse skill for this task.",
      "invocation": "codex --skill kyc-doc-parse \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "finance",
    "mcpservers"
  ]
}
