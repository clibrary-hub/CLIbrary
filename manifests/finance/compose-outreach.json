{
  "name": "compose-outreach",
  "version": "0.1.0",
  "category": "finance",
  "description": "Generate personalized outreach messages using Common Room signals.",
  "intent_triggers": [
    "use the compose outreach agent skill",
    "run a workflow for compose outreach",
    "help me with compose outreach",
    "我需要處理 compose outreach 相關工作",
    "幫我啟用 compose outreach 這個 skill",
    "用 agent skill 完成 compose outreach",
    "use compose outreach from knowledge-work-plugins"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the compose outreach skill for this task.",
      "invocation": "codex --skill compose-outreach \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "finance",
    "mcpservers"
  ]
}
