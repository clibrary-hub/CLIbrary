{
  "name": "reconciliation",
  "version": "0.1.0",
  "category": "finance",
  "description": "Reconcile accounts by comparing GL balances to subledgers, bank statements, or third-party data.",
  "intent_triggers": [
    "use the reconciliation agent skill",
    "run a workflow for reconciliation",
    "help me with reconciliation",
    "我需要處理 reconciliation 相關工作",
    "幫我啟用 reconciliation 這個 skill",
    "用 agent skill 完成 reconciliation",
    "use reconciliation from knowledge-work-plugins"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the reconciliation skill for this task.",
      "invocation": "codex --skill reconciliation \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "finance",
    "mcpservers"
  ]
}
