{
  "name": "directives",
  "version": "0.1.0",
  "category": "finance",
  "description": "Pre-built custom directives for json-render — formatting, math, string manipulation, and i18n.",
  "intent_triggers": [
    "use the directives agent skill",
    "run a workflow for directives",
    "help me with directives",
    "我需要處理 directives 相關工作",
    "幫我啟用 directives 這個 skill",
    "用 agent skill 完成 directives",
    "use directives from json-render"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the directives skill for this task.",
      "invocation": "codex --skill directives \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "finance",
    "mcpservers"
  ]
}
