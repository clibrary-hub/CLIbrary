{
  "name": "qdrant-scaling-qps",
  "version": "0.1.0",
  "category": "finance",
  "description": "Guides Qdrant query throughput (QPS) scaling.",
  "intent_triggers": [
    "use the qdrant scaling qps agent skill",
    "run a workflow for qdrant scaling qps",
    "help me with qdrant scaling qps",
    "我需要處理 qdrant scaling qps 相關工作",
    "幫我啟用 qdrant scaling qps 這個 skill",
    "用 agent skill 完成 qdrant scaling qps"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the qdrant scaling qps skill for this task.",
      "invocation": "codex --skill qdrant-scaling-qps \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "finance",
    "mit",
    "skill"
  ]
}
