{
  "name": "datapack-builder-financial-services",
  "version": "0.1.0",
  "category": "finance",
  "description": "Build professional financial services data packs from various sources including CIMs, offering memorandums, SEC filings, web search, or MCP servers.",
  "intent_triggers": [
    "use the datapack builder agent skill",
    "run a workflow for datapack builder",
    "help me with datapack builder",
    "我需要處理 datapack builder 相關工作",
    "幫我啟用 datapack builder 這個 skill",
    "用 agent skill 完成 datapack builder",
    "use datapack builder from financial-services"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the datapack builder skill for this task.",
      "invocation": "codex --skill datapack-builder \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "finance",
    "mcpservers"
  ]
}
