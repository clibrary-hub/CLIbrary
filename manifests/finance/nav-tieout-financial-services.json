{
  "name": "nav-tieout-financial-services",
  "version": "0.1.0",
  "category": "finance",
  "description": "Tie an LP statement to the fund's NAV pack — recompute the LP's capital account from the NAV components and flag any line that doesn't agree.",
  "intent_triggers": [
    "use the nav tieout agent skill",
    "run a workflow for nav tieout",
    "help me with nav tieout",
    "我需要處理 nav tieout 相關工作",
    "幫我啟用 nav tieout 這個 skill",
    "用 agent skill 完成 nav tieout",
    "use nav tieout from financial-services"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the nav tieout skill for this task.",
      "invocation": "codex --skill nav-tieout \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "finance",
    "mcpservers"
  ]
}
