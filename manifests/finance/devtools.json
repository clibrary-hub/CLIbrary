{
  "name": "devtools",
  "version": "0.1.0",
  "category": "finance",
  "description": "Drop-in inspector panel for any json-render app.",
  "intent_triggers": [
    "use the devtools agent skill",
    "run a workflow for devtools",
    "help me with devtools",
    "我需要處理 devtools 相關工作",
    "幫我啟用 devtools 這個 skill",
    "用 agent skill 完成 devtools",
    "use devtools from json-render"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the devtools skill for this task.",
      "invocation": "codex --skill devtools \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "finance",
    "mcpservers"
  ]
}
