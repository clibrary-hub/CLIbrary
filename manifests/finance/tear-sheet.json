{
  "name": "tear-sheet",
  "version": "0.1.0",
  "category": "finance",
  "description": "Generate professional company tear sheets using S&P Capital IQ data via the Kensho LLM-ready API MCP server.",
  "intent_triggers": [
    "use the tear sheet agent skill",
    "run a workflow for tear sheet",
    "help me with tear sheet",
    "我需要處理 tear sheet 相關工作",
    "幫我啟用 tear sheet 這個 skill",
    "用 agent skill 完成 tear sheet",
    "use tear sheet from financial-services-plugins"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the tear sheet skill for this task.",
      "invocation": "codex --skill tear-sheet \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "finance",
    "mcpservers"
  ]
}
