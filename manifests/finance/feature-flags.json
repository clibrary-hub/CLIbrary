{
  "name": "feature-flags",
  "version": "0.1.0",
  "category": "finance",
  "description": "Use when feature flag tests fail, flags need updating, understanding @gate pragmas, debugging channel-specific test failures, or adding new flags to React.",
  "intent_triggers": [
    "use the feature flags agent skill",
    "run a workflow for feature flags",
    "help me with feature flags",
    "我需要處理 feature flags 相關工作",
    "幫我啟用 feature flags 這個 skill",
    "用 agent skill 完成 feature flags",
    "use feature flags from react"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the feature flags skill for this task.",
      "invocation": "codex --skill feature-flags \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "finance",
    "mcpservers",
    "mit"
  ]
}
