{
  "name": "recruiting-pipeline",
  "version": "0.1.0",
  "category": "finance",
  "description": "Track and manage recruiting pipeline stages.",
  "intent_triggers": [
    "use the recruiting pipeline agent skill",
    "run a workflow for recruiting pipeline",
    "help me with recruiting pipeline",
    "我需要處理 recruiting pipeline 相關工作",
    "幫我啟用 recruiting pipeline 這個 skill",
    "用 agent skill 完成 recruiting pipeline",
    "use recruiting pipeline from knowledge-work-plugins"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recruiting pipeline skill for this task.",
      "invocation": "codex --skill recruiting-pipeline \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "finance",
    "mcpservers"
  ]
}
