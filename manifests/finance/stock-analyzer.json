{
  "name": "stock-analyzer",
  "version": "0.1.0",
  "category": "finance",
  "description": "Agent skill for stock analyzer workflows.",
  "intent_triggers": [
    "use the stock analyzer agent skill",
    "run a workflow for stock analyzer",
    "help me with stock analyzer",
    "我需要處理 stock analyzer 相關工作",
    "幫我啟用 stock analyzer 這個 skill",
    "用 agent skill 完成 stock analyzer",
    "use stock analyzer from PopiStudio"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the stock analyzer skill for this task.",
      "invocation": "codex --skill stock-analyzer \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "finance",
    "mit",
    "skills-rest"
  ]
}
