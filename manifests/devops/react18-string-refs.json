{
  "name": "react18-string-refs",
  "version": "0.1.0",
  "category": "devops",
  "description": "Provides exact migration patterns for React string refs (ref=\"name\" + this.refs.name) to React.createRef() in class components.",
  "intent_triggers": [
    "use the react18 string refs agent skill",
    "run a workflow for react18 string refs",
    "help me with react18 string refs",
    "我需要處理 react18 string refs 相關工作",
    "幫我啟用 react18 string refs 這個 skill",
    "用 agent skill 完成 react18 string refs",
    "use react18 string refs from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the react18 string refs skill for this task.",
      "invocation": "codex --skill react18-string-refs \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
