{
  "name": "powerbi-authoring-cli",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for powerbi authoring cli workflows.",
  "intent_triggers": [
    "use the powerbi authoring cli agent skill",
    "run a workflow for powerbi authoring cli",
    "help me with powerbi authoring cli",
    "我需要處理 powerbi authoring cli 相關工作",
    "幫我啟用 powerbi authoring cli 這個 skill",
    "用 agent skill 完成 powerbi authoring cli",
    "use powerbi authoring cli from skills-for-fabric"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the powerbi authoring cli skill for this task.",
      "invocation": "codex --skill powerbi-authoring-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
