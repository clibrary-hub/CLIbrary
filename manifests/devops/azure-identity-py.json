{
  "name": "azure-identity-py",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for azure identity py workflows.",
  "intent_triggers": [
    "use the azure identity py agent skill",
    "run a workflow for azure identity py",
    "help me with azure identity py",
    "我需要處理 azure identity py 相關工作",
    "幫我啟用 azure identity py 這個 skill",
    "用 agent skill 完成 azure identity py",
    "use azure identity py from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the azure identity py skill for this task.",
      "invocation": "codex --skill azure-identity-py \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
