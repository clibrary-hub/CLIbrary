{
  "name": "langgraph-persistence",
  "version": "0.1.0",
  "category": "devops",
  "description": "INVOKE THIS SKILL when your LangGraph needs to persist state, remember conversations, travel through history, or configure subgraph checkpointer scoping.",
  "intent_triggers": [
    "use the langgraph persistence agent skill",
    "run a workflow for langgraph persistence",
    "help me with langgraph persistence",
    "我需要處理 langgraph persistence 相關工作",
    "幫我啟用 langgraph persistence 這個 skill",
    "用 agent skill 完成 langgraph persistence"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the langgraph persistence skill for this task.",
      "invocation": "codex --skill langgraph-persistence \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skill"
  ]
}
