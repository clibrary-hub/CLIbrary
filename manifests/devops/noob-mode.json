{
  "name": "noob-mode",
  "version": "0.1.0",
  "category": "devops",
  "description": "Plain-English translation layer for non-technical Copilot CLI users.",
  "intent_triggers": [
    "use the noob mode agent skill",
    "run a workflow for noob mode",
    "help me with noob mode",
    "我需要處理 noob mode 相關工作",
    "幫我啟用 noob mode 這個 skill",
    "用 agent skill 完成 noob mode",
    "use noob mode from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the noob mode skill for this task.",
      "invocation": "codex --skill noob-mode \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
