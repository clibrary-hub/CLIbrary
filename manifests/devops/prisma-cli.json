{
  "name": "prisma-cli",
  "version": "0.1.0",
  "category": "devops",
  "description": "Prisma CLI commands reference covering all available commands, options, and usage patterns.",
  "intent_triggers": [
    "use the prisma cli agent skill",
    "run a workflow for prisma cli",
    "help me with prisma cli",
    "我需要處理 prisma cli 相關工作",
    "幫我啟用 prisma cli 這個 skill",
    "用 agent skill 完成 prisma cli",
    "use prisma cli from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the prisma cli skill for this task.",
      "invocation": "codex --skill prisma-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
