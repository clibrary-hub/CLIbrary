{
  "name": "azure-deployment-preflight",
  "version": "0.1.0",
  "category": "devops",
  "description": "Performs comprehensive preflight validation of Bicep deployments to Azure, including template syntax validation, what-if analysis, and permission checks.",
  "intent_triggers": [
    "use the azure deployment preflight agent skill",
    "run a workflow for azure deployment preflight",
    "help me with azure deployment preflight",
    "我需要處理 azure deployment preflight 相關工作",
    "幫我啟用 azure deployment preflight 這個 skill",
    "用 agent skill 完成 azure deployment preflight",
    "use azure deployment preflight from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the azure deployment preflight skill for this task.",
      "invocation": "codex --skill azure-deployment-preflight \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
