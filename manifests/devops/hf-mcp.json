{
  "name": "hf-mcp",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use Hugging Face Hub via MCP server tools.",
  "intent_triggers": [
    "use the hf mcp agent skill",
    "run a workflow for hf mcp",
    "help me with hf mcp",
    "我需要處理 hf mcp 相關工作",
    "幫我啟用 hf mcp 這個 skill",
    "用 agent skill 完成 hf mcp",
    "use hf mcp from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the hf mcp skill for this task.",
      "invocation": "codex --skill hf-mcp \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
