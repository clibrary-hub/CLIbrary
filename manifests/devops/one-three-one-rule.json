{
  "name": "one-three-one-rule",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for one three one rule workflows.",
  "intent_triggers": [
    "use the one three one rule agent skill",
    "run a workflow for one three one rule",
    "help me with one three one rule",
    "我需要處理 one three one rule 相關工作",
    "幫我啟用 one three one rule 這個 skill",
    "用 agent skill 完成 one three one rule",
    "use one three one rule from hermes-agent-zh"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the one three one rule skill for this task.",
      "invocation": "codex --skill one-three-one-rule \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skills-rest"
  ]
}
