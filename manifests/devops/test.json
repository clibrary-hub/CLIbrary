{
  "name": "test",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use when you need to run tests for React core.",
  "intent_triggers": [
    "use the test agent skill",
    "run a workflow for test",
    "help me with test",
    "我需要處理 test 相關工作",
    "幫我啟用 test 這個 skill",
    "用 agent skill 完成 test",
    "use test from react"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the test skill for this task.",
      "invocation": "codex --skill test \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
