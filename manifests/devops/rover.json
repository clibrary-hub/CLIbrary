{
  "name": "rover",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for rover workflows.",
  "intent_triggers": [
    "use the rover agent skill",
    "run a workflow for rover",
    "help me with rover",
    "我需要處理 rover 相關工作",
    "幫我啟用 rover 這個 skill",
    "用 agent skill 完成 rover",
    "use rover from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the rover skill for this task.",
      "invocation": "codex --skill rover \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
