{
  "name": "configure",
  "version": "0.1.0",
  "category": "devops",
  "description": "Set up the Discord channel — save the bot token and review access policy.",
  "intent_triggers": [
    "use the configure agent skill",
    "run a workflow for configure",
    "help me with configure",
    "我需要處理 configure 相關工作",
    "幫我啟用 configure 這個 skill",
    "用 agent skill 完成 configure",
    "use configure from claude-plugins-official"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the configure skill for this task.",
      "invocation": "codex --skill configure \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
