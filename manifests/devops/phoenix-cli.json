{
  "name": "phoenix-cli",
  "version": "0.1.0",
  "category": "devops",
  "description": "Debug LLM applications using the Phoenix CLI.",
  "intent_triggers": [
    "use the phoenix cli agent skill",
    "run a workflow for phoenix cli",
    "help me with phoenix cli",
    "我需要處理 phoenix cli 相關工作",
    "幫我啟用 phoenix cli 這個 skill",
    "用 agent skill 完成 phoenix cli",
    "use phoenix cli from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the phoenix cli skill for this task.",
      "invocation": "codex --skill phoenix-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
