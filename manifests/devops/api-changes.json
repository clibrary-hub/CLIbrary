{
  "name": "api-changes",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use when customer-facing API changes were made — i.e., API report .md files differ from main.",
  "intent_triggers": [
    "use the api changes agent skill",
    "run a workflow for api changes",
    "help me with api changes",
    "我需要處理 api changes 相關工作",
    "幫我啟用 api changes 這個 skill",
    "用 agent skill 完成 api changes",
    "use api changes from fluidframework"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the api changes skill for this task.",
      "invocation": "codex --skill api-changes \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
