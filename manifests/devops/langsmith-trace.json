{
  "name": "langsmith-trace",
  "version": "0.1.0",
  "category": "devops",
  "description": "INVOKE THIS SKILL when working with LangSmith tracing OR querying traces.",
  "intent_triggers": [
    "use the langsmith trace agent skill",
    "run a workflow for langsmith trace",
    "help me with langsmith trace",
    "我需要處理 langsmith trace 相關工作",
    "幫我啟用 langsmith trace 這個 skill",
    "用 agent skill 完成 langsmith trace"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the langsmith trace skill for this task.",
      "invocation": "codex --skill langsmith-trace \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skill"
  ]
}
