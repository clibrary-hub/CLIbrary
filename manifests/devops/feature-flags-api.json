{
  "name": "feature-flags-api",
  "version": "0.1.0",
  "category": "devops",
  "description": "PostHog feature flags for API applications",
  "intent_triggers": [
    "use the feature flags api agent skill",
    "run a workflow for feature flags api",
    "help me with feature flags api",
    "我需要處理 feature flags api 相關工作",
    "幫我啟用 feature flags api 這個 skill",
    "用 agent skill 完成 feature flags api",
    "use feature flags api from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the feature flags api skill for this task.",
      "invocation": "codex --skill feature-flags-api \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
