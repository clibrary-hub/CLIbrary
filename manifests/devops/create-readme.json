{
  "name": "create-readme",
  "version": "0.1.0",
  "category": "devops",
  "description": "Create a README.md file for the project",
  "intent_triggers": [
    "use the create readme agent skill",
    "run a workflow for create readme",
    "help me with create readme",
    "我需要處理 create readme 相關工作",
    "幫我啟用 create readme 這個 skill",
    "用 agent skill 完成 create readme",
    "use create readme from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the create readme skill for this task.",
      "invocation": "codex --skill create-readme \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
