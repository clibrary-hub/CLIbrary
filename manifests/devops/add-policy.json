{
  "name": "add-policy",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use when adding, modifying, or reviewing VS Code configuration policies.",
  "intent_triggers": [
    "use the add policy agent skill",
    "run a workflow for add policy",
    "help me with add policy",
    "我需要處理 add policy 相關工作",
    "幫我啟用 add policy 這個 skill",
    "用 agent skill 完成 add policy",
    "use add policy from vscode"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the add policy skill for this task.",
      "invocation": "codex --skill add-policy \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
