{
  "name": "mcp-integration-claude-plugins-public",
  "version": "0.1.0",
  "category": "devops",
  "description": "This skill should be used when the user asks to \"add MCP server\", \"integrate MCP\", \"configure MCP in plugin\", \"use .mcp.json\", \"set up Model Context Protocol\", \"connect external service\", mentions...",
  "intent_triggers": [
    "use the mcp integration agent skill",
    "run a workflow for mcp integration",
    "help me with mcp integration",
    "我需要處理 mcp integration 相關工作",
    "幫我啟用 mcp integration 這個 skill",
    "用 agent skill 完成 mcp integration",
    "use mcp integration from claude-plugins-public"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mcp integration skill for this task.",
      "invocation": "codex --skill mcp-integration \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
