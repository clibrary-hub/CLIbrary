{
  "name": "persona-exec-assistant",
  "version": "0.1.0",
  "category": "devops",
  "description": "Manage an executive's schedule, inbox, and communications.",
  "intent_triggers": [
    "use the persona exec assistant agent skill",
    "run a workflow for persona exec assistant",
    "help me with persona exec assistant",
    "我需要處理 persona exec assistant 相關工作",
    "幫我啟用 persona exec assistant 這個 skill",
    "用 agent skill 完成 persona exec assistant",
    "use persona exec assistant from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the persona exec assistant skill for this task.",
      "invocation": "codex --skill persona-exec-assistant \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
