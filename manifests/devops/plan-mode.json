{
  "name": "plan-mode",
  "version": "0.1.0",
  "category": "devops",
  "description": "Holistic, system-aware planning before implementing non-trivial tasks.",
  "intent_triggers": [
    "use the plan mode agent skill",
    "run a workflow for plan mode",
    "help me with plan mode",
    "我需要處理 plan mode 相關工作",
    "幫我啟用 plan mode 這個 skill",
    "用 agent skill 完成 plan mode",
    "use plan mode from open-agents"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the plan mode skill for this task.",
      "invocation": "codex --skill plan-mode \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
