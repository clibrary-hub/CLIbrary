{
  "name": "better-env",
  "version": "0.1.0",
  "category": "devops",
  "description": "Better environment variable management for agents and humans with full type safety, CLI-based remote environment synchronization, and environment validation.",
  "intent_triggers": [
    "use the better env agent skill",
    "run a workflow for better env",
    "help me with better env",
    "我需要處理 better env 相關工作",
    "幫我啟用 better env 這個 skill",
    "用 agent skill 完成 better env",
    "use better env from better-env"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the better env skill for this task.",
      "invocation": "codex --skill better-env \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
