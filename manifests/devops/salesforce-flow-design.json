{
  "name": "salesforce-flow-design",
  "version": "0.1.0",
  "category": "devops",
  "description": "Salesforce Flow architecture decisions, flow type selection, bulk safety validation, and fault handling standards.",
  "intent_triggers": [
    "use the salesforce flow design agent skill",
    "run a workflow for salesforce flow design",
    "help me with salesforce flow design",
    "我需要處理 salesforce flow design 相關工作",
    "幫我啟用 salesforce flow design 這個 skill",
    "用 agent skill 完成 salesforce flow design",
    "use salesforce flow design from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the salesforce flow design skill for this task.",
      "invocation": "codex --skill salesforce-flow-design \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
