{
  "name": "neon-serverless",
  "version": "0.1.0",
  "category": "devops",
  "description": "Configures Neon Serverless Driver for Next.js, Vercel Edge Functions, AWS Lambda, and other serverless environments.",
  "intent_triggers": [
    "use the neon serverless agent skill",
    "run a workflow for neon serverless",
    "help me with neon serverless",
    "我需要處理 neon serverless 相關工作",
    "幫我啟用 neon serverless 這個 skill",
    "用 agent skill 完成 neon serverless",
    "use neon serverless from ai-rules"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the neon serverless skill for this task.",
      "invocation": "codex --skill neon-serverless \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
