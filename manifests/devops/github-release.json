{
  "name": "github-release",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for github release workflows.",
  "intent_triggers": [
    "use the github release agent skill",
    "run a workflow for github release",
    "help me with github release",
    "我需要處理 github release 相關工作",
    "幫我啟用 github release 這個 skill",
    "用 agent skill 完成 github release",
    "use github release from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the github release skill for this task.",
      "invocation": "codex --skill github-release \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
