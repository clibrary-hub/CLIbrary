{
  "name": "pytorch-fsdp2",
  "version": "0.1.0",
  "category": "devops",
  "description": "Adds PyTorch FSDP2 (fully_shard) to training scripts with correct init, sharding, mixed precision/offload config, and distributed checkpointing.",
  "intent_triggers": [
    "use the pytorch fsdp2 agent skill",
    "run a workflow for pytorch fsdp2",
    "help me with pytorch fsdp2",
    "我需要處理 pytorch fsdp2 相關工作",
    "幫我啟用 pytorch fsdp2 這個 skill",
    "用 agent skill 完成 pytorch fsdp2",
    "use pytorch fsdp2 from ai-research-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the pytorch fsdp2 skill for this task.",
      "invocation": "codex --skill pytorch-fsdp2 \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
