{
  "name": "console-rendering",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use Go struct tags to render styled terminal output in gh-aw.",
  "intent_triggers": [
    "use the console rendering agent skill",
    "run a workflow for console rendering",
    "help me with console rendering",
    "我需要處理 console rendering 相關工作",
    "幫我啟用 console rendering 這個 skill",
    "用 agent skill 完成 console rendering",
    "use console rendering from gh-aw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the console rendering skill for this task.",
      "invocation": "codex --skill console-rendering \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
