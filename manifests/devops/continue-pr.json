{
  "name": "continue-pr",
  "version": "0.1.0",
  "category": "devops",
  "description": "Continue work on an existing PR - resolve conflicts, fix CI failures, address reviewer feedback, and push updates.",
  "intent_triggers": [
    "use the continue pr agent skill",
    "run a workflow for continue pr",
    "help me with continue pr",
    "我需要處理 continue pr 相關工作",
    "幫我啟用 continue pr 這個 skill",
    "用 agent skill 完成 continue pr",
    "use continue pr from clickhouse"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the continue pr skill for this task.",
      "invocation": "codex --skill continue-pr \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
