{
  "name": "running-dbt-commands-dbt-agent-skills",
  "version": "0.1.0",
  "category": "devops",
  "description": "Formats and executes dbt CLI commands, selects the correct dbt executable, and structures command parameters.",
  "intent_triggers": [
    "use the running dbt commands agent skill",
    "run a workflow for running dbt commands",
    "help me with running dbt commands",
    "我需要處理 running dbt commands 相關工作",
    "幫我啟用 running dbt commands 這個 skill",
    "用 agent skill 完成 running dbt commands",
    "use running dbt commands from dbt-agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the running dbt commands skill for this task.",
      "invocation": "codex --skill running-dbt-commands \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
