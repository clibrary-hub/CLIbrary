{
  "name": "langsmith-evaluator",
  "version": "0.1.0",
  "category": "devops",
  "description": "INVOKE THIS SKILL when building evaluation pipelines for LangSmith.",
  "intent_triggers": [
    "use the langsmith evaluator agent skill",
    "run a workflow for langsmith evaluator",
    "help me with langsmith evaluator",
    "我需要處理 langsmith evaluator 相關工作",
    "幫我啟用 langsmith evaluator 這個 skill",
    "用 agent skill 完成 langsmith evaluator"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the langsmith evaluator skill for this task.",
      "invocation": "codex --skill langsmith-evaluator \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skill"
  ]
}
