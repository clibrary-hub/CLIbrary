{
  "name": "airflow-hitl",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use when the user needs human-in-the-loop workflows in Airflow (approval/reject, form input, or human-driven branching).",
  "intent_triggers": [
    "use the airflow hitl agent skill",
    "run a workflow for airflow hitl",
    "help me with airflow hitl",
    "我需要處理 airflow hitl 相關工作",
    "幫我啟用 airflow hitl 這個 skill",
    "用 agent skill 完成 airflow hitl",
    "use airflow hitl from agents"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the airflow hitl skill for this task.",
      "invocation": "codex --skill airflow-hitl \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
