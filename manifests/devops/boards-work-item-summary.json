{
  "name": "boards-work-item-summary",
  "version": "0.1.0",
  "category": "devops",
  "description": "Summarize a single Azure DevOps work item (and its links and comments) by ID.",
  "intent_triggers": [
    "use the boards work item summary agent skill",
    "run a workflow for boards work item summary",
    "help me with boards work item summary",
    "我需要處理 boards work item summary 相關工作",
    "幫我啟用 boards work item summary 這個 skill",
    "用 agent skill 完成 boards work item summary",
    "use boards work item summary from azure-devops-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the boards work item summary skill for this task.",
      "invocation": "codex --skill boards-work-item-summary \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
