{
  "name": "agent-install-agent-skill-agent-skill",
  "version": "0.1.0",
  "category": "devops",
  "description": "Install the Datadog Agent on Linux hosts via SSH with Single Step Instrumentation (SSI) enabled — SSI automatically instruments applications for APM without code changes.",
  "intent_triggers": [
    "use the agent install agent skill",
    "run a workflow for agent install",
    "help me with agent install",
    "我需要處理 agent install 相關工作",
    "幫我啟用 agent install 這個 skill",
    "用 agent skill 完成 agent install"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the agent install skill for this task.",
      "invocation": "codex --skill agent-install \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skill"
  ]
}
