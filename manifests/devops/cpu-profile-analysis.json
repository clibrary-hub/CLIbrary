{
  "name": "cpu-profile-analysis",
  "version": "0.1.0",
  "category": "devops",
  "description": "Analyze V8/Chrome CPU profiles (.cpuprofile) and DevTools trace files (Trace-*.json).",
  "intent_triggers": [
    "use the cpu profile analysis agent skill",
    "run a workflow for cpu profile analysis",
    "help me with cpu profile analysis",
    "我需要處理 cpu profile analysis 相關工作",
    "幫我啟用 cpu profile analysis 這個 skill",
    "用 agent skill 完成 cpu profile analysis",
    "use cpu profile analysis from vscode"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the cpu profile analysis skill for this task.",
      "invocation": "codex --skill cpu-profile-analysis \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
