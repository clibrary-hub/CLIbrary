{
  "name": "run-tests",
  "version": "0.1.0",
  "category": "devops",
  "description": "Guide for running pytest tests locally.",
  "intent_triggers": [
    "use the run tests agent skill",
    "run a workflow for run tests",
    "help me with run tests",
    "我需要處理 run tests 相關工作",
    "幫我啟用 run tests 這個 skill",
    "用 agent skill 完成 run tests",
    "use run tests from semantic-link-labs"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the run tests skill for this task.",
      "invocation": "codex --skill run-tests \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
