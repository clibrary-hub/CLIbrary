{
  "name": "recipe-review-overdue-tasks",
  "version": "0.1.0",
  "category": "devops",
  "description": "Find Google Tasks that are past due and need attention.",
  "intent_triggers": [
    "use the recipe review overdue tasks agent skill",
    "run a workflow for recipe review overdue tasks",
    "help me with recipe review overdue tasks",
    "我需要處理 recipe review overdue tasks 相關工作",
    "幫我啟用 recipe review overdue tasks 這個 skill",
    "用 agent skill 完成 recipe review overdue tasks",
    "use recipe review overdue tasks from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recipe review overdue tasks skill for this task.",
      "invocation": "codex --skill recipe-review-overdue-tasks \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
