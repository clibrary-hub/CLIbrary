{
  "name": "feature-flags-ios",
  "version": "0.1.0",
  "category": "devops",
  "description": "PostHog feature flags for iOS applications",
  "intent_triggers": [
    "use the feature flags ios agent skill",
    "run a workflow for feature flags ios",
    "help me with feature flags ios",
    "我需要處理 feature flags ios 相關工作",
    "幫我啟用 feature flags ios 這個 skill",
    "用 agent skill 完成 feature flags ios",
    "use feature flags ios from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the feature flags ios skill for this task.",
      "invocation": "codex --skill feature-flags-ios \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
