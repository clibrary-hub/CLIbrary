{
  "name": "mcp-apps-builder",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for mcp apps builder workflows.",
  "intent_triggers": [
    "use the mcp apps builder agent skill",
    "run a workflow for mcp apps builder",
    "help me with mcp apps builder",
    "我需要處理 mcp apps builder 相關工作",
    "幫我啟用 mcp apps builder 這個 skill",
    "用 agent skill 完成 mcp apps builder",
    "use mcp apps builder from mcp-use"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mcp apps builder skill for this task.",
      "invocation": "codex --skill mcp-apps-builder \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
