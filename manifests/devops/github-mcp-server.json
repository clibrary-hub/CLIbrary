{
  "name": "github-mcp-server",
  "version": "0.1.0",
  "category": "devops",
  "description": "Reference for GitHub MCP server tools, methods, and usage patterns.",
  "intent_triggers": [
    "use the github mcp server agent skill",
    "run a workflow for github mcp server",
    "help me with github mcp server",
    "我需要處理 github mcp server 相關工作",
    "幫我啟用 github mcp server 這個 skill",
    "用 agent skill 完成 github mcp server",
    "use github mcp server from gh-aw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the github mcp server skill for this task.",
      "invocation": "codex --skill github-mcp-server \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
