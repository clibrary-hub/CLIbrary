{
  "name": "enable-ssi-agent-skills",
  "version": "0.1.0",
  "category": "devops",
  "description": "Enable Single Step Instrumentation (SSI) on Kubernetes — automatically instruments applications for APM without code changes.",
  "intent_triggers": [
    "use the enable ssi agent skill",
    "run a workflow for enable ssi",
    "help me with enable ssi",
    "我需要處理 enable ssi 相關工作",
    "幫我啟用 enable ssi 這個 skill",
    "用 agent skill 完成 enable ssi",
    "use enable ssi from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the enable ssi skill for this task.",
      "invocation": "codex --skill enable-ssi \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
