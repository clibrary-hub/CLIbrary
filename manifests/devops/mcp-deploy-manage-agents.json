{
  "name": "mcp-deploy-manage-agents",
  "version": "0.1.0",
  "category": "devops",
  "description": "Skill converted from mcp-deploy-manage-agents.prompt.md",
  "intent_triggers": [
    "use the mcp deploy manage agents agent skill",
    "run a workflow for mcp deploy manage agents",
    "help me with mcp deploy manage agents",
    "我需要處理 mcp deploy manage agents 相關工作",
    "幫我啟用 mcp deploy manage agents 這個 skill",
    "用 agent skill 完成 mcp deploy manage agents",
    "use mcp deploy manage agents from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mcp deploy manage agents skill for this task.",
      "invocation": "codex --skill mcp-deploy-manage-agents \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
