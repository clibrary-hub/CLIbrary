{
  "name": "deploy-open-harness",
  "version": "0.1.0",
  "category": "devops",
  "description": "Guides a user through collecting the credentials needed to deploy their own copy of Open Harness, deploying this repo on Vercel, and completing first-run setup.",
  "intent_triggers": [
    "use the deploy open harness agent skill",
    "run a workflow for deploy open harness",
    "help me with deploy open harness",
    "我需要處理 deploy open harness 相關工作",
    "幫我啟用 deploy open harness 這個 skill",
    "用 agent skill 完成 deploy open harness",
    "use deploy open harness from open-agents"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the deploy open harness skill for this task.",
      "invocation": "codex --skill deploy-open-harness \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
