{
  "name": "pulumi-upgrade-provider",
  "version": "0.1.0",
  "category": "devops",
  "description": "Automate Pulumi provider repo upgrades with the `upgrade-provider` tool.",
  "intent_triggers": [
    "use the pulumi upgrade provider agent skill",
    "run a workflow for pulumi upgrade provider",
    "help me with pulumi upgrade provider",
    "我需要處理 pulumi upgrade provider 相關工作",
    "幫我啟用 pulumi upgrade provider 這個 skill",
    "用 agent skill 完成 pulumi upgrade provider",
    "use pulumi upgrade provider from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the pulumi upgrade provider skill for this task.",
      "invocation": "codex --skill pulumi-upgrade-provider \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
