{
  "name": "cli-mastery",
  "version": "0.1.0",
  "category": "devops",
  "description": "Interactive training for the GitHub Copilot CLI.",
  "intent_triggers": [
    "use the cli mastery agent skill",
    "run a workflow for cli mastery",
    "help me with cli mastery",
    "我需要處理 cli mastery 相關工作",
    "幫我啟用 cli mastery 這個 skill",
    "用 agent skill 完成 cli mastery",
    "use cli mastery from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the cli mastery skill for this task.",
      "invocation": "codex --skill cli-mastery \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
