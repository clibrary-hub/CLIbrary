{
  "name": "vercel-cli",
  "version": "0.1.0",
  "category": "devops",
  "description": "Deploy, manage, inspect, and troubleshoot Vercel projects from the command line.",
  "intent_triggers": [
    "use the vercel cli agent skill",
    "run a workflow for vercel cli",
    "help me with vercel cli",
    "我需要處理 vercel cli 相關工作",
    "幫我啟用 vercel cli 這個 skill",
    "用 agent skill 完成 vercel cli",
    "use vercel cli from vercel"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the vercel cli skill for this task.",
      "invocation": "codex --skill vercel-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
