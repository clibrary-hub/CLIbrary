{
  "name": "geofeed-tuner",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for geofeed tuner workflows.",
  "intent_triggers": [
    "use the geofeed tuner agent skill",
    "run a workflow for geofeed tuner",
    "help me with geofeed tuner",
    "我需要處理 geofeed tuner 相關工作",
    "幫我啟用 geofeed tuner 這個 skill",
    "用 agent skill 完成 geofeed tuner",
    "use geofeed tuner from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the geofeed tuner skill for this task.",
      "invocation": "codex --skill geofeed-tuner \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
