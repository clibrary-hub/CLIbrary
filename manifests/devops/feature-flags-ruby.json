{
  "name": "feature-flags-ruby",
  "version": "0.1.0",
  "category": "devops",
  "description": "PostHog feature flags for Ruby applications",
  "intent_triggers": [
    "use the feature flags ruby agent skill",
    "run a workflow for feature flags ruby",
    "help me with feature flags ruby",
    "我需要處理 feature flags ruby 相關工作",
    "幫我啟用 feature flags ruby 這個 skill",
    "用 agent skill 完成 feature flags ruby",
    "use feature flags ruby from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the feature flags ruby skill for this task.",
      "invocation": "codex --skill feature-flags-ruby \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
