{
  "name": "upstream-patches",
  "version": "0.1.0",
  "category": "devops",
  "description": "Create, amend, remove, and rebase patches for Terraform provider submodules using `./scripts/upstream.sh`.",
  "intent_triggers": [
    "use the upstream patches agent skill",
    "run a workflow for upstream patches",
    "help me with upstream patches",
    "我需要處理 upstream patches 相關工作",
    "幫我啟用 upstream patches 這個 skill",
    "用 agent skill 完成 upstream patches",
    "use upstream patches from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the upstream patches skill for this task.",
      "invocation": "codex --skill upstream-patches \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
