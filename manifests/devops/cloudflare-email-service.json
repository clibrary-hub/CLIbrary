{
  "name": "cloudflare-email-service",
  "version": "0.1.0",
  "category": "devops",
  "description": "Send and receive transactional emails with Cloudflare Email Service (Email Sending + Email Routing).",
  "intent_triggers": [
    "use the cloudflare email service agent skill",
    "run a workflow for cloudflare email service",
    "help me with cloudflare email service",
    "我需要處理 cloudflare email service 相關工作",
    "幫我啟用 cloudflare email service 這個 skill",
    "用 agent skill 完成 cloudflare email service",
    "use cloudflare email service from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the cloudflare email service skill for this task.",
      "invocation": "codex --skill cloudflare-email-service \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
