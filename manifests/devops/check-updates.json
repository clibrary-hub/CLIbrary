{
  "name": "check-updates",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for check updates workflows.",
  "intent_triggers": [
    "use the check updates agent skill",
    "run a workflow for check updates",
    "help me with check updates",
    "我需要處理 check updates 相關工作",
    "幫我啟用 check updates 這個 skill",
    "用 agent skill 完成 check updates",
    "use check updates from skills-for-fabric"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the check updates skill for this task.",
      "invocation": "codex --skill check-updates \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
