{
  "name": "huggingface-tool-builder",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use this skill when the user wants to build tool/scripts or achieve a task where using data from the Hugging Face API would help.",
  "intent_triggers": [
    "use the huggingface tool builder agent skill",
    "run a workflow for huggingface tool builder",
    "help me with huggingface tool builder",
    "我需要處理 huggingface tool builder 相關工作",
    "幫我啟用 huggingface tool builder 這個 skill",
    "用 agent skill 完成 huggingface tool builder",
    "use huggingface tool builder from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the huggingface tool builder skill for this task.",
      "invocation": "codex --skill huggingface-tool-builder \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
