{
  "name": "cloudflare",
  "version": "0.1.0",
  "category": "devops",
  "description": "Comprehensive Cloudflare platform skill covering Workers, Pages, storage (KV, D1, R2), AI (Workers AI, Vectorize, Agents SDK), feature flags (Flagship), networking (Tunnel, Spectrum), security (WAF...",
  "intent_triggers": [
    "use the cloudflare agent skill",
    "run a workflow for cloudflare",
    "help me with cloudflare",
    "我需要處理 cloudflare 相關工作",
    "幫我啟用 cloudflare 這個 skill",
    "用 agent skill 完成 cloudflare",
    "use cloudflare from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the cloudflare skill for this task.",
      "invocation": "codex --skill cloudflare \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
