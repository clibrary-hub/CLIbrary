{
  "name": "sentry-sdk-upgrade",
  "version": "0.1.0",
  "category": "devops",
  "description": "Upgrade the Sentry JavaScript SDK across major versions.",
  "intent_triggers": [
    "use the sentry sdk upgrade agent skill",
    "run a workflow for sentry sdk upgrade",
    "help me with sentry sdk upgrade",
    "我需要處理 sentry sdk upgrade 相關工作",
    "幫我啟用 sentry sdk upgrade 這個 skill",
    "用 agent skill 完成 sentry sdk upgrade",
    "use sentry sdk upgrade from sentry-for-ai"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sentry sdk upgrade skill for this task.",
      "invocation": "codex --skill sentry-sdk-upgrade \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
