{
  "name": "github-pr-workflow",
  "version": "0.1.0",
  "category": "devops",
  "description": "Full pull request lifecycle — create branches, commit changes, open PRs, monitor CI status, auto-fix failures, and merge.",
  "intent_triggers": [
    "use the github pr workflow agent skill",
    "run a workflow for github pr workflow",
    "help me with github pr workflow",
    "我需要處理 github pr workflow 相關工作",
    "幫我啟用 github pr workflow 這個 skill",
    "用 agent skill 完成 github pr workflow"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the github pr workflow skill for this task.",
      "invocation": "codex --skill github-pr-workflow \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skill"
  ]
}
