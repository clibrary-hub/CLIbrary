{
  "name": "bx",
  "version": "0.1.0",
  "category": "devops",
  "description": "USE FOR web search, research, RAG, grounding, browse, find, lookups, fact-checking, documentation, agentic AI.",
  "intent_triggers": [
    "use the bx agent skill",
    "run a workflow for bx",
    "help me with bx",
    "我需要處理 bx 相關工作",
    "幫我啟用 bx 這個 skill",
    "用 agent skill 完成 bx",
    "use bx from brave-search-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the bx skill for this task.",
      "invocation": "codex --skill bx \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
