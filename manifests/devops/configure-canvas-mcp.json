{
  "name": "configure-canvas-mcp",
  "version": "0.1.0",
  "category": "devops",
  "description": "Configure the Canvas Authoring MCP server for the current coauthoring session.",
  "intent_triggers": [
    "use the configure canvas mcp agent skill",
    "run a workflow for configure canvas mcp",
    "help me with configure canvas mcp",
    "我需要處理 configure canvas mcp 相關工作",
    "幫我啟用 configure canvas mcp 這個 skill",
    "用 agent skill 完成 configure canvas mcp",
    "use configure canvas mcp from power-platform-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the configure canvas mcp skill for this task.",
      "invocation": "codex --skill configure-canvas-mcp \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
