{
  "name": "github-auth",
  "version": "0.1.0",
  "category": "devops",
  "description": "Set up GitHub authentication for the agent using git (universally available) or the gh CLI.",
  "intent_triggers": [
    "use the github auth agent skill",
    "run a workflow for github auth",
    "help me with github auth",
    "我需要處理 github auth 相關工作",
    "幫我啟用 github auth 這個 skill",
    "用 agent skill 完成 github auth"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the github auth skill for this task.",
      "invocation": "codex --skill github-auth \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skill"
  ]
}
