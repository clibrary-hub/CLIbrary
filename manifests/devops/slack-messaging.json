{
  "name": "slack-messaging",
  "version": "0.1.0",
  "category": "devops",
  "description": "Guidance for composing well-formatted, effective Slack messages using mrkdwn syntax",
  "intent_triggers": [
    "use the slack messaging agent skill",
    "run a workflow for slack messaging",
    "help me with slack messaging",
    "我需要處理 slack messaging 相關工作",
    "幫我啟用 slack messaging 這個 skill",
    "用 agent skill 完成 slack messaging",
    "use slack messaging from knowledge-work-plugins"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the slack messaging skill for this task.",
      "invocation": "codex --skill slack-messaging \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
