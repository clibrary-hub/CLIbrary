{
  "name": "apollo-ios",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for apollo ios workflows.",
  "intent_triggers": [
    "use the apollo ios agent skill",
    "run a workflow for apollo ios",
    "help me with apollo ios",
    "我需要處理 apollo ios 相關工作",
    "幫我啟用 apollo ios 這個 skill",
    "用 agent skill 完成 apollo ios",
    "use apollo ios from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the apollo ios skill for this task.",
      "invocation": "codex --skill apollo-ios \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
