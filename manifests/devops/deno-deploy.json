{
  "name": "deno-deploy",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use when deploying Deno apps to production, asking about Deno Deploy, or working with `deno deploy` CLI commands.",
  "intent_triggers": [
    "use the deno deploy agent skill",
    "run a workflow for deno deploy",
    "help me with deno deploy",
    "我需要處理 deno deploy 相關工作",
    "幫我啟用 deno deploy 這個 skill",
    "用 agent skill 完成 deno deploy",
    "use deno deploy from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the deno deploy skill for this task.",
      "invocation": "codex --skill deno-deploy \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
