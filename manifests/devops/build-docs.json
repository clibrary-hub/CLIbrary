{
  "name": "build-docs",
  "version": "0.1.0",
  "category": "devops",
  "description": "Guide for building documentation and validating docstrings.",
  "intent_triggers": [
    "use the build docs agent skill",
    "run a workflow for build docs",
    "help me with build docs",
    "我需要處理 build docs 相關工作",
    "幫我啟用 build docs 這個 skill",
    "用 agent skill 完成 build docs",
    "use build docs from semantic-link-labs"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the build docs skill for this task.",
      "invocation": "codex --skill build-docs \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
