{
  "name": "bump-size-limit",
  "version": "0.1.0",
  "category": "devops",
  "description": "Bump size limits in .size-limit.js when the size-limit CI check is failing.",
  "intent_triggers": [
    "use the bump size limit agent skill",
    "run a workflow for bump size limit",
    "help me with bump size limit",
    "我需要處理 bump size limit 相關工作",
    "幫我啟用 bump size limit 這個 skill",
    "用 agent skill 完成 bump size limit",
    "use bump size limit from sentry-javascript"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the bump size limit skill for this task.",
      "invocation": "codex --skill bump-size-limit \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
