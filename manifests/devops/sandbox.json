{
  "name": "sandbox",
  "version": "0.1.0",
  "category": "devops",
  "description": "Creates isolated Linux MicroVMs using Vercel Sandbox SDK.",
  "intent_triggers": [
    "use the sandbox agent skill",
    "run a workflow for sandbox",
    "help me with sandbox",
    "我需要處理 sandbox 相關工作",
    "幫我啟用 sandbox 這個 skill",
    "用 agent skill 完成 sandbox",
    "use sandbox from sandbox"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sandbox skill for this task.",
      "invocation": "codex --skill sandbox \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
