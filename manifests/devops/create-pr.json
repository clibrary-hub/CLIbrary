{
  "name": "create-pr",
  "version": "0.1.0",
  "category": "devops",
  "description": "Create a pull request using the repository PR template.",
  "intent_triggers": [
    "use the create pr agent skill",
    "run a workflow for create pr",
    "help me with create pr",
    "我需要處理 create pr 相關工作",
    "幫我啟用 create pr 這個 skill",
    "用 agent skill 完成 create pr",
    "use create pr from aspire"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the create pr skill for this task.",
      "invocation": "codex --skill create-pr \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
