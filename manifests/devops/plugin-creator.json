{
  "name": "plugin-creator",
  "version": "0.1.0",
  "category": "devops",
  "description": "Create and scaffold plugin directories for Codex with a required `.codex-plugin/plugin.json`, optional plugin folders/files, valid manifest defaults, and personal-marketplace entries by default.",
  "intent_triggers": [
    "use the plugin creator agent skill",
    "run a workflow for plugin creator",
    "help me with plugin creator",
    "我需要處理 plugin creator 相關工作",
    "幫我啟用 plugin creator 這個 skill",
    "用 agent skill 完成 plugin creator",
    "use plugin creator from codex"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the plugin creator skill for this task.",
      "invocation": "codex --skill plugin-creator \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
