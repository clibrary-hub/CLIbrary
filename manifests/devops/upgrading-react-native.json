{
  "name": "upgrading-react-native",
  "version": "0.1.0",
  "category": "devops",
  "description": "Upgrades React Native apps to newer versions by applying rn-diff-purge template diffs, updating package.json dependencies, migrating native iOS and Android configuration, resolving CocoaPods and Gr...",
  "intent_triggers": [
    "use the upgrading react native agent skill",
    "run a workflow for upgrading react native",
    "help me with upgrading react native",
    "我需要處理 upgrading react native 相關工作",
    "幫我啟用 upgrading react native 這個 skill",
    "用 agent skill 完成 upgrading react native",
    "use upgrading react native from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the upgrading react native skill for this task.",
      "invocation": "codex --skill upgrading-react-native \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
