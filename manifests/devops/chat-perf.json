{
  "name": "chat-perf",
  "version": "0.1.0",
  "category": "devops",
  "description": "Run chat perf benchmarks and memory leak checks against the local dev build or any published VS Code version.",
  "intent_triggers": [
    "use the chat perf agent skill",
    "run a workflow for chat perf",
    "help me with chat perf",
    "我需要處理 chat perf 相關工作",
    "幫我啟用 chat perf 這個 skill",
    "用 agent skill 完成 chat perf",
    "use chat perf from vscode"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the chat perf skill for this task.",
      "invocation": "codex --skill chat-perf \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
