{
  "name": "changeset-validation",
  "version": "0.1.0",
  "category": "devops",
  "description": "Validate changesets in openai-agents-js using LLM judgment against git diffs (including uncommitted local changes).",
  "intent_triggers": [
    "use the changeset validation agent skill",
    "run a workflow for changeset validation",
    "help me with changeset validation",
    "我需要處理 changeset validation 相關工作",
    "幫我啟用 changeset validation 這個 skill",
    "用 agent skill 完成 changeset validation",
    "use changeset validation from openai-agents-js"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the changeset validation skill for this task.",
      "invocation": "codex --skill changeset-validation \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
