{
  "name": "add-neon-docs",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use this skill when the user asks to add documentation, add docs, add references, or install documentation about Neon.",
  "intent_triggers": [
    "use the add neon docs agent skill",
    "run a workflow for add neon docs",
    "help me with add neon docs",
    "我需要處理 add neon docs 相關工作",
    "幫我啟用 add neon docs 這個 skill",
    "用 agent skill 完成 add neon docs",
    "use add neon docs from ai-rules"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the add neon docs skill for this task.",
      "invocation": "codex --skill add-neon-docs \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
