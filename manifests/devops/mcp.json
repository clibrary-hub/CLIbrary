{
  "name": "mcp",
  "version": "0.1.0",
  "category": "devops",
  "description": "MCP Apps integration for json-render.",
  "intent_triggers": [
    "use the mcp agent skill",
    "run a workflow for mcp",
    "help me with mcp",
    "我需要處理 mcp 相關工作",
    "幫我啟用 mcp 這個 skill",
    "用 agent skill 完成 mcp",
    "use mcp from json-render"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mcp skill for this task.",
      "invocation": "codex --skill mcp \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
