{
  "name": "implementation-strategy",
  "version": "0.1.0",
  "category": "devops",
  "description": "Decide how to implement runtime and API changes in openai-agents-js before editing code.",
  "intent_triggers": [
    "use the implementation strategy agent skill",
    "run a workflow for implementation strategy",
    "help me with implementation strategy",
    "我需要處理 implementation strategy 相關工作",
    "幫我啟用 implementation strategy 這個 skill",
    "用 agent skill 完成 implementation strategy",
    "use implementation strategy from openai-agents-js"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the implementation strategy skill for this task.",
      "invocation": "codex --skill implementation-strategy \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
