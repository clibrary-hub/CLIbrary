{
  "name": "sherlock",
  "version": "0.1.0",
  "category": "devops",
  "description": "OSINT username search across 400+ social networks.",
  "intent_triggers": [
    "use the sherlock agent skill",
    "run a workflow for sherlock",
    "help me with sherlock",
    "我需要處理 sherlock 相關工作",
    "幫我啟用 sherlock 這個 skill",
    "用 agent skill 完成 sherlock",
    "use sherlock from hermes-agent-zh"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sherlock skill for this task.",
      "invocation": "codex --skill sherlock \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skills-rest"
  ]
}
