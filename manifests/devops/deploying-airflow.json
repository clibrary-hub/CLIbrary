{
  "name": "deploying-airflow",
  "version": "0.1.0",
  "category": "devops",
  "description": "Deploy Airflow DAGs and projects.",
  "intent_triggers": [
    "use the deploying airflow agent skill",
    "run a workflow for deploying airflow",
    "help me with deploying airflow",
    "我需要處理 deploying airflow 相關工作",
    "幫我啟用 deploying airflow 這個 skill",
    "用 agent skill 完成 deploying airflow",
    "use deploying airflow from agents"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the deploying airflow skill for this task.",
      "invocation": "codex --skill deploying-airflow \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
