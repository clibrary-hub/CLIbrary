{
  "name": "upstash-box-js",
  "version": "0.1.0",
  "category": "devops",
  "description": "Work with the @upstash/box TypeScript/JavaScript SDK for sandboxed cloud containers with AI agents, shell, filesystem, and git.",
  "intent_triggers": [
    "use the upstash box js agent skill",
    "run a workflow for upstash box js",
    "help me with upstash box js",
    "我需要處理 upstash box js 相關工作",
    "幫我啟用 upstash box js 這個 skill",
    "用 agent skill 完成 upstash box js",
    "use upstash box js from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the upstash box js skill for this task.",
      "invocation": "codex --skill upstash-box-js \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
