{
  "name": "vercel-sandbox",
  "version": "0.1.0",
  "category": "devops",
  "description": "Investigate Vercel Sandbox lifecycle, timeout, snapshot, and persistence behavior.",
  "intent_triggers": [
    "use the vercel sandbox agent skill",
    "run a workflow for vercel sandbox",
    "help me with vercel sandbox",
    "我需要處理 vercel sandbox 相關工作",
    "幫我啟用 vercel sandbox 這個 skill",
    "用 agent skill 完成 vercel sandbox",
    "use vercel sandbox from junior"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the vercel sandbox skill for this task.",
      "invocation": "codex --skill vercel-sandbox \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
