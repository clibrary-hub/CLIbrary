{
  "name": "commit-categories",
  "version": "0.1.0",
  "category": "devops",
  "description": "Commit categorization rules for changelogs and \"what's new\" summaries.",
  "intent_triggers": [
    "use the commit categories agent skill",
    "run a workflow for commit categories",
    "help me with commit categories",
    "我需要處理 commit categories 相關工作",
    "幫我啟用 commit categories 這個 skill",
    "用 agent skill 完成 commit categories",
    "use commit categories from workerd"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the commit categories skill for this task.",
      "invocation": "codex --skill commit-categories \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
