{
  "name": "playwright-generate-test",
  "version": "0.1.0",
  "category": "devops",
  "description": "Generate a Playwright test based on a scenario using Playwright MCP",
  "intent_triggers": [
    "use the playwright generate test agent skill",
    "run a workflow for playwright generate test",
    "help me with playwright generate test",
    "我需要處理 playwright generate test 相關工作",
    "幫我啟用 playwright generate test 這個 skill",
    "用 agent skill 完成 playwright generate test",
    "use playwright generate test from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the playwright generate test skill for this task.",
      "invocation": "codex --skill playwright-generate-test \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
