{
  "name": "flags-sdk",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for flags sdk workflows.",
  "intent_triggers": [
    "use the flags sdk agent skill",
    "run a workflow for flags sdk",
    "help me with flags sdk",
    "我需要處理 flags sdk 相關工作",
    "幫我啟用 flags sdk 這個 skill",
    "用 agent skill 完成 flags sdk",
    "use flags sdk from flags"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the flags sdk skill for this task.",
      "invocation": "codex --skill flags-sdk \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
