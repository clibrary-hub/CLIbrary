{
  "name": "create-branch-skills",
  "version": "0.1.0",
  "category": "devops",
  "description": "Create a git branch following Sentry naming conventions.",
  "intent_triggers": [
    "use the create branch agent skill",
    "run a workflow for create branch",
    "help me with create branch",
    "我需要處理 create branch 相關工作",
    "幫我啟用 create branch 這個 skill",
    "用 agent skill 完成 create branch",
    "use create branch from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the create branch skill for this task.",
      "invocation": "codex --skill create-branch \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
