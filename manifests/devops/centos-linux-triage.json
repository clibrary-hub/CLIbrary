{
  "name": "centos-linux-triage",
  "version": "0.1.0",
  "category": "devops",
  "description": "Triage and resolve CentOS issues using RHEL-compatible tooling, SELinux-aware practices, and firewalld.",
  "intent_triggers": [
    "use the centos linux triage agent skill",
    "run a workflow for centos linux triage",
    "help me with centos linux triage",
    "我需要處理 centos linux triage 相關工作",
    "幫我啟用 centos linux triage 這個 skill",
    "用 agent skill 完成 centos linux triage",
    "use centos linux triage from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the centos linux triage skill for this task.",
      "invocation": "codex --skill centos-linux-triage \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
