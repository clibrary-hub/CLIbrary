{
  "name": "restore-function-signatures",
  "version": "0.1.0",
  "category": "devops",
  "description": "Restore degraded function signatures in decompiled C/C++ code.",
  "intent_triggers": [
    "use the restore function signatures agent skill",
    "run a workflow for restore function signatures",
    "help me with restore function signatures",
    "我需要處理 restore function signatures 相關工作",
    "幫我啟用 restore function signatures 這個 skill",
    "用 agent skill 完成 restore function signatures",
    "use restore function signatures from CodeRepairAgent"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the restore function signatures skill for this task.",
      "invocation": "codex --skill restore-function-signatures \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skills-rest"
  ]
}
