{
  "name": "xcodebuildmcp-cli",
  "version": "0.1.0",
  "category": "devops",
  "description": "Official skill for the XcodeBuildMCP CLI.",
  "intent_triggers": [
    "use the xcodebuildmcp cli agent skill",
    "run a workflow for xcodebuildmcp cli",
    "help me with xcodebuildmcp cli",
    "我需要處理 xcodebuildmcp cli 相關工作",
    "幫我啟用 xcodebuildmcp cli 這個 skill",
    "用 agent skill 完成 xcodebuildmcp cli",
    "use xcodebuildmcp cli from xcodebuildmcp"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the xcodebuildmcp cli skill for this task.",
      "invocation": "codex --skill xcodebuildmcp-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
