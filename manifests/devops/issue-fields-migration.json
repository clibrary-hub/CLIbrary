{
  "name": "issue-fields-migration",
  "version": "0.1.0",
  "category": "devops",
  "description": "Bulk-migrate metadata to GitHub issue fields from two sources: repo labels (e.g.",
  "intent_triggers": [
    "use the issue fields migration agent skill",
    "run a workflow for issue fields migration",
    "help me with issue fields migration",
    "我需要處理 issue fields migration 相關工作",
    "幫我啟用 issue fields migration 這個 skill",
    "用 agent skill 完成 issue fields migration",
    "use issue fields migration from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the issue fields migration skill for this task.",
      "invocation": "codex --skill issue-fields-migration \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
