{
  "name": "apollo-mcp-server",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for apollo mcp server workflows.",
  "intent_triggers": [
    "use the apollo mcp server agent skill",
    "run a workflow for apollo mcp server",
    "help me with apollo mcp server",
    "我需要處理 apollo mcp server 相關工作",
    "幫我啟用 apollo mcp server 這個 skill",
    "用 agent skill 完成 apollo mcp server",
    "use apollo mcp server from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the apollo mcp server skill for this task.",
      "invocation": "codex --skill apollo-mcp-server \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
