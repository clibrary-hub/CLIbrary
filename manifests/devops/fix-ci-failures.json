{
  "name": "fix-ci-failures",
  "version": "0.1.0",
  "category": "devops",
  "description": "Investigate and fix CI failures on a pull request.",
  "intent_triggers": [
    "use the fix ci failures agent skill",
    "run a workflow for fix ci failures",
    "help me with fix ci failures",
    "我需要處理 fix ci failures 相關工作",
    "幫我啟用 fix ci failures 這個 skill",
    "用 agent skill 完成 fix ci failures",
    "use fix ci failures from vscode"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the fix ci failures skill for this task.",
      "invocation": "codex --skill fix-ci-failures \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
