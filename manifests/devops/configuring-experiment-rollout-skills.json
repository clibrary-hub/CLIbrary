{
  "name": "configuring-experiment-rollout-skills",
  "version": "0.1.0",
  "category": "devops",
  "description": "Configures the rollout shape of a PostHog experiment — the variant split (50/50, 80/20, A/B/C ratios), the overall rollout percentage that gates how many users enter the experiment, and the disambi...",
  "intent_triggers": [
    "use the configuring experiment rollout agent skill",
    "run a workflow for configuring experiment rollout",
    "help me with configuring experiment rollout",
    "我需要處理 configuring experiment rollout 相關工作",
    "幫我啟用 configuring experiment rollout 這個 skill",
    "用 agent skill 完成 configuring experiment rollout",
    "use configuring experiment rollout from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the configuring experiment rollout skill for this task.",
      "invocation": "codex --skill configuring-experiment-rollout \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
