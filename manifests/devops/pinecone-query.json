{
  "name": "pinecone-query",
  "version": "0.1.0",
  "category": "devops",
  "description": "Query integrated indexes using text with Pinecone MCP.",
  "intent_triggers": [
    "use the pinecone query agent skill",
    "run a workflow for pinecone query",
    "help me with pinecone query",
    "我需要處理 pinecone query 相關工作",
    "幫我啟用 pinecone query 這個 skill",
    "用 agent skill 完成 pinecone query",
    "use pinecone query from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the pinecone query skill for this task.",
      "invocation": "codex --skill pinecone-query \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
