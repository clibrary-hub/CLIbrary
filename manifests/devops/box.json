{
  "name": "box",
  "version": "0.1.0",
  "category": "devops",
  "description": "Build and troubleshoot Box integrations for uploads, folders, folder listings, downloads and previews, shared links, collaborations, search, metadata, event-driven automations, and Box AI retrieval...",
  "intent_triggers": [
    "use the box agent skill",
    "run a workflow for box",
    "help me with box",
    "我需要處理 box 相關工作",
    "幫我啟用 box 這個 skill",
    "用 agent skill 完成 box",
    "use box from box-for-ai"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the box skill for this task.",
      "invocation": "codex --skill box \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
