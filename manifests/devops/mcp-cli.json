{
  "name": "mcp-cli",
  "version": "0.1.0",
  "category": "devops",
  "description": "Interface for MCP (Model Context Protocol) servers via CLI.",
  "intent_triggers": [
    "use the mcp cli agent skill",
    "run a workflow for mcp cli",
    "help me with mcp cli",
    "我需要處理 mcp cli 相關工作",
    "幫我啟用 mcp cli 這個 skill",
    "用 agent skill 完成 mcp cli",
    "use mcp cli from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mcp cli skill for this task.",
      "invocation": "codex --skill mcp-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
