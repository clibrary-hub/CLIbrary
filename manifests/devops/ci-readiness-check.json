{
  "name": "ci-readiness-check",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use when the user explicitly asks for a CI check or to push their branch — e.g.",
  "intent_triggers": [
    "use the ci readiness check agent skill",
    "run a workflow for ci readiness check",
    "help me with ci readiness check",
    "我需要處理 ci readiness check 相關工作",
    "幫我啟用 ci readiness check 這個 skill",
    "用 agent skill 完成 ci readiness check",
    "use ci readiness check from fluidframework"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the ci readiness check skill for this task.",
      "invocation": "codex --skill ci-readiness-check \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
