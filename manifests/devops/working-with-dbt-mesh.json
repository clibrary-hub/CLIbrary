{
  "name": "working-with-dbt-mesh",
  "version": "0.1.0",
  "category": "devops",
  "description": "Implements dbt Mesh governance features (model contracts, access modifiers, groups, versioning) and multi-project collaboration with cross-project refs.",
  "intent_triggers": [
    "use the working with dbt mesh agent skill",
    "run a workflow for working with dbt mesh",
    "help me with working with dbt mesh",
    "我需要處理 working with dbt mesh 相關工作",
    "幫我啟用 working with dbt mesh 這個 skill",
    "用 agent skill 完成 working with dbt mesh"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the working with dbt mesh skill for this task.",
      "invocation": "codex --skill working-with-dbt-mesh \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skill"
  ]
}
