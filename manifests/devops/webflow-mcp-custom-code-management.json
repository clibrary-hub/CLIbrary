{
  "name": "webflow-mcp-custom-code-management",
  "version": "0.1.0",
  "category": "devops",
  "description": "Add, review, or remove inline custom scripts on a Webflow site (up to 10,000 chars).",
  "intent_triggers": [
    "use the webflow mcp custom code management agent skill",
    "run a workflow for webflow mcp custom code management",
    "help me with webflow mcp custom code management",
    "我需要處理 webflow mcp custom code management 相關工作",
    "幫我啟用 webflow mcp custom code management 這個 skill",
    "用 agent skill 完成 webflow mcp custom code management",
    "use webflow mcp custom code management from webflow-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the webflow mcp custom code management skill for this task.",
      "invocation": "codex --skill webflow-mcp-custom-code-management \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
