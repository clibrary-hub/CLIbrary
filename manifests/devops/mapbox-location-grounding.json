{
  "name": "mapbox-location-grounding",
  "version": "0.1.0",
  "category": "devops",
  "description": "Compose Mapbox MCP tools to produce grounded, cited location-aware responses from live data instead of training data",
  "intent_triggers": [
    "use the mapbox location grounding agent skill",
    "run a workflow for mapbox location grounding",
    "help me with mapbox location grounding",
    "我需要處理 mapbox location grounding 相關工作",
    "幫我啟用 mapbox location grounding 這個 skill",
    "用 agent skill 完成 mapbox location grounding",
    "use mapbox location grounding from mapbox-agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mapbox location grounding skill for this task.",
      "invocation": "codex --skill mapbox-location-grounding \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
