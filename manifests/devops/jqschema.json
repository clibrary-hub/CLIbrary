{
  "name": "jqschema",
  "version": "0.1.0",
  "category": "devops",
  "description": "Infer JSON structure and types with jq-based schema discovery.",
  "intent_triggers": [
    "use the jqschema agent skill",
    "run a workflow for jqschema",
    "help me with jqschema",
    "我需要處理 jqschema 相關工作",
    "幫我啟用 jqschema 這個 skill",
    "用 agent skill 完成 jqschema",
    "use jqschema from gh-aw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the jqschema skill for this task.",
      "invocation": "codex --skill jqschema \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
