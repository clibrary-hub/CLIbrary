{
  "name": "sred-work-summary-skills",
  "version": "0.1.0",
  "category": "devops",
  "description": "Go back through the previous year of work and create a Notion doc that groups relevant links into projects that can then be documented as SRED projects.",
  "intent_triggers": [
    "use the sred work summary agent skill",
    "run a workflow for sred work summary",
    "help me with sred work summary",
    "我需要處理 sred work summary 相關工作",
    "幫我啟用 sred work summary 這個 skill",
    "用 agent skill 完成 sred work summary",
    "use sred work summary from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sred work summary skill for this task.",
      "invocation": "codex --skill sred-work-summary \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
