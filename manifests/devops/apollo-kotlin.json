{
  "name": "apollo-kotlin",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for apollo kotlin workflows.",
  "intent_triggers": [
    "use the apollo kotlin agent skill",
    "run a workflow for apollo kotlin",
    "help me with apollo kotlin",
    "我需要處理 apollo kotlin 相關工作",
    "幫我啟用 apollo kotlin 這個 skill",
    "用 agent skill 完成 apollo kotlin",
    "use apollo kotlin from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the apollo kotlin skill for this task.",
      "invocation": "codex --skill apollo-kotlin \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
