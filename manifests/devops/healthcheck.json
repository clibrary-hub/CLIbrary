{
  "name": "healthcheck",
  "version": "0.1.0",
  "category": "devops",
  "description": "Host security hardening and risk-tolerance configuration for OpenClaw deployments.",
  "intent_triggers": [
    "use the healthcheck agent skill",
    "run a workflow for healthcheck",
    "help me with healthcheck",
    "我需要處理 healthcheck 相關工作",
    "幫我啟用 healthcheck 這個 skill",
    "用 agent skill 完成 healthcheck",
    "use healthcheck from openclaw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the healthcheck skill for this task.",
      "invocation": "codex --skill healthcheck \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
