{
  "name": "semantic-kernel",
  "version": "0.1.0",
  "category": "devops",
  "description": "Create, update, refactor, explain, or review Semantic Kernel solutions using shared guidance plus language-specific references for .NET and Python.",
  "intent_triggers": [
    "use the semantic kernel agent skill",
    "run a workflow for semantic kernel",
    "help me with semantic kernel",
    "我需要處理 semantic kernel 相關工作",
    "幫我啟用 semantic kernel 這個 skill",
    "用 agent skill 完成 semantic kernel",
    "use semantic kernel from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the semantic kernel skill for this task.",
      "invocation": "codex --skill semantic-kernel \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
