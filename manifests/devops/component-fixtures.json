{
  "name": "component-fixtures",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use when creating or updating component fixtures for screenshot testing, or when designing UI components to be fixture-friendly.",
  "intent_triggers": [
    "use the component fixtures agent skill",
    "run a workflow for component fixtures",
    "help me with component fixtures",
    "我需要處理 component fixtures 相關工作",
    "幫我啟用 component fixtures 這個 skill",
    "用 agent skill 完成 component fixtures",
    "use component fixtures from vscode"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the component fixtures skill for this task.",
      "invocation": "codex --skill component-fixtures \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
