{
  "name": "shopify-pos-ui",
  "version": "0.1.0",
  "category": "devops",
  "description": "Build retail point-of-sale applications using Shopify's POS UI components.",
  "intent_triggers": [
    "use the shopify pos ui agent skill",
    "run a workflow for shopify pos ui",
    "help me with shopify pos ui",
    "我需要處理 shopify pos ui 相關工作",
    "幫我啟用 shopify pos ui 這個 skill",
    "用 agent skill 完成 shopify pos ui",
    "use shopify pos ui from shopify-ai-toolkit"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the shopify pos ui skill for this task.",
      "invocation": "codex --skill shopify-pos-ui \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
