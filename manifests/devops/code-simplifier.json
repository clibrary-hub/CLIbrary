{
  "name": "code-simplifier",
  "version": "0.1.0",
  "category": "devops",
  "description": "Simplifies and refines code for clarity, consistency, and maintainability while preserving all functionality.",
  "intent_triggers": [
    "use the code simplifier agent skill",
    "run a workflow for code simplifier",
    "help me with code simplifier",
    "我需要處理 code simplifier 相關工作",
    "幫我啟用 code simplifier 這個 skill",
    "用 agent skill 完成 code simplifier",
    "use code simplifier from sentry-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the code simplifier skill for this task.",
      "invocation": "codex --skill code-simplifier \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
