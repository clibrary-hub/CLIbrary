{
  "name": "batch-files",
  "version": "0.1.0",
  "category": "devops",
  "description": "Expert-level Windows batch file (.bat/.cmd) skill for writing, debugging, and maintaining CMD scripts.",
  "intent_triggers": [
    "use the batch files agent skill",
    "run a workflow for batch files",
    "help me with batch files",
    "我需要處理 batch files 相關工作",
    "幫我啟用 batch files 這個 skill",
    "用 agent skill 完成 batch files",
    "use batch files from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the batch files skill for this task.",
      "invocation": "codex --skill batch-files \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
