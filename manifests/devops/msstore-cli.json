{
  "name": "msstore-cli",
  "version": "0.1.0",
  "category": "devops",
  "description": "Microsoft Store Developer CLI (msstore) for publishing Windows applications to the Microsoft Store.",
  "intent_triggers": [
    "use the msstore cli agent skill",
    "run a workflow for msstore cli",
    "help me with msstore cli",
    "我需要處理 msstore cli 相關工作",
    "幫我啟用 msstore cli 這個 skill",
    "用 agent skill 完成 msstore cli",
    "use msstore cli from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the msstore cli skill for this task.",
      "invocation": "codex --skill msstore-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
