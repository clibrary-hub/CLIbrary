{
  "name": "qdrant-clients-sdk",
  "version": "0.1.0",
  "category": "devops",
  "description": "Qdrant provides client SDKs for various programming languages, allowing easy integration with Qdrant deployments.",
  "intent_triggers": [
    "use the qdrant clients sdk agent skill",
    "run a workflow for qdrant clients sdk",
    "help me with qdrant clients sdk",
    "我需要處理 qdrant clients sdk 相關工作",
    "幫我啟用 qdrant clients sdk 這個 skill",
    "用 agent skill 完成 qdrant clients sdk",
    "use qdrant clients sdk from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the qdrant clients sdk skill for this task.",
      "invocation": "codex --skill qdrant-clients-sdk \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
