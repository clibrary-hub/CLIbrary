{
  "name": "upgrading-expo-skills",
  "version": "0.1.0",
  "category": "devops",
  "description": "Guidelines for upgrading Expo SDK versions and fixing dependency issues",
  "intent_triggers": [
    "use the upgrading expo agent skill",
    "run a workflow for upgrading expo",
    "help me with upgrading expo",
    "我需要處理 upgrading expo 相關工作",
    "幫我啟用 upgrading expo 這個 skill",
    "用 agent skill 完成 upgrading expo",
    "use upgrading expo from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the upgrading expo skill for this task.",
      "invocation": "codex --skill upgrading-expo \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
