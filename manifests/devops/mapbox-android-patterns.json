{
  "name": "mapbox-android-patterns",
  "version": "0.1.0",
  "category": "devops",
  "description": "Official integration patterns for Mapbox Maps SDK on Android.",
  "intent_triggers": [
    "use the mapbox android patterns agent skill",
    "run a workflow for mapbox android patterns",
    "help me with mapbox android patterns",
    "我需要處理 mapbox android patterns 相關工作",
    "幫我啟用 mapbox android patterns 這個 skill",
    "用 agent skill 完成 mapbox android patterns",
    "use mapbox android patterns from mapbox-agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mapbox android patterns skill for this task.",
      "invocation": "codex --skill mapbox-android-patterns \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
