{
  "name": "query-metrics",
  "version": "0.1.0",
  "category": "devops",
  "description": "Runs metrics queries against Axiom MetricsDB via scripts.",
  "intent_triggers": [
    "use the query metrics agent skill",
    "run a workflow for query metrics",
    "help me with query metrics",
    "我需要處理 query metrics 相關工作",
    "幫我啟用 query metrics 這個 skill",
    "用 agent skill 完成 query metrics",
    "use query metrics from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the query metrics skill for this task.",
      "invocation": "codex --skill query-metrics \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
