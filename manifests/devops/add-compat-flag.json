{
  "name": "add-compat-flag",
  "version": "0.1.0",
  "category": "devops",
  "description": "Step-by-step guide for adding a new compatibility flag to workerd, including capnp schema, C++ usage, testing, and documentation requirements.",
  "intent_triggers": [
    "use the add compat flag agent skill",
    "run a workflow for add compat flag",
    "help me with add compat flag",
    "我需要處理 add compat flag 相關工作",
    "幫我啟用 add compat flag 這個 skill",
    "用 agent skill 完成 add compat flag",
    "use add compat flag from workerd"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the add compat flag skill for this task.",
      "invocation": "codex --skill add-compat-flag \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
