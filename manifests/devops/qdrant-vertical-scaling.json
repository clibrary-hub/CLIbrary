{
  "name": "qdrant-vertical-scaling",
  "version": "0.1.0",
  "category": "devops",
  "description": "Guides Qdrant vertical scaling decisions.",
  "intent_triggers": [
    "use the qdrant vertical scaling agent skill",
    "run a workflow for qdrant vertical scaling",
    "help me with qdrant vertical scaling",
    "我需要處理 qdrant vertical scaling 相關工作",
    "幫我啟用 qdrant vertical scaling 這個 skill",
    "用 agent skill 完成 qdrant vertical scaling"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the qdrant vertical scaling skill for this task.",
      "invocation": "codex --skill qdrant-vertical-scaling \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skill"
  ]
}
