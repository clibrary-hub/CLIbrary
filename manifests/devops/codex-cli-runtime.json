{
  "name": "codex-cli-runtime",
  "version": "0.1.0",
  "category": "devops",
  "description": "Internal helper contract for calling the codex-companion runtime from Claude Code",
  "intent_triggers": [
    "use the codex cli runtime agent skill",
    "run a workflow for codex cli runtime",
    "help me with codex cli runtime",
    "我需要處理 codex cli runtime 相關工作",
    "幫我啟用 codex cli runtime 這個 skill",
    "用 agent skill 完成 codex cli runtime",
    "use codex cli runtime from codex-plugin-cc"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the codex cli runtime skill for this task.",
      "invocation": "codex --skill codex-cli-runtime \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
