{
  "name": "anthropic-sdk-upgrader",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use this agent when the user needs to upgrade Anthropic SDK packages.",
  "intent_triggers": [
    "use the anthropic sdk upgrader agent skill",
    "run a workflow for anthropic sdk upgrader",
    "help me with anthropic sdk upgrader",
    "我需要處理 anthropic sdk upgrader 相關工作",
    "幫我啟用 anthropic sdk upgrader 這個 skill",
    "用 agent skill 完成 anthropic sdk upgrader",
    "use anthropic sdk upgrader from vscode-copilot-chat"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the anthropic sdk upgrader skill for this task.",
      "invocation": "codex --skill anthropic-sdk-upgrader \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
