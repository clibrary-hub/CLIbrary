{
  "name": "bisect",
  "version": "0.1.0",
  "category": "devops",
  "description": "Bisect a ClickHouse regression using pre-built master binaries from CI.",
  "intent_triggers": [
    "use the bisect agent skill",
    "run a workflow for bisect",
    "help me with bisect",
    "我需要處理 bisect 相關工作",
    "幫我啟用 bisect 這個 skill",
    "用 agent skill 完成 bisect",
    "use bisect from clickhouse"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the bisect skill for this task.",
      "invocation": "codex --skill bisect \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
