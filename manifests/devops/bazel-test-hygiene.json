{
  "name": "bazel-test-hygiene",
  "version": "0.1.0",
  "category": "devops",
  "description": "Mandatory rules for running bazel tests during development.",
  "intent_triggers": [
    "use the bazel test hygiene agent skill",
    "run a workflow for bazel test hygiene",
    "help me with bazel test hygiene",
    "我需要處理 bazel test hygiene 相關工作",
    "幫我啟用 bazel test hygiene 這個 skill",
    "用 agent skill 完成 bazel test hygiene",
    "use bazel test hygiene from workerd"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the bazel test hygiene skill for this task.",
      "invocation": "codex --skill bazel-test-hygiene \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
