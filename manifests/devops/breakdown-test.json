{
  "name": "breakdown-test",
  "version": "0.1.0",
  "category": "devops",
  "description": "Test Planning and Quality Assurance prompt that generates comprehensive test strategies, task breakdowns, and quality validation plans for GitHub projects.",
  "intent_triggers": [
    "use the breakdown test agent skill",
    "run a workflow for breakdown test",
    "help me with breakdown test",
    "我需要處理 breakdown test 相關工作",
    "幫我啟用 breakdown test 這個 skill",
    "用 agent skill 完成 breakdown test",
    "use breakdown test from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the breakdown test skill for this task.",
      "invocation": "codex --skill breakdown-test \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
