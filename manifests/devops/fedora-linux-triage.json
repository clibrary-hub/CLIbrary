{
  "name": "fedora-linux-triage",
  "version": "0.1.0",
  "category": "devops",
  "description": "Triage and resolve Fedora issues with dnf, systemd, and SELinux-aware guidance.",
  "intent_triggers": [
    "use the fedora linux triage agent skill",
    "run a workflow for fedora linux triage",
    "help me with fedora linux triage",
    "我需要處理 fedora linux triage 相關工作",
    "幫我啟用 fedora linux triage 這個 skill",
    "用 agent skill 完成 fedora linux triage",
    "use fedora linux triage from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the fedora linux triage skill for this task.",
      "invocation": "codex --skill fedora-linux-triage \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
