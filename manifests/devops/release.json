{
  "name": "release",
  "version": "0.1.0",
  "category": "devops",
  "description": "Publish a new Sentry JavaScript SDK release.",
  "intent_triggers": [
    "use the release agent skill",
    "run a workflow for release",
    "help me with release",
    "我需要處理 release 相關工作",
    "幫我啟用 release 這個 skill",
    "用 agent skill 完成 release",
    "use release from sentry-javascript"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the release skill for this task.",
      "invocation": "codex --skill release \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
