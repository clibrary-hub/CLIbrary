{
  "name": "memory-leak-audit",
  "version": "0.1.0",
  "category": "devops",
  "description": "Audit code for memory leaks and disposable issues.",
  "intent_triggers": [
    "use the memory leak audit agent skill",
    "run a workflow for memory leak audit",
    "help me with memory leak audit",
    "我需要處理 memory leak audit 相關工作",
    "幫我啟用 memory leak audit 這個 skill",
    "用 agent skill 完成 memory leak audit",
    "use memory leak audit from vscode"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the memory leak audit skill for this task.",
      "invocation": "codex --skill memory-leak-audit \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
