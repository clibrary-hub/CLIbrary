{
  "name": "firebase-firestore",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for firebase firestore workflows.",
  "intent_triggers": [
    "use the firebase firestore agent skill",
    "run a workflow for firebase firestore",
    "help me with firebase firestore",
    "我需要處理 firebase firestore 相關工作",
    "幫我啟用 firebase firestore 這個 skill",
    "用 agent skill 完成 firebase firestore",
    "use firebase firestore from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the firebase firestore skill for this task.",
      "invocation": "codex --skill firebase-firestore \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
