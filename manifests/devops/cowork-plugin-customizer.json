{
  "name": "cowork-plugin-customizer",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for cowork plugin customizer workflows.",
  "intent_triggers": [
    "use the cowork plugin customizer agent skill",
    "run a workflow for cowork plugin customizer",
    "help me with cowork plugin customizer",
    "我需要處理 cowork plugin customizer 相關工作",
    "幫我啟用 cowork plugin customizer 這個 skill",
    "用 agent skill 完成 cowork plugin customizer",
    "use cowork plugin customizer from knowledge-work-plugins"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the cowork plugin customizer skill for this task.",
      "invocation": "codex --skill cowork-plugin-customizer \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
