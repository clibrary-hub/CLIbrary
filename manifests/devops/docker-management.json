{
  "name": "docker-management",
  "version": "0.1.0",
  "category": "devops",
  "description": "管理 Docker containers, images, volumes, networks, and Compose stacks — lifecycle ops, debugging, cleanup, and Dockerfile optimization.",
  "intent_triggers": [
    "use the docker management agent skill",
    "run a workflow for docker management",
    "help me with docker management",
    "我需要處理 docker management 相關工作",
    "幫我啟用 docker management 這個 skill",
    "用 agent skill 完成 docker management",
    "use docker management from hermes-agent-zh"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the docker management skill for this task.",
      "invocation": "codex --skill docker-management \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skills-rest"
  ]
}
