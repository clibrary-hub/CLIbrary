{
  "name": "react-audit-grep-patterns",
  "version": "0.1.0",
  "category": "devops",
  "description": "Provides the complete, verified grep scan command library for auditing React codebases before a React 18.3.1 or React 19 upgrade.",
  "intent_triggers": [
    "use the react audit grep patterns agent skill",
    "run a workflow for react audit grep patterns",
    "help me with react audit grep patterns",
    "我需要處理 react audit grep patterns 相關工作",
    "幫我啟用 react audit grep patterns 這個 skill",
    "用 agent skill 完成 react audit grep patterns",
    "use react audit grep patterns from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the react audit grep patterns skill for this task.",
      "invocation": "codex --skill react-audit-grep-patterns \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
