{
  "name": "migrating-dbt-core-to-fusion",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use when a user needs help triaging dbt-core to Fusion migration errors.",
  "intent_triggers": [
    "use the migrating dbt core to fusion agent skill",
    "run a workflow for migrating dbt core to fusion",
    "help me with migrating dbt core to fusion",
    "我需要處理 migrating dbt core to fusion 相關工作",
    "幫我啟用 migrating dbt core to fusion 這個 skill",
    "用 agent skill 完成 migrating dbt core to fusion"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the migrating dbt core to fusion skill for this task.",
      "invocation": "codex --skill migrating-dbt-core-to-fusion \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skill"
  ]
}
