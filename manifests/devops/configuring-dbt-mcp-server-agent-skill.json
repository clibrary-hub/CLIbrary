{
  "name": "configuring-dbt-mcp-server-agent-skill",
  "version": "0.1.0",
  "category": "devops",
  "description": "Generates MCP server configuration JSON, resolves authentication setup, and validates server connectivity for dbt.",
  "intent_triggers": [
    "use the configuring dbt mcp server agent skill",
    "run a workflow for configuring dbt mcp server",
    "help me with configuring dbt mcp server",
    "我需要處理 configuring dbt mcp server 相關工作",
    "幫我啟用 configuring dbt mcp server 這個 skill",
    "用 agent skill 完成 configuring dbt mcp server"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the configuring dbt mcp server skill for this task.",
      "invocation": "codex --skill configuring-dbt-mcp-server \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skill"
  ]
}
