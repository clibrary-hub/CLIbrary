{
  "name": "recipe-block-focus-time",
  "version": "0.1.0",
  "category": "devops",
  "description": "Create recurring focus time blocks on Google Calendar to protect deep work hours.",
  "intent_triggers": [
    "use the recipe block focus time agent skill",
    "run a workflow for recipe block focus time",
    "help me with recipe block focus time",
    "我需要處理 recipe block focus time 相關工作",
    "幫我啟用 recipe block focus time 這個 skill",
    "用 agent skill 完成 recipe block focus time",
    "use recipe block focus time from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recipe block focus time skill for this task.",
      "invocation": "codex --skill recipe-block-focus-time \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
