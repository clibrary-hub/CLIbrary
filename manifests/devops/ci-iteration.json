{
  "name": "ci-iteration",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for ci iteration workflows.",
  "intent_triggers": [
    "use the ci iteration agent skill",
    "run a workflow for ci iteration",
    "help me with ci iteration",
    "我需要處理 ci iteration 相關工作",
    "幫我啟用 ci iteration 這個 skill",
    "用 agent skill 完成 ci iteration",
    "use ci iteration from erk"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the ci iteration skill for this task.",
      "invocation": "codex --skill ci-iteration \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
