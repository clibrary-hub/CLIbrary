{
  "name": "create-github-action-workflow-specification",
  "version": "0.1.0",
  "category": "devops",
  "description": "Create a formal specification for an existing GitHub Actions CI/CD workflow, optimized for AI consumption and workflow maintenance.",
  "intent_triggers": [
    "use the create github action workflow specification agent skill",
    "run a workflow for create github action workflow specification",
    "help me with create github action workflow specification",
    "我需要處理 create github action workflow specification 相關工作",
    "幫我啟用 create github action workflow specification 這個 skill",
    "用 agent skill 完成 create github action workflow specification",
    "use create github action workflow specification from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the create github action workflow specification skill for this task.",
      "invocation": "codex --skill create-github-action-workflow-specification \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
