{
  "name": "graphite",
  "version": "0.1.0",
  "category": "devops",
  "description": "Work with Graphite (gt) for stacked PRs - creating, navigating, and managing PR stacks.",
  "intent_triggers": [
    "use the graphite agent skill",
    "run a workflow for graphite",
    "help me with graphite",
    "我需要處理 graphite 相關工作",
    "幫我啟用 graphite 這個 skill",
    "用 agent skill 完成 graphite",
    "use graphite from erk"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the graphite skill for this task.",
      "invocation": "codex --skill graphite \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
