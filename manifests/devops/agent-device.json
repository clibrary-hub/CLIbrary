{
  "name": "agent-device",
  "version": "0.1.0",
  "category": "devops",
  "description": "Automates Apple-platform apps (iOS, tvOS, macOS) and Android devices.",
  "intent_triggers": [
    "use the agent device agent skill",
    "run a workflow for agent device",
    "help me with agent device",
    "我需要處理 agent device 相關工作",
    "幫我啟用 agent device 這個 skill",
    "用 agent skill 完成 agent device",
    "use agent device from agent-device"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the agent device skill for this task.",
      "invocation": "codex --skill agent-device \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
