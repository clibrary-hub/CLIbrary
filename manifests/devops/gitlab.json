{
  "name": "gitlab",
  "version": "0.1.0",
  "category": "devops",
  "description": "Manage GitLab merge requests and pipelines with a Python CLI - Brought to you by microsoft/hve-core",
  "intent_triggers": [
    "use the gitlab agent skill",
    "run a workflow for gitlab",
    "help me with gitlab",
    "我需要處理 gitlab 相關工作",
    "幫我啟用 gitlab 這個 skill",
    "用 agent skill 完成 gitlab",
    "use gitlab from hve-core"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the gitlab skill for this task.",
      "invocation": "codex --skill gitlab \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
