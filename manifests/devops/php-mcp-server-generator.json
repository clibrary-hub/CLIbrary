{
  "name": "php-mcp-server-generator",
  "version": "0.1.0",
  "category": "devops",
  "description": "Generate a complete PHP Model Context Protocol server project with tools, resources, prompts, and tests using the official PHP SDK",
  "intent_triggers": [
    "use the php mcp server generator agent skill",
    "run a workflow for php mcp server generator",
    "help me with php mcp server generator",
    "我需要處理 php mcp server generator 相關工作",
    "幫我啟用 php mcp server generator 這個 skill",
    "用 agent skill 完成 php mcp server generator",
    "use php mcp server generator from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the php mcp server generator skill for this task.",
      "invocation": "codex --skill php-mcp-server-generator \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
