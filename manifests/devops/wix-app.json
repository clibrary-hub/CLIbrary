{
  "name": "wix-app",
  "version": "0.1.0",
  "category": "devops",
  "description": "Build and review Wix CLI app extensions — dashboard pages, modals, plugins, menu plugins, custom element widgets, Editor React components, site plugins, embedded scripts, backend APIs, backend even...",
  "intent_triggers": [
    "use the wix app agent skill",
    "run a workflow for wix app",
    "help me with wix app",
    "我需要處理 wix app 相關工作",
    "幫我啟用 wix app 這個 skill",
    "用 agent skill 完成 wix app",
    "use wix app from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the wix app skill for this task.",
      "invocation": "codex --skill wix-app \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
