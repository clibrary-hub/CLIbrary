{
  "name": "kotlin-mcp-server-generator",
  "version": "0.1.0",
  "category": "devops",
  "description": "Generate a complete Kotlin MCP server project with proper structure, dependencies, and implementation using the official io.modelcontextprotocol:kotlin-sdk library.",
  "intent_triggers": [
    "use the kotlin mcp server generator agent skill",
    "run a workflow for kotlin mcp server generator",
    "help me with kotlin mcp server generator",
    "我需要處理 kotlin mcp server generator 相關工作",
    "幫我啟用 kotlin mcp server generator 這個 skill",
    "用 agent skill 完成 kotlin mcp server generator",
    "use kotlin mcp server generator from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the kotlin mcp server generator skill for this task.",
      "invocation": "codex --skill kotlin-mcp-server-generator \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
