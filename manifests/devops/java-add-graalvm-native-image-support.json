{
  "name": "java-add-graalvm-native-image-support",
  "version": "0.1.0",
  "category": "devops",
  "description": "GraalVM Native Image expert that adds native image support to Java applications, builds the project, analyzes build errors, applies fixes, and iterates until successful compilation using Oracle bes...",
  "intent_triggers": [
    "use the java add graalvm native image support agent skill",
    "run a workflow for java add graalvm native image support",
    "help me with java add graalvm native image support",
    "我需要處理 java add graalvm native image support 相關工作",
    "幫我啟用 java add graalvm native image support 這個 skill",
    "用 agent skill 完成 java add graalvm native image support",
    "use java add graalvm native image support from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the java add graalvm native image support skill for this task.",
      "invocation": "codex --skill java-add-graalvm-native-image-support \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
