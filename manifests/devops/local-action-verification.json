{
  "name": "local-action-verification",
  "version": "0.1.0",
  "category": "devops",
  "description": "Set up a repository for local GitHub Actions verification using act, so Jules can validate CI before pushing",
  "intent_triggers": [
    "use the local action verification agent skill",
    "run a workflow for local action verification",
    "help me with local action verification",
    "我需要處理 local action verification 相關工作",
    "幫我啟用 local action verification 這個 skill",
    "用 agent skill 完成 local action verification",
    "use local action verification from jules-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the local action verification skill for this task.",
      "invocation": "codex --skill local-action-verification \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
