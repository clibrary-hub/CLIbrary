{
  "name": "neon-postgres-branches",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for neon postgres branches workflows.",
  "intent_triggers": [
    "use the neon postgres branches agent skill",
    "run a workflow for neon postgres branches",
    "help me with neon postgres branches",
    "我需要處理 neon postgres branches 相關工作",
    "幫我啟用 neon postgres branches 這個 skill",
    "用 agent skill 完成 neon postgres branches",
    "use neon postgres branches from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the neon postgres branches skill for this task.",
      "invocation": "codex --skill neon-postgres-branches \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
