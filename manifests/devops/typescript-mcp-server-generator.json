{
  "name": "typescript-mcp-server-generator",
  "version": "0.1.0",
  "category": "devops",
  "description": "Generate a complete MCP server project in TypeScript with tools, resources, and proper configuration",
  "intent_triggers": [
    "use the typescript mcp server generator agent skill",
    "run a workflow for typescript mcp server generator",
    "help me with typescript mcp server generator",
    "我需要處理 typescript mcp server generator 相關工作",
    "幫我啟用 typescript mcp server generator 這個 skill",
    "用 agent skill 完成 typescript mcp server generator",
    "use typescript mcp server generator from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the typescript mcp server generator skill for this task.",
      "invocation": "codex --skill typescript-mcp-server-generator \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
