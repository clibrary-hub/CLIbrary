{
  "name": "agent-browser",
  "version": "0.1.0",
  "category": "devops",
  "description": "Browser automation CLI for AI agents.",
  "intent_triggers": [
    "use the agent browser agent skill",
    "run a workflow for agent browser",
    "help me with agent browser",
    "我需要處理 agent browser 相關工作",
    "幫我啟用 agent browser 這個 skill",
    "用 agent skill 完成 agent browser",
    "use agent browser from agent-browser"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the agent browser skill for this task.",
      "invocation": "codex --skill agent-browser \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
