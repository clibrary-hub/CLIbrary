{
  "name": "rename-swarm",
  "version": "0.1.0",
  "category": "devops",
  "description": "Parallel bulk rename across many files using a swarm of haiku agents.",
  "intent_triggers": [
    "use the rename swarm agent skill",
    "run a workflow for rename swarm",
    "help me with rename swarm",
    "我需要處理 rename swarm 相關工作",
    "幫我啟用 rename swarm 這個 skill",
    "用 agent skill 完成 rename swarm",
    "use rename swarm from erk"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the rename swarm skill for this task.",
      "invocation": "codex --skill rename-swarm \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
