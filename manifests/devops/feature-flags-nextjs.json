{
  "name": "feature-flags-nextjs",
  "version": "0.1.0",
  "category": "devops",
  "description": "PostHog feature flags for Next.js applications",
  "intent_triggers": [
    "use the feature flags nextjs agent skill",
    "run a workflow for feature flags nextjs",
    "help me with feature flags nextjs",
    "我需要處理 feature flags nextjs 相關工作",
    "幫我啟用 feature flags nextjs 這個 skill",
    "用 agent skill 完成 feature flags nextjs",
    "use feature flags nextjs from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the feature flags nextjs skill for this task.",
      "invocation": "codex --skill feature-flags-nextjs \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
