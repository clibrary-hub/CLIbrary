{
  "name": "imsg",
  "version": "0.1.0",
  "category": "devops",
  "description": "iMessage/SMS CLI for listing chats, history, and sending messages via Messages.app.",
  "intent_triggers": [
    "use the imsg agent skill",
    "run a workflow for imsg",
    "help me with imsg",
    "我需要處理 imsg 相關工作",
    "幫我啟用 imsg 這個 skill",
    "用 agent skill 完成 imsg",
    "use imsg from openclaw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the imsg skill for this task.",
      "invocation": "codex --skill imsg \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
