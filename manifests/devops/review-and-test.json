{
  "name": "review-and-test",
  "version": "0.1.0",
  "category": "devops",
  "description": "Review a FlashList PR or branch, run unit tests, test on iOS simulator, and verify RTL/LTR behavior.",
  "intent_triggers": [
    "use the review and test agent skill",
    "run a workflow for review and test",
    "help me with review and test",
    "我需要處理 review and test 相關工作",
    "幫我啟用 review and test 這個 skill",
    "用 agent skill 完成 review and test",
    "use review and test from flash-list"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the review and test skill for this task.",
      "invocation": "codex --skill review-and-test \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
