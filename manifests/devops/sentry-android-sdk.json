{
  "name": "sentry-android-sdk",
  "version": "0.1.0",
  "category": "devops",
  "description": "Full Sentry SDK setup for Android.",
  "intent_triggers": [
    "use the sentry android sdk agent skill",
    "run a workflow for sentry android sdk",
    "help me with sentry android sdk",
    "我需要處理 sentry android sdk 相關工作",
    "幫我啟用 sentry android sdk 這個 skill",
    "用 agent skill 完成 sentry android sdk",
    "use sentry android sdk from sentry-for-ai"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sentry android sdk skill for this task.",
      "invocation": "codex --skill sentry-android-sdk \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
