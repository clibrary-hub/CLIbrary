{
  "name": "recompile-workflows",
  "version": "0.1.0",
  "category": "devops",
  "description": "Regenerate and post-process all agentic workflows.",
  "intent_triggers": [
    "use the recompile workflows agent skill",
    "run a workflow for recompile workflows",
    "help me with recompile workflows",
    "我需要處理 recompile workflows 相關工作",
    "幫我啟用 recompile workflows 這個 skill",
    "用 agent skill 完成 recompile workflows",
    "use recompile workflows from gh-aw-firewall"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recompile workflows skill for this task.",
      "invocation": "codex --skill recompile-workflows \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
