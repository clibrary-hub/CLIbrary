{
  "name": "shopify-admin",
  "version": "0.1.0",
  "category": "devops",
  "description": "Write or explain **Admin GraphQL** queries and mutations for apps and integrations that extend the Shopify admin.",
  "intent_triggers": [
    "use the shopify admin agent skill",
    "run a workflow for shopify admin",
    "help me with shopify admin",
    "我需要處理 shopify admin 相關工作",
    "幫我啟用 shopify admin 這個 skill",
    "用 agent skill 完成 shopify admin",
    "use shopify admin from shopify-ai-toolkit"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the shopify admin skill for this task.",
      "invocation": "codex --skill shopify-admin \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
