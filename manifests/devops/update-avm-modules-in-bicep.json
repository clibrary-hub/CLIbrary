{
  "name": "update-avm-modules-in-bicep",
  "version": "0.1.0",
  "category": "devops",
  "description": "Update Azure Verified Modules (AVM) to latest versions in Bicep files.",
  "intent_triggers": [
    "use the update avm modules in bicep agent skill",
    "run a workflow for update avm modules in bicep",
    "help me with update avm modules in bicep",
    "我需要處理 update avm modules in bicep 相關工作",
    "幫我啟用 update avm modules in bicep 這個 skill",
    "用 agent skill 完成 update avm modules in bicep",
    "use update avm modules in bicep from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the update avm modules in bicep skill for this task.",
      "invocation": "codex --skill update-avm-modules-in-bicep \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
