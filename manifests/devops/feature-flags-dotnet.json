{
  "name": "feature-flags-dotnet",
  "version": "0.1.0",
  "category": "devops",
  "description": "PostHog feature flags for .NET applications",
  "intent_triggers": [
    "use the feature flags dotnet agent skill",
    "run a workflow for feature flags dotnet",
    "help me with feature flags dotnet",
    "我需要處理 feature flags dotnet 相關工作",
    "幫我啟用 feature flags dotnet 這個 skill",
    "用 agent skill 完成 feature flags dotnet",
    "use feature flags dotnet from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the feature flags dotnet skill for this task.",
      "invocation": "codex --skill feature-flags-dotnet \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
