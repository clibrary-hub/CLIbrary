{
  "name": "vercel-queues",
  "version": "0.1.0",
  "category": "devops",
  "description": "Build and operate Vercel Queues with @vercel/queue across push and poll consumers.",
  "intent_triggers": [
    "use the vercel queues agent skill",
    "run a workflow for vercel queues",
    "help me with vercel queues",
    "我需要處理 vercel queues 相關工作",
    "幫我啟用 vercel queues 這個 skill",
    "用 agent skill 完成 vercel queues",
    "use vercel queues from junior"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the vercel queues skill for this task.",
      "invocation": "codex --skill vercel-queues \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
