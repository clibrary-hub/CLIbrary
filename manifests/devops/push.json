{
  "name": "push",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for push workflows.",
  "intent_triggers": [
    "use the push agent skill",
    "run a workflow for push",
    "help me with push",
    "我需要處理 push 相關工作",
    "幫我啟用 push 這個 skill",
    "用 agent skill 完成 push",
    "use push from symphony"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the push skill for this task.",
      "invocation": "codex --skill push \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
