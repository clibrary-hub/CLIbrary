{
  "name": "rw-recipe-full-setup",
  "version": "0.1.0",
  "category": "devops",
  "description": "Complete Runway API setup: check compatibility, configure API key, and integrate generation endpoints",
  "intent_triggers": [
    "use the rw recipe full setup agent skill",
    "run a workflow for rw recipe full setup",
    "help me with rw recipe full setup",
    "我需要處理 rw recipe full setup 相關工作",
    "幫我啟用 rw recipe full setup 這個 skill",
    "用 agent skill 完成 rw recipe full setup",
    "use rw recipe full setup from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the rw recipe full setup skill for this task.",
      "invocation": "codex --skill rw-recipe-full-setup \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
