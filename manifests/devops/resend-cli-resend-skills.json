{
  "name": "resend-cli-resend-skills",
  "version": "0.1.0",
  "category": "devops",
  "description": "Named auth profile for multi-account setups.",
  "intent_triggers": [
    "use the resend cli agent skill",
    "run a workflow for resend cli",
    "help me with resend cli",
    "我需要處理 resend cli 相關工作",
    "幫我啟用 resend cli 這個 skill",
    "用 agent skill 完成 resend cli",
    "use resend cli from resend-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the resend cli skill for this task.",
      "invocation": "codex --skill resend-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
