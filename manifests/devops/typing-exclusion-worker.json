{
  "name": "typing-exclusion-worker",
  "version": "0.1.0",
  "category": "devops",
  "description": "Python typing exclusion worker: remove assigned mypy exclusion modules in small scoped batches, fix typing issues, run validation, and produce a structured completion summary.",
  "intent_triggers": [
    "use the typing exclusion worker agent skill",
    "run a workflow for typing exclusion worker",
    "help me with typing exclusion worker",
    "我需要處理 typing exclusion worker 相關工作",
    "幫我啟用 typing exclusion worker 這個 skill",
    "用 agent skill 完成 typing exclusion worker",
    "use typing exclusion worker from sentry-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the typing exclusion worker skill for this task.",
      "invocation": "codex --skill typing-exclusion-worker \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
