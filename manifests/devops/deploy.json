{
  "name": "deploy",
  "version": "0.1.0",
  "category": "devops",
  "description": "Deploy applications to Vercel.",
  "intent_triggers": [
    "use the deploy agent skill",
    "run a workflow for deploy",
    "help me with deploy",
    "我需要處理 deploy 相關工作",
    "幫我啟用 deploy 這個 skill",
    "用 agent skill 完成 deploy",
    "use deploy from vercel-deploy-claude-code-plugin"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the deploy skill for this task.",
      "invocation": "codex --skill deploy \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
