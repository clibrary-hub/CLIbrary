{
  "name": "update-screenshots",
  "version": "0.1.0",
  "category": "devops",
  "description": "Download screenshot baselines from the latest CI run and commit them.",
  "intent_triggers": [
    "use the update screenshots agent skill",
    "run a workflow for update screenshots",
    "help me with update screenshots",
    "我需要處理 update screenshots 相關工作",
    "幫我啟用 update screenshots 這個 skill",
    "用 agent skill 完成 update screenshots",
    "use update screenshots from vscode"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the update screenshots skill for this task.",
      "invocation": "codex --skill update-screenshots \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
