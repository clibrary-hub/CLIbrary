{
  "name": "voltra",
  "version": "0.1.0",
  "category": "devops",
  "description": "Build, review, refactor, configure, or debug Voltra code using Voltra JSX, Voltra JS APIs, and the Expo config plugin.",
  "intent_triggers": [
    "use the voltra agent skill",
    "run a workflow for voltra",
    "help me with voltra",
    "我需要處理 voltra 相關工作",
    "幫我啟用 voltra 這個 skill",
    "用 agent skill 完成 voltra",
    "use voltra from voltra"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the voltra skill for this task.",
      "invocation": "codex --skill voltra \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
