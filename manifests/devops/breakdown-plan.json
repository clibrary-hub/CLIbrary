{
  "name": "breakdown-plan",
  "version": "0.1.0",
  "category": "devops",
  "description": "Issue Planning and Automation prompt that generates comprehensive project plans with Epic > Feature > Story/Enabler > Test hierarchy, dependencies, priorities, and automated tracking.",
  "intent_triggers": [
    "use the breakdown plan agent skill",
    "run a workflow for breakdown plan",
    "help me with breakdown plan",
    "我需要處理 breakdown plan 相關工作",
    "幫我啟用 breakdown plan 這個 skill",
    "用 agent skill 完成 breakdown plan",
    "use breakdown plan from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the breakdown plan skill for this task.",
      "invocation": "codex --skill breakdown-plan \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
