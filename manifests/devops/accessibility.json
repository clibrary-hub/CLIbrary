{
  "name": "accessibility",
  "version": "0.1.0",
  "category": "devops",
  "description": "Primary accessibility skill for VS Code.",
  "intent_triggers": [
    "use the accessibility agent skill",
    "run a workflow for accessibility",
    "help me with accessibility",
    "我需要處理 accessibility 相關工作",
    "幫我啟用 accessibility 這個 skill",
    "用 agent skill 完成 accessibility",
    "use accessibility from vscode"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the accessibility skill for this task.",
      "invocation": "codex --skill accessibility \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
