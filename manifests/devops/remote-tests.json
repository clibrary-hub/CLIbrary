{
  "name": "remote-tests",
  "version": "0.1.0",
  "category": "devops",
  "description": "How to run tests using remote executor.",
  "intent_triggers": [
    "use the remote tests agent skill",
    "run a workflow for remote tests",
    "help me with remote tests",
    "我需要處理 remote tests 相關工作",
    "幫我啟用 remote tests 這個 skill",
    "用 agent skill 完成 remote tests",
    "use remote tests from codex"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the remote tests skill for this task.",
      "invocation": "codex --skill remote-tests \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
