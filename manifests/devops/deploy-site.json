{
  "name": "deploy-site",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for deploy site workflows.",
  "intent_triggers": [
    "use the deploy site agent skill",
    "run a workflow for deploy site",
    "help me with deploy site",
    "我需要處理 deploy site 相關工作",
    "幫我啟用 deploy site 這個 skill",
    "用 agent skill 完成 deploy site",
    "use deploy site from power-platform-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the deploy site skill for this task.",
      "invocation": "codex --skill deploy-site \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
