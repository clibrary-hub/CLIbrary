{
  "name": "acreadiness-policy",
  "version": "0.1.0",
  "category": "devops",
  "description": "Help the user pick, write, or apply an AgentRC policy.",
  "intent_triggers": [
    "use the acreadiness policy agent skill",
    "run a workflow for acreadiness policy",
    "help me with acreadiness policy",
    "我需要處理 acreadiness policy 相關工作",
    "幫我啟用 acreadiness policy 這個 skill",
    "用 agent skill 完成 acreadiness policy",
    "use acreadiness policy from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the acreadiness policy skill for this task.",
      "invocation": "codex --skill acreadiness-policy \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
