{
  "name": "pr-operations",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use when working with PR review comments, resolving threads, or replying to discussion comments.",
  "intent_triggers": [
    "use the pr operations agent skill",
    "run a workflow for pr operations",
    "help me with pr operations",
    "我需要處理 pr operations 相關工作",
    "幫我啟用 pr operations 這個 skill",
    "用 agent skill 完成 pr operations",
    "use pr operations from erk"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the pr operations skill for this task.",
      "invocation": "codex --skill pr-operations \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
