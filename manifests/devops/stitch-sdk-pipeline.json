{
  "name": "stitch-sdk-pipeline",
  "version": "0.1.0",
  "category": "devops",
  "description": "Run the full Stitch SDK generation pipeline.",
  "intent_triggers": [
    "use the stitch sdk pipeline agent skill",
    "run a workflow for stitch sdk pipeline",
    "help me with stitch sdk pipeline",
    "我需要處理 stitch sdk pipeline 相關工作",
    "幫我啟用 stitch sdk pipeline 這個 skill",
    "用 agent skill 完成 stitch sdk pipeline",
    "use stitch sdk pipeline from stitch-sdk"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the stitch sdk pipeline skill for this task.",
      "invocation": "codex --skill stitch-sdk-pipeline \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
