{
  "name": "xcode-project-setup-skills",
  "version": "0.1.0",
  "category": "devops",
  "description": "Safely modifies Xcode projects (.pbxproj) to add Swift Packages and link files.",
  "intent_triggers": [
    "use the xcode project setup agent skill",
    "run a workflow for xcode project setup",
    "help me with xcode project setup",
    "我需要處理 xcode project setup 相關工作",
    "幫我啟用 xcode project setup 這個 skill",
    "用 agent skill 完成 xcode project setup",
    "use xcode project setup from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the xcode project setup skill for this task.",
      "invocation": "codex --skill xcode-project-setup \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
