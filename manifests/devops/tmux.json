{
  "name": "tmux",
  "version": "0.1.0",
  "category": "devops",
  "description": "Remote-control tmux sessions for interactive CLIs by sending keystrokes and scraping pane output.",
  "intent_triggers": [
    "use the tmux agent skill",
    "run a workflow for tmux",
    "help me with tmux",
    "我需要處理 tmux 相關工作",
    "幫我啟用 tmux 這個 skill",
    "用 agent skill 完成 tmux",
    "use tmux from openclaw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the tmux skill for this task.",
      "invocation": "codex --skill tmux \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
