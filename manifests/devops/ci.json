{
  "name": "ci",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for ci workflows.",
  "intent_triggers": [
    "use the ci agent skill",
    "run a workflow for ci",
    "help me with ci",
    "我需要處理 ci 相關工作",
    "幫我啟用 ci 這個 skill",
    "用 agent skill 完成 ci",
    "use ci from gemini-cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the ci skill for this task.",
      "invocation": "codex --skill ci \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
