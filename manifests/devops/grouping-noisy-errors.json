{
  "name": "grouping-noisy-errors",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for grouping noisy errors workflows.",
  "intent_triggers": [
    "use the grouping noisy errors agent skill",
    "run a workflow for grouping noisy errors",
    "help me with grouping noisy errors",
    "我需要處理 grouping noisy errors 相關工作",
    "幫我啟用 grouping noisy errors 這個 skill",
    "用 agent skill 完成 grouping noisy errors"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the grouping noisy errors skill for this task.",
      "invocation": "codex --skill grouping-noisy-errors \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skill"
  ]
}
