{
  "name": "managing-experiment-lifecycle",
  "version": "0.1.0",
  "category": "devops",
  "description": "Guides experiment state transitions: launching, pausing, resuming, ending, shipping variants, archiving, resetting, and duplicating.",
  "intent_triggers": [
    "use the managing experiment lifecycle agent skill",
    "run a workflow for managing experiment lifecycle",
    "help me with managing experiment lifecycle",
    "我需要處理 managing experiment lifecycle 相關工作",
    "幫我啟用 managing experiment lifecycle 這個 skill",
    "用 agent skill 完成 managing experiment lifecycle"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the managing experiment lifecycle skill for this task.",
      "invocation": "codex --skill managing-experiment-lifecycle \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skill"
  ]
}
