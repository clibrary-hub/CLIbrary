{
  "name": "github-issue-query",
  "version": "0.1.0",
  "category": "devops",
  "description": "Query GitHub issues with jq filtering and reusable selectors.",
  "intent_triggers": [
    "use the github issue query agent skill",
    "run a workflow for github issue query",
    "help me with github issue query",
    "我需要處理 github issue query 相關工作",
    "幫我啟用 github issue query 這個 skill",
    "用 agent skill 完成 github issue query",
    "use github issue query from gh-aw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the github issue query skill for this task.",
      "invocation": "codex --skill github-issue-query \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
