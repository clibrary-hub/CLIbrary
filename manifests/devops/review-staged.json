{
  "name": "review-staged",
  "version": "0.1.0",
  "category": "devops",
  "description": "Generate structured code review for staged files AND all branch commits since main, using Claude Code agents.",
  "intent_triggers": [
    "use the review staged agent skill",
    "run a workflow for review staged",
    "help me with review staged",
    "我需要處理 review staged 相關工作",
    "幫我啟用 review staged 這個 skill",
    "用 agent skill 完成 review staged",
    "use review staged from agent365-devtools"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the review staged skill for this task.",
      "invocation": "codex --skill review-staged \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
