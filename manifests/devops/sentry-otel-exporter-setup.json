{
  "name": "sentry-otel-exporter-setup",
  "version": "0.1.0",
  "category": "devops",
  "description": "Configure the OpenTelemetry Collector with Sentry Exporter for multi-project routing and automatic project creation.",
  "intent_triggers": [
    "use the sentry otel exporter setup agent skill",
    "run a workflow for sentry otel exporter setup",
    "help me with sentry otel exporter setup",
    "我需要處理 sentry otel exporter setup 相關工作",
    "幫我啟用 sentry otel exporter setup 這個 skill",
    "用 agent skill 完成 sentry otel exporter setup",
    "use sentry otel exporter setup from sentry-for-ai"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sentry otel exporter setup skill for this task.",
      "invocation": "codex --skill sentry-otel-exporter-setup \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
