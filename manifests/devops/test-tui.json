{
  "name": "test-tui",
  "version": "0.1.0",
  "category": "devops",
  "description": "Guide for testing Codex TUI interactively",
  "intent_triggers": [
    "use the test tui agent skill",
    "run a workflow for test tui",
    "help me with test tui",
    "我需要處理 test tui 相關工作",
    "幫我啟用 test tui 這個 skill",
    "用 agent skill 完成 test tui",
    "use test tui from codex"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the test tui skill for this task.",
      "invocation": "codex --skill test-tui \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
