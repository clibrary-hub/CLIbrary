{
  "name": "git-flow-branch-creator",
  "version": "0.1.0",
  "category": "devops",
  "description": "Intelligent Git Flow branch creator that analyzes git status/diff and creates appropriate branches following the nvie Git Flow branching model.",
  "intent_triggers": [
    "use the git flow branch creator agent skill",
    "run a workflow for git flow branch creator",
    "help me with git flow branch creator",
    "我需要處理 git flow branch creator 相關工作",
    "幫我啟用 git flow branch creator 這個 skill",
    "用 agent skill 完成 git flow branch creator",
    "use git flow branch creator from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the git flow branch creator skill for this task.",
      "invocation": "codex --skill git-flow-branch-creator \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
