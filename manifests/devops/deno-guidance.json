{
  "name": "deno-guidance",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use when starting any Deno project, choosing packages, configuring deno.json, or running CLI commands.",
  "intent_triggers": [
    "use the deno guidance agent skill",
    "run a workflow for deno guidance",
    "help me with deno guidance",
    "我需要處理 deno guidance 相關工作",
    "幫我啟用 deno guidance 這個 skill",
    "用 agent skill 完成 deno guidance",
    "use deno guidance from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the deno guidance skill for this task.",
      "invocation": "codex --skill deno-guidance \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
