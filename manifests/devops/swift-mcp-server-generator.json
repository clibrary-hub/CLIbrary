{
  "name": "swift-mcp-server-generator",
  "version": "0.1.0",
  "category": "devops",
  "description": "Generate a complete Model Context Protocol server project in Swift using the official MCP Swift SDK package.",
  "intent_triggers": [
    "use the swift mcp server generator agent skill",
    "run a workflow for swift mcp server generator",
    "help me with swift mcp server generator",
    "我需要處理 swift mcp server generator 相關工作",
    "幫我啟用 swift mcp server generator 這個 skill",
    "用 agent skill 完成 swift mcp server generator",
    "use swift mcp server generator from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the swift mcp server generator skill for this task.",
      "invocation": "codex --skill swift-mcp-server-generator \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
