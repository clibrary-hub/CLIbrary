{
  "name": "copying-flags-across-projects",
  "version": "0.1.0",
  "category": "devops",
  "description": "Copy a feature flag from one PostHog project to one or more target projects in the same organization.",
  "intent_triggers": [
    "use the copying flags across projects agent skill",
    "run a workflow for copying flags across projects",
    "help me with copying flags across projects",
    "我需要處理 copying flags across projects 相關工作",
    "幫我啟用 copying flags across projects 這個 skill",
    "用 agent skill 完成 copying flags across projects"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the copying flags across projects skill for this task.",
      "invocation": "codex --skill copying-flags-across-projects \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skill"
  ]
}
