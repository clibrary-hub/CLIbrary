{
  "name": "sentry-cocoa-sdk",
  "version": "0.1.0",
  "category": "devops",
  "description": "Full Sentry SDK setup for Apple platforms (iOS, macOS, tvOS, watchOS, visionOS).",
  "intent_triggers": [
    "use the sentry cocoa sdk agent skill",
    "run a workflow for sentry cocoa sdk",
    "help me with sentry cocoa sdk",
    "我需要處理 sentry cocoa sdk 相關工作",
    "幫我啟用 sentry cocoa sdk 這個 skill",
    "用 agent skill 完成 sentry cocoa sdk",
    "use sentry cocoa sdk from sentry-for-ai"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sentry cocoa sdk skill for this task.",
      "invocation": "codex --skill sentry-cocoa-sdk \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
