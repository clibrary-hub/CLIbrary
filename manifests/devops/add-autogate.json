{
  "name": "add-autogate",
  "version": "0.1.0",
  "category": "devops",
  "description": "Step-by-step guide for adding a new autogate to workerd for gradual rollout of risky changes, including enum registration, string mapping, usage pattern, and testing.",
  "intent_triggers": [
    "use the add autogate agent skill",
    "run a workflow for add autogate",
    "help me with add autogate",
    "我需要處理 add autogate 相關工作",
    "幫我啟用 add autogate 這個 skill",
    "用 agent skill 完成 add autogate",
    "use add autogate from workerd"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the add autogate skill for this task.",
      "invocation": "codex --skill add-autogate \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
