{
  "name": "dataflows-consumption-cli",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for dataflows consumption cli workflows.",
  "intent_triggers": [
    "use the dataflows consumption cli agent skill",
    "run a workflow for dataflows consumption cli",
    "help me with dataflows consumption cli",
    "我需要處理 dataflows consumption cli 相關工作",
    "幫我啟用 dataflows consumption cli 這個 skill",
    "用 agent skill 完成 dataflows consumption cli",
    "use dataflows consumption cli from skills-for-fabric"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the dataflows consumption cli skill for this task.",
      "invocation": "codex --skill dataflows-consumption-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
