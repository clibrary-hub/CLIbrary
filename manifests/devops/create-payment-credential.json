{
  "name": "create-payment-credential",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for create payment credential workflows.",
  "intent_triggers": [
    "use the create payment credential agent skill",
    "run a workflow for create payment credential",
    "help me with create payment credential",
    "我需要處理 create payment credential 相關工作",
    "幫我啟用 create payment credential 這個 skill",
    "用 agent skill 完成 create payment credential",
    "use create payment credential from link-cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the create payment credential skill for this task.",
      "invocation": "codex --skill create-payment-credential \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
