{
  "name": "ef-core",
  "version": "0.1.0",
  "category": "devops",
  "description": "Get best practices for Entity Framework Core",
  "intent_triggers": [
    "use the ef core agent skill",
    "run a workflow for ef core",
    "help me with ef core",
    "我需要處理 ef core 相關工作",
    "幫我啟用 ef core 這個 skill",
    "用 agent skill 完成 ef core",
    "use ef core from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the ef core skill for this task.",
      "invocation": "codex --skill ef-core \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
