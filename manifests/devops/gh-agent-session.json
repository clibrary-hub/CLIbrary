{
  "name": "gh-agent-session",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use gh agent-session commands to inspect and manage agent sessions.",
  "intent_triggers": [
    "use the gh agent session agent skill",
    "run a workflow for gh agent session",
    "help me with gh agent session",
    "我需要處理 gh agent session 相關工作",
    "幫我啟用 gh agent session 這個 skill",
    "用 agent skill 完成 gh agent session",
    "use gh agent session from gh-aw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the gh agent session skill for this task.",
      "invocation": "codex --skill gh-agent-session \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
