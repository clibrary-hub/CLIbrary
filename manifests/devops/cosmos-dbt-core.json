{
  "name": "cosmos-dbt-core",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use when turning a dbt Core project into an Airflow DAG/TaskGroup using Astronomer Cosmos.",
  "intent_triggers": [
    "use the cosmos dbt core agent skill",
    "run a workflow for cosmos dbt core",
    "help me with cosmos dbt core",
    "我需要處理 cosmos dbt core 相關工作",
    "幫我啟用 cosmos dbt core 這個 skill",
    "用 agent skill 完成 cosmos dbt core",
    "use cosmos dbt core from agents"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the cosmos dbt core skill for this task.",
      "invocation": "codex --skill cosmos-dbt-core \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
