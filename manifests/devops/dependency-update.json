{
  "name": "dependency-update",
  "version": "0.1.0",
  "category": "devops",
  "description": "Guides dependency version updates by checking nuget.org for latest versions, triggering the dotnet-migrate-package Azure DevOps pipeline, and monitoring runs.",
  "intent_triggers": [
    "use the dependency update agent skill",
    "run a workflow for dependency update",
    "help me with dependency update",
    "我需要處理 dependency update 相關工作",
    "幫我啟用 dependency update 這個 skill",
    "用 agent skill 完成 dependency update",
    "use dependency update from aspire"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the dependency update skill for this task.",
      "invocation": "codex --skill dependency-update \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
