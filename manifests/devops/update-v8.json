{
  "name": "update-v8",
  "version": "0.1.0",
  "category": "devops",
  "description": "Step-by-step guide for updating the V8 JavaScript engine in workerd, including patch rebasing, dependency updates, integrity hashes, and verification.",
  "intent_triggers": [
    "use the update v8 agent skill",
    "run a workflow for update v8",
    "help me with update v8",
    "我需要處理 update v8 相關工作",
    "幫我啟用 update v8 這個 skill",
    "用 agent skill 完成 update v8",
    "use update v8 from workerd"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the update v8 skill for this task.",
      "invocation": "codex --skill update-v8 \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
