{
  "name": "rozenite-agent-sdk",
  "version": "0.1.0",
  "category": "devops",
  "description": "Use Rozenite for Agents through `@rozenite/agent-sdk` in Node.js or TypeScript code.",
  "intent_triggers": [
    "use the rozenite agent sdk agent skill",
    "run a workflow for rozenite agent sdk",
    "help me with rozenite agent sdk",
    "我需要處理 rozenite agent sdk 相關工作",
    "幫我啟用 rozenite agent sdk 這個 skill",
    "用 agent skill 完成 rozenite agent sdk",
    "use rozenite agent sdk from rozenite"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the rozenite agent sdk skill for this task.",
      "invocation": "codex --skill rozenite-agent-sdk \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
