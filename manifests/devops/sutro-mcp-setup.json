{
  "name": "sutro-mcp-setup",
  "version": "0.1.0",
  "category": "devops",
  "description": "Wire the local Sutro MCP server into the user’s editor — security bundle path, env vars, build steps for mcp/sutro.",
  "intent_triggers": [
    "use the sutro mcp setup agent skill",
    "run a workflow for sutro mcp setup",
    "help me with sutro mcp setup",
    "我需要處理 sutro mcp setup 相關工作",
    "幫我啟用 sutro mcp setup 這個 skill",
    "用 agent skill 完成 sutro mcp setup",
    "use sutro mcp setup from sutro-mcp-server"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the sutro mcp setup skill for this task.",
      "invocation": "codex --skill sutro-mcp-setup \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skills-rest"
  ]
}
