{
  "name": "pinecone-cli",
  "version": "0.1.0",
  "category": "devops",
  "description": "Guide for using the Pinecone CLI (pc) to manage Pinecone resources from the terminal.",
  "intent_triggers": [
    "use the pinecone cli agent skill",
    "run a workflow for pinecone cli",
    "help me with pinecone cli",
    "我需要處理 pinecone cli 相關工作",
    "幫我啟用 pinecone cli 這個 skill",
    "用 agent skill 完成 pinecone cli",
    "use pinecone cli from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the pinecone cli skill for this task.",
      "invocation": "codex --skill pinecone-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
