{
  "name": "qdrant-deployment-options",
  "version": "0.1.0",
  "category": "devops",
  "description": "Guides Qdrant deployment selection.",
  "intent_triggers": [
    "use the qdrant deployment options agent skill",
    "run a workflow for qdrant deployment options",
    "help me with qdrant deployment options",
    "我需要處理 qdrant deployment options 相關工作",
    "幫我啟用 qdrant deployment options 這個 skill",
    "用 agent skill 完成 qdrant deployment options",
    "use qdrant deployment options from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the qdrant deployment options skill for this task.",
      "invocation": "codex --skill qdrant-deployment-options \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
