{
  "name": "lsp-setup",
  "version": "0.1.0",
  "category": "devops",
  "description": "Enable code intelligence (go-to-definition, find-references, hover, type info) for any programming language by installing and configuring an LSP server for Copilot CLI.",
  "intent_triggers": [
    "use the lsp setup agent skill",
    "run a workflow for lsp setup",
    "help me with lsp setup",
    "我需要處理 lsp setup 相關工作",
    "幫我啟用 lsp setup 這個 skill",
    "用 agent skill 完成 lsp setup",
    "use lsp setup from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the lsp setup skill for this task.",
      "invocation": "codex --skill lsp-setup \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
