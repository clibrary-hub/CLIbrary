{
  "name": "generate-custom-instructions-from-codebase",
  "version": "0.1.0",
  "category": "devops",
  "description": "Migration and code evolution instructions generator for GitHub Copilot.",
  "intent_triggers": [
    "use the generate custom instructions from codebase agent skill",
    "run a workflow for generate custom instructions from codebase",
    "help me with generate custom instructions from codebase",
    "我需要處理 generate custom instructions from codebase 相關工作",
    "幫我啟用 generate custom instructions from codebase 這個 skill",
    "用 agent skill 完成 generate custom instructions from codebase",
    "use generate custom instructions from codebase from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the generate custom instructions from codebase skill for this task.",
      "invocation": "codex --skill generate-custom-instructions-from-codebase \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
