{
  "name": "langfuse",
  "version": "0.1.0",
  "category": "devops",
  "description": "Interact with Langfuse and access its documentation.",
  "intent_triggers": [
    "use the langfuse agent skill",
    "run a workflow for langfuse",
    "help me with langfuse",
    "我需要處理 langfuse 相關工作",
    "幫我啟用 langfuse 這個 skill",
    "用 agent skill 完成 langfuse",
    "use langfuse from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the langfuse skill for this task.",
      "invocation": "codex --skill langfuse \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
