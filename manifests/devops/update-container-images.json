{
  "name": "update-container-images",
  "version": "0.1.0",
  "category": "devops",
  "description": "Updates Docker container image tags used by Aspire hosting integrations.",
  "intent_triggers": [
    "use the update container images agent skill",
    "run a workflow for update container images",
    "help me with update container images",
    "我需要處理 update container images 相關工作",
    "幫我啟用 update container images 這個 skill",
    "用 agent skill 完成 update container images",
    "use update container images from aspire"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the update container images skill for this task.",
      "invocation": "codex --skill update-container-images \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
