{
  "name": "livekit-agents",
  "version": "0.1.0",
  "category": "devops",
  "description": "Build voice AI agents with LiveKit Cloud and the Agents SDK.",
  "intent_triggers": [
    "use the livekit agents agent skill",
    "run a workflow for livekit agents",
    "help me with livekit agents",
    "我需要處理 livekit agents 相關工作",
    "幫我啟用 livekit agents 這個 skill",
    "用 agent skill 完成 livekit agents",
    "use livekit agents from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the livekit agents skill for this task.",
      "invocation": "codex --skill livekit-agents \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
