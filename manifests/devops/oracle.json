{
  "name": "oracle",
  "version": "0.1.0",
  "category": "devops",
  "description": "Best practices for using the oracle CLI (prompt + file bundling, engines, sessions, and file attachment patterns).",
  "intent_triggers": [
    "use the oracle agent skill",
    "run a workflow for oracle",
    "help me with oracle",
    "我需要處理 oracle 相關工作",
    "幫我啟用 oracle 這個 skill",
    "用 agent skill 完成 oracle",
    "use oracle from openclaw"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the oracle skill for this task.",
      "invocation": "codex --skill oracle \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
