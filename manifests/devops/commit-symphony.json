{
  "name": "commit-symphony",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for commit workflows.",
  "intent_triggers": [
    "use the commit agent skill",
    "run a workflow for commit",
    "help me with commit",
    "我需要處理 commit 相關工作",
    "幫我啟用 commit 這個 skill",
    "用 agent skill 完成 commit",
    "use commit from symphony"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the commit skill for this task.",
      "invocation": "codex --skill commit \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
