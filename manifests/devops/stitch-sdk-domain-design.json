{
  "name": "stitch-sdk-domain-design",
  "version": "0.1.0",
  "category": "devops",
  "description": "Design the domain model for the Stitch SDK.",
  "intent_triggers": [
    "use the stitch sdk domain design agent skill",
    "run a workflow for stitch sdk domain design",
    "help me with stitch sdk domain design",
    "我需要處理 stitch sdk domain design 相關工作",
    "幫我啟用 stitch sdk domain design 這個 skill",
    "用 agent skill 完成 stitch sdk domain design",
    "use stitch sdk domain design from stitch-sdk"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the stitch sdk domain design skill for this task.",
      "invocation": "codex --skill stitch-sdk-domain-design \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
