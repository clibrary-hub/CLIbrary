{
  "name": "fix-errors",
  "version": "0.1.0",
  "category": "devops",
  "description": "Guidelines for fixing unhandled errors from the VS Code error telemetry dashboard.",
  "intent_triggers": [
    "use the fix errors agent skill",
    "run a workflow for fix errors",
    "help me with fix errors",
    "我需要處理 fix errors 相關工作",
    "幫我啟用 fix errors 這個 skill",
    "用 agent skill 完成 fix errors",
    "use fix errors from vscode"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the fix errors skill for this task.",
      "invocation": "codex --skill fix-errors \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
