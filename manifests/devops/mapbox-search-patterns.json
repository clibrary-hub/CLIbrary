{
  "name": "mapbox-search-patterns",
  "version": "0.1.0",
  "category": "devops",
  "description": "Expert guidance on choosing the right Mapbox search tool and parameters for geocoding, POI search, and location discovery",
  "intent_triggers": [
    "use the mapbox search patterns agent skill",
    "run a workflow for mapbox search patterns",
    "help me with mapbox search patterns",
    "我需要處理 mapbox search patterns 相關工作",
    "幫我啟用 mapbox search patterns 這個 skill",
    "用 agent skill 完成 mapbox search patterns",
    "use mapbox search patterns from mapbox-agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mapbox search patterns skill for this task.",
      "invocation": "codex --skill mapbox-search-patterns \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
