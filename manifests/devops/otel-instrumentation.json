{
  "name": "otel-instrumentation",
  "version": "0.1.0",
  "category": "devops",
  "description": "Configures trace spans, defines custom metrics, sets up log exporters, and optimizes sampling strategies for OpenTelemetry instrumentation.",
  "intent_triggers": [
    "use the otel instrumentation agent skill",
    "run a workflow for otel instrumentation",
    "help me with otel instrumentation",
    "我需要處理 otel instrumentation 相關工作",
    "幫我啟用 otel instrumentation 這個 skill",
    "用 agent skill 完成 otel instrumentation",
    "use otel instrumentation from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the otel instrumentation skill for this task.",
      "invocation": "codex --skill otel-instrumentation \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
