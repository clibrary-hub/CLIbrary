{
  "name": "work-iterations",
  "version": "0.1.0",
  "category": "devops",
  "description": "List, create, and assign iterations for Azure DevOps projects and teams.",
  "intent_triggers": [
    "use the work iterations agent skill",
    "run a workflow for work iterations",
    "help me with work iterations",
    "我需要處理 work iterations 相關工作",
    "幫我啟用 work iterations 這個 skill",
    "用 agent skill 完成 work iterations",
    "use work iterations from azure-devops-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the work iterations skill for this task.",
      "invocation": "codex --skill work-iterations \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
