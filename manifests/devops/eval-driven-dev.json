{
  "name": "eval-driven-dev",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for eval driven dev workflows.",
  "intent_triggers": [
    "use the eval driven dev agent skill",
    "run a workflow for eval driven dev",
    "help me with eval driven dev",
    "我需要處理 eval driven dev 相關工作",
    "幫我啟用 eval driven dev 這個 skill",
    "用 agent skill 完成 eval driven dev",
    "use eval driven dev from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the eval driven dev skill for this task.",
      "invocation": "codex --skill eval-driven-dev \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
