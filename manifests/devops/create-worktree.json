{
  "name": "create-worktree",
  "version": "0.1.0",
  "category": "devops",
  "description": "Create a ClickHouse git worktree with submodules hardlinked from the main repo.",
  "intent_triggers": [
    "use the create worktree agent skill",
    "run a workflow for create worktree",
    "help me with create worktree",
    "我需要處理 create worktree 相關工作",
    "幫我啟用 create worktree 這個 skill",
    "用 agent skill 完成 create worktree",
    "use create worktree from clickhouse"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the create worktree skill for this task.",
      "invocation": "codex --skill create-worktree \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
