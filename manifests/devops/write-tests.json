{
  "name": "write-tests",
  "version": "0.1.0",
  "category": "devops",
  "description": "Guide for writing unit tests.",
  "intent_triggers": [
    "use the write tests agent skill",
    "run a workflow for write tests",
    "help me with write tests",
    "我需要處理 write tests 相關工作",
    "幫我啟用 write tests 這個 skill",
    "用 agent skill 完成 write tests",
    "use write tests from semantic-link-labs"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the write tests skill for this task.",
      "invocation": "codex --skill write-tests \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
