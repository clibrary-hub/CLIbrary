{
  "name": "jupyter-live-kernel",
  "version": "0.1.0",
  "category": "devops",
  "description": "Agent skill for jupyter live kernel workflows.",
  "intent_triggers": [
    "use the jupyter live kernel agent skill",
    "run a workflow for jupyter live kernel",
    "help me with jupyter live kernel",
    "我需要處理 jupyter live kernel 相關工作",
    "幫我啟用 jupyter live kernel 這個 skill",
    "用 agent skill 完成 jupyter live kernel"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the jupyter live kernel skill for this task.",
      "invocation": "codex --skill jupyter-live-kernel \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mit",
    "skill"
  ]
}
