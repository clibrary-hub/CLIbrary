{
  "name": "ruby-mcp-server-generator",
  "version": "0.1.0",
  "category": "devops",
  "description": "Generate a complete Model Context Protocol server project in Ruby using the official MCP Ruby SDK gem.",
  "intent_triggers": [
    "use the ruby mcp server generator agent skill",
    "run a workflow for ruby mcp server generator",
    "help me with ruby mcp server generator",
    "我需要處理 ruby mcp server generator 相關工作",
    "幫我啟用 ruby mcp server generator 這個 skill",
    "用 agent skill 完成 ruby mcp server generator",
    "use ruby mcp server generator from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the ruby mcp server generator skill for this task.",
      "invocation": "codex --skill ruby-mcp-server-generator \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
