{
  "name": "nio",
  "version": "0.1.0",
  "category": "devops",
  "description": "Nio — AI agent execution assurance.",
  "intent_triggers": [
    "use the nio agent skill",
    "run a workflow for nio",
    "help me with nio",
    "我需要處理 nio 相關工作",
    "幫我啟用 nio 這個 skill",
    "用 agent skill 完成 nio",
    "use nio from nio"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the nio skill for this task.",
      "invocation": "codex --skill nio \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "skills-rest"
  ]
}
