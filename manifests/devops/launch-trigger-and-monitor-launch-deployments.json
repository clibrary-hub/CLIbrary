{
  "name": "launch-trigger-and-monitor-launch-deployments",
  "version": "0.1.0",
  "category": "devops",
  "description": "Trigger a Launch deployment for a specific environment, then poll its status until completion.",
  "intent_triggers": [
    "use the launch trigger and monitor launch deployments agent skill",
    "run a workflow for launch trigger and monitor launch deployments",
    "help me with launch trigger and monitor launch deployments",
    "我需要處理 launch trigger and monitor launch deployments 相關工作",
    "幫我啟用 launch trigger and monitor launch deployments 這個 skill",
    "用 agent skill 完成 launch trigger and monitor launch deployments",
    "use launch trigger and monitor launch deployments from contentstack-agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the launch trigger and monitor launch deployments skill for this task.",
      "invocation": "codex --skill launch-trigger-and-monitor-launch-deployments \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
