{
  "name": "multi-stage-dockerfile",
  "version": "0.1.0",
  "category": "devops",
  "description": "Create optimized multi-stage Dockerfiles for any language or framework",
  "intent_triggers": [
    "use the multi stage dockerfile agent skill",
    "run a workflow for multi stage dockerfile",
    "help me with multi stage dockerfile",
    "我需要處理 multi stage dockerfile 相關工作",
    "幫我啟用 multi stage dockerfile 這個 skill",
    "用 agent skill 完成 multi stage dockerfile",
    "use multi stage dockerfile from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the multi stage dockerfile skill for this task.",
      "invocation": "codex --skill multi-stage-dockerfile \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
