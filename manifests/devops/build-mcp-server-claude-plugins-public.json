{
  "name": "build-mcp-server-claude-plugins-public",
  "version": "0.1.0",
  "category": "devops",
  "description": "This skill should be used when the user asks to \"build an MCP server\", \"create an MCP\", \"make an MCP integration\", \"wrap an API for Claude\", \"expose tools to Claude\", \"make an MCP app\", or discusse...",
  "intent_triggers": [
    "use the build mcp server agent skill",
    "run a workflow for build mcp server",
    "help me with build mcp server",
    "我需要處理 build mcp server 相關工作",
    "幫我啟用 build mcp server 這個 skill",
    "用 agent skill 完成 build mcp server",
    "use build mcp server from claude-plugins-public"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the build mcp server skill for this task.",
      "invocation": "codex --skill build-mcp-server \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "devops",
    "mcpservers"
  ]
}
