{
  "name": "expo-dev-client-skills",
  "version": "0.1.0",
  "category": "devops",
  "description": "Build and distribute Expo development clients locally or via TestFlight",
  "intent_triggers": [
    "use the expo dev client agent skill",
    "run a workflow for expo dev client",
    "help me with expo dev client",
    "我需要處理 expo dev client 相關工作",
    "幫我啟用 expo dev client 這個 skill",
    "用 agent skill 完成 expo dev client",
    "use expo dev client from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the expo dev client skill for this task.",
      "invocation": "codex --skill expo-dev-client \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "devops",
    "mcpservers",
    "mit"
  ]
}
