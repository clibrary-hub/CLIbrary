{
  "name": "query-writing",
  "version": "0.1.0",
  "category": "data",
  "description": "Writes and executes SQL queries from simple SELECTs to complex multi-table JOINs, aggregations, and subqueries.",
  "intent_triggers": [
    "use the query writing agent skill",
    "run a workflow for query writing",
    "help me with query writing",
    "我需要處理 query writing 相關工作",
    "幫我啟用 query writing 這個 skill",
    "用 agent skill 完成 query writing",
    "use query writing from deepagents"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the query writing skill for this task.",
      "invocation": "codex --skill query-writing \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
