{
  "name": "shopify-custom-data",
  "version": "0.1.0",
  "category": "data",
  "description": "MUST be used first when prompts mention Metafields or Metaobjects.",
  "intent_triggers": [
    "use the shopify custom data agent skill",
    "run a workflow for shopify custom data",
    "help me with shopify custom data",
    "我需要處理 shopify custom data 相關工作",
    "幫我啟用 shopify custom data 這個 skill",
    "用 agent skill 完成 shopify custom data",
    "use shopify custom data from shopify-ai-toolkit"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the shopify custom data skill for this task.",
      "invocation": "codex --skill shopify-custom-data \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
