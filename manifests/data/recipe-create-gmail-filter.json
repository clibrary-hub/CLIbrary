{
  "name": "recipe-create-gmail-filter",
  "version": "0.1.0",
  "category": "data",
  "description": "Create a Gmail filter to automatically label, star, or categorize incoming messages.",
  "intent_triggers": [
    "use the recipe create gmail filter agent skill",
    "run a workflow for recipe create gmail filter",
    "help me with recipe create gmail filter",
    "我需要處理 recipe create gmail filter 相關工作",
    "幫我啟用 recipe create gmail filter 這個 skill",
    "用 agent skill 完成 recipe create gmail filter",
    "use recipe create gmail filter from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recipe create gmail filter skill for this task.",
      "invocation": "codex --skill recipe-create-gmail-filter \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
