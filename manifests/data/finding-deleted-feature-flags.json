{
  "name": "finding-deleted-feature-flags",
  "version": "0.1.0",
  "category": "data",
  "description": "Find feature flags that were soft-deleted in the active project within a recent time window.",
  "intent_triggers": [
    "use the finding deleted feature flags agent skill",
    "run a workflow for finding deleted feature flags",
    "help me with finding deleted feature flags",
    "我需要處理 finding deleted feature flags 相關工作",
    "幫我啟用 finding deleted feature flags 這個 skill",
    "用 agent skill 完成 finding deleted feature flags"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the finding deleted feature flags skill for this task.",
      "invocation": "codex --skill finding-deleted-feature-flags \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mit",
    "skill"
  ]
}
