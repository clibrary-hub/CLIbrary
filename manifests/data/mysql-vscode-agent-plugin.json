{
  "name": "mysql-vscode-agent-plugin",
  "version": "0.1.0",
  "category": "data",
  "description": "Plan and review MySQL/InnoDB schema, indexing, query tuning, transactions, and operations.",
  "intent_triggers": [
    "use the mysql agent skill",
    "run a workflow for mysql",
    "help me with mysql",
    "我需要處理 mysql 相關工作",
    "幫我啟用 mysql 這個 skill",
    "用 agent skill 完成 mysql",
    "use mysql from vscode-agent-plugin"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mysql skill for this task.",
      "invocation": "codex --skill mysql \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
