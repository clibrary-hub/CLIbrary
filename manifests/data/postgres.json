{
  "name": "postgres",
  "version": "0.1.0",
  "category": "data",
  "description": "PostgreSQL best practices, query optimization, connection troubleshooting, and performance improvement.",
  "intent_triggers": [
    "use the postgres agent skill",
    "run a workflow for postgres",
    "help me with postgres",
    "我需要處理 postgres 相關工作",
    "幫我啟用 postgres 這個 skill",
    "用 agent skill 完成 postgres",
    "use postgres from database-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the postgres skill for this task.",
      "invocation": "codex --skill postgres \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
