{
  "name": "downloading-batch-export-files",
  "version": "0.1.0",
  "category": "data",
  "description": "Agent skill for downloading batch export files workflows.",
  "intent_triggers": [
    "use the downloading batch export files agent skill",
    "run a workflow for downloading batch export files",
    "help me with downloading batch export files",
    "我需要處理 downloading batch export files 相關工作",
    "幫我啟用 downloading batch export files 這個 skill",
    "用 agent skill 完成 downloading batch export files"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the downloading batch export files skill for this task.",
      "invocation": "codex --skill downloading-batch-export-files \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mit",
    "skill"
  ]
}
