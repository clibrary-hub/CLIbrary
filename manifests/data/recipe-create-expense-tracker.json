{
  "name": "recipe-create-expense-tracker",
  "version": "0.1.0",
  "category": "data",
  "description": "Set up a Google Sheets spreadsheet for tracking expenses with headers and initial entries.",
  "intent_triggers": [
    "use the recipe create expense tracker agent skill",
    "run a workflow for recipe create expense tracker",
    "help me with recipe create expense tracker",
    "我需要處理 recipe create expense tracker 相關工作",
    "幫我啟用 recipe create expense tracker 這個 skill",
    "用 agent skill 完成 recipe create expense tracker",
    "use recipe create expense tracker from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recipe create expense tracker skill for this task.",
      "invocation": "codex --skill recipe-create-expense-tracker \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
