{
  "name": "recipe-copy-sheet-for-new-month",
  "version": "0.1.0",
  "category": "data",
  "description": "Duplicate a Google Sheets template tab for a new month of tracking.",
  "intent_triggers": [
    "use the recipe copy sheet for new month agent skill",
    "run a workflow for recipe copy sheet for new month",
    "help me with recipe copy sheet for new month",
    "我需要處理 recipe copy sheet for new month 相關工作",
    "幫我啟用 recipe copy sheet for new month 這個 skill",
    "用 agent skill 完成 recipe copy sheet for new month",
    "use recipe copy sheet for new month from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recipe copy sheet for new month skill for this task.",
      "invocation": "codex --skill recipe-copy-sheet-for-new-month \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
