{
  "name": "data-context-extractor",
  "version": "0.1.0",
  "category": "data",
  "description": "Agent skill for data context extractor workflows.",
  "intent_triggers": [
    "use the data context extractor agent skill",
    "run a workflow for data context extractor",
    "help me with data context extractor",
    "我需要處理 data context extractor 相關工作",
    "幫我啟用 data context extractor 這個 skill",
    "用 agent skill 完成 data context extractor",
    "use data context extractor from knowledge-work-plugins"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the data context extractor skill for this task.",
      "invocation": "codex --skill data-context-extractor \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
