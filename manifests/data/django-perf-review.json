{
  "name": "django-perf-review",
  "version": "0.1.0",
  "category": "data",
  "description": "Django performance code review.",
  "intent_triggers": [
    "use the django perf review agent skill",
    "run a workflow for django perf review",
    "help me with django perf review",
    "我需要處理 django perf review 相關工作",
    "幫我啟用 django perf review 這個 skill",
    "用 agent skill 完成 django perf review",
    "use django perf review from sentry-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the django perf review skill for this task.",
      "invocation": "codex --skill django-perf-review \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
