{
  "name": "dv-security",
  "version": "0.1.0",
  "category": "data",
  "description": "Security-role assignment, user access, application users, business units, and admin self-elevation in Dataverse environments.",
  "intent_triggers": [
    "use the dv security agent skill",
    "run a workflow for dv security",
    "help me with dv security",
    "我需要處理 dv security 相關工作",
    "幫我啟用 dv security 這個 skill",
    "用 agent skill 完成 dv security",
    "use dv security from dataverse-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the dv security skill for this task.",
      "invocation": "codex --skill dv-security \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
