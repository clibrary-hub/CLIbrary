{
  "name": "prisma-database-setup",
  "version": "0.1.0",
  "category": "data",
  "description": "Guides for configuring Prisma with different database providers (PostgreSQL, MySQL, SQLite, MongoDB, etc.).",
  "intent_triggers": [
    "use the prisma database setup agent skill",
    "run a workflow for prisma database setup",
    "help me with prisma database setup",
    "我需要處理 prisma database setup 相關工作",
    "幫我啟用 prisma database setup 這個 skill",
    "用 agent skill 完成 prisma database setup",
    "use prisma database setup from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the prisma database setup skill for this task.",
      "invocation": "codex --skill prisma-database-setup \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
