{
  "name": "typespec-api-operations",
  "version": "0.1.0",
  "category": "data",
  "description": "Add GET, POST, PATCH, and DELETE operations to a TypeSpec API plugin with proper routing, parameters, and adaptive cards",
  "intent_triggers": [
    "use the typespec api operations agent skill",
    "run a workflow for typespec api operations",
    "help me with typespec api operations",
    "我需要處理 typespec api operations 相關工作",
    "幫我啟用 typespec api operations 這個 skill",
    "用 agent skill 完成 typespec api operations",
    "use typespec api operations from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the typespec api operations skill for this task.",
      "invocation": "codex --skill typespec-api-operations \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
