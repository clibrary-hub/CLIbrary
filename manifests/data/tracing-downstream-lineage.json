{
  "name": "tracing-downstream-lineage",
  "version": "0.1.0",
  "category": "data",
  "description": "Trace downstream data lineage and impact analysis.",
  "intent_triggers": [
    "use the tracing downstream lineage agent skill",
    "run a workflow for tracing downstream lineage",
    "help me with tracing downstream lineage",
    "我需要處理 tracing downstream lineage 相關工作",
    "幫我啟用 tracing downstream lineage 這個 skill",
    "用 agent skill 完成 tracing downstream lineage",
    "use tracing downstream lineage from agents"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the tracing downstream lineage skill for this task.",
      "invocation": "codex --skill tracing-downstream-lineage \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
