{
  "name": "claimable-postgres",
  "version": "0.1.0",
  "category": "data",
  "description": "Agent skill for claimable postgres workflows.",
  "intent_triggers": [
    "use the claimable postgres agent skill",
    "run a workflow for claimable postgres",
    "help me with claimable postgres",
    "我需要處理 claimable postgres 相關工作",
    "幫我啟用 claimable postgres 這個 skill",
    "用 agent skill 完成 claimable postgres",
    "use claimable postgres from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the claimable postgres skill for this task.",
      "invocation": "codex --skill claimable-postgres \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
