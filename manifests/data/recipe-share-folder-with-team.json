{
  "name": "recipe-share-folder-with-team",
  "version": "0.1.0",
  "category": "data",
  "description": "Share a Google Drive folder and all its contents with a list of collaborators.",
  "intent_triggers": [
    "use the recipe share folder with team agent skill",
    "run a workflow for recipe share folder with team",
    "help me with recipe share folder with team",
    "我需要處理 recipe share folder with team 相關工作",
    "幫我啟用 recipe share folder with team 這個 skill",
    "用 agent skill 完成 recipe share folder with team",
    "use recipe share folder with team from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recipe share folder with team skill for this task.",
      "invocation": "codex --skill recipe-share-folder-with-team \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
