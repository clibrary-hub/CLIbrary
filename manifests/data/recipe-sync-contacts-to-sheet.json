{
  "name": "recipe-sync-contacts-to-sheet",
  "version": "0.1.0",
  "category": "data",
  "description": "Export Google Contacts directory to a Google Sheets spreadsheet.",
  "intent_triggers": [
    "use the recipe sync contacts to sheet agent skill",
    "run a workflow for recipe sync contacts to sheet",
    "help me with recipe sync contacts to sheet",
    "我需要處理 recipe sync contacts to sheet 相關工作",
    "幫我啟用 recipe sync contacts to sheet 這個 skill",
    "用 agent skill 完成 recipe sync contacts to sheet",
    "use recipe sync contacts to sheet from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recipe sync contacts to sheet skill for this task.",
      "invocation": "codex --skill recipe-sync-contacts-to-sheet \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
