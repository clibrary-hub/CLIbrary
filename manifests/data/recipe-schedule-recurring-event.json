{
  "name": "recipe-schedule-recurring-event",
  "version": "0.1.0",
  "category": "data",
  "description": "Create a recurring Google Calendar event with attendees.",
  "intent_triggers": [
    "use the recipe schedule recurring event agent skill",
    "run a workflow for recipe schedule recurring event",
    "help me with recipe schedule recurring event",
    "我需要處理 recipe schedule recurring event 相關工作",
    "幫我啟用 recipe schedule recurring event 這個 skill",
    "用 agent skill 完成 recipe schedule recurring event",
    "use recipe schedule recurring event from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recipe schedule recurring event skill for this task.",
      "invocation": "codex --skill recipe-schedule-recurring-event \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
