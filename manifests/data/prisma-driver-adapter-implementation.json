{
  "name": "prisma-driver-adapter-implementation",
  "version": "0.1.0",
  "category": "data",
  "description": "Required reference for Prisma v7 driver adapter work.",
  "intent_triggers": [
    "use the prisma driver adapter implementation agent skill",
    "run a workflow for prisma driver adapter implementation",
    "help me with prisma driver adapter implementation",
    "我需要處理 prisma driver adapter implementation 相關工作",
    "幫我啟用 prisma driver adapter implementation 這個 skill",
    "用 agent skill 完成 prisma driver adapter implementation",
    "use prisma driver adapter implementation from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the prisma driver adapter implementation skill for this task.",
      "invocation": "codex --skill prisma-driver-adapter-implementation \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
