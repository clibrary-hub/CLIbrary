{
  "name": "investigate-metric",
  "version": "0.1.0",
  "category": "data",
  "description": "Agent skill for investigate metric workflows.",
  "intent_triggers": [
    "use the investigate metric agent skill",
    "run a workflow for investigate metric",
    "help me with investigate metric",
    "我需要處理 investigate metric 相關工作",
    "幫我啟用 investigate metric 這個 skill",
    "用 agent skill 完成 investigate metric"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the investigate metric skill for this task.",
      "invocation": "codex --skill investigate-metric \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mit",
    "skill"
  ]
}
