{
  "name": "setup-datamodel",
  "version": "0.1.0",
  "category": "data",
  "description": "Agent skill for setup datamodel workflows.",
  "intent_triggers": [
    "use the setup datamodel agent skill",
    "run a workflow for setup datamodel",
    "help me with setup datamodel",
    "我需要處理 setup datamodel 相關工作",
    "幫我啟用 setup datamodel 這個 skill",
    "用 agent skill 完成 setup datamodel",
    "use setup datamodel from power-platform-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the setup datamodel skill for this task.",
      "invocation": "codex --skill setup-datamodel \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
