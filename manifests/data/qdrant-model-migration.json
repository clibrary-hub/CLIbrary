{
  "name": "qdrant-model-migration",
  "version": "0.1.0",
  "category": "data",
  "description": "Guides embedding model migration in Qdrant without downtime.",
  "intent_triggers": [
    "use the qdrant model migration agent skill",
    "run a workflow for qdrant model migration",
    "help me with qdrant model migration",
    "我需要處理 qdrant model migration 相關工作",
    "幫我啟用 qdrant model migration 這個 skill",
    "用 agent skill 完成 qdrant model migration",
    "use qdrant model migration from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the qdrant model migration skill for this task.",
      "invocation": "codex --skill qdrant-model-migration \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
