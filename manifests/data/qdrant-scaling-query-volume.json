{
  "name": "qdrant-scaling-query-volume",
  "version": "0.1.0",
  "category": "data",
  "description": "Guides Qdrant query volume scaling.",
  "intent_triggers": [
    "use the qdrant scaling query volume agent skill",
    "run a workflow for qdrant scaling query volume",
    "help me with qdrant scaling query volume",
    "我需要處理 qdrant scaling query volume 相關工作",
    "幫我啟用 qdrant scaling query volume 這個 skill",
    "用 agent skill 完成 qdrant scaling query volume"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the qdrant scaling query volume skill for this task.",
      "invocation": "codex --skill qdrant-scaling-query-volume \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mit",
    "skill"
  ]
}
