{
  "name": "shuffle-json-data",
  "version": "0.1.0",
  "category": "data",
  "description": "Shuffle repetitive JSON objects safely by validating schema consistency before randomising entries.",
  "intent_triggers": [
    "use the shuffle json data agent skill",
    "run a workflow for shuffle json data",
    "help me with shuffle json data",
    "我需要處理 shuffle json data 相關工作",
    "幫我啟用 shuffle json data 這個 skill",
    "用 agent skill 完成 shuffle json data",
    "use shuffle json data from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the shuffle json data skill for this task.",
      "invocation": "codex --skill shuffle-json-data \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
