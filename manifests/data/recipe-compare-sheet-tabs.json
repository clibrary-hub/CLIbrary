{
  "name": "recipe-compare-sheet-tabs",
  "version": "0.1.0",
  "category": "data",
  "description": "Read data from two tabs in a Google Sheet to compare and identify differences.",
  "intent_triggers": [
    "use the recipe compare sheet tabs agent skill",
    "run a workflow for recipe compare sheet tabs",
    "help me with recipe compare sheet tabs",
    "我需要處理 recipe compare sheet tabs 相關工作",
    "幫我啟用 recipe compare sheet tabs 這個 skill",
    "用 agent skill 完成 recipe compare sheet tabs",
    "use recipe compare sheet tabs from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recipe compare sheet tabs skill for this task.",
      "invocation": "codex --skill recipe-compare-sheet-tabs \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
