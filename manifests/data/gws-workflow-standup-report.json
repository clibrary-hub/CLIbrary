{
  "name": "gws-workflow-standup-report",
  "version": "0.1.0",
  "category": "data",
  "description": "Google Workflow: Today's meetings + open tasks as a standup summary.",
  "intent_triggers": [
    "use the gws workflow standup report agent skill",
    "run a workflow for gws workflow standup report",
    "help me with gws workflow standup report",
    "我需要處理 gws workflow standup report 相關工作",
    "幫我啟用 gws workflow standup report 這個 skill",
    "用 agent skill 完成 gws workflow standup report",
    "use gws workflow standup report from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the gws workflow standup report skill for this task.",
      "invocation": "codex --skill gws-workflow-standup-report \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
