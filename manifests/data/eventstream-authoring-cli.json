{
  "name": "eventstream-authoring-cli",
  "version": "0.1.0",
  "category": "data",
  "description": "Agent skill for eventstream authoring cli workflows.",
  "intent_triggers": [
    "use the eventstream authoring cli agent skill",
    "run a workflow for eventstream authoring cli",
    "help me with eventstream authoring cli",
    "我需要處理 eventstream authoring cli 相關工作",
    "幫我啟用 eventstream authoring cli 這個 skill",
    "用 agent skill 完成 eventstream authoring cli",
    "use eventstream authoring cli from skills-for-fabric"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the eventstream authoring cli skill for this task.",
      "invocation": "codex --skill eventstream-authoring-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
