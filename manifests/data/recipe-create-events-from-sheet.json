{
  "name": "recipe-create-events-from-sheet",
  "version": "0.1.0",
  "category": "data",
  "description": "Read event data from a Google Sheets spreadsheet and create Google Calendar entries for each row.",
  "intent_triggers": [
    "use the recipe create events from sheet agent skill",
    "run a workflow for recipe create events from sheet",
    "help me with recipe create events from sheet",
    "我需要處理 recipe create events from sheet 相關工作",
    "幫我啟用 recipe create events from sheet 這個 skill",
    "用 agent skill 完成 recipe create events from sheet",
    "use recipe create events from sheet from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recipe create events from sheet skill for this task.",
      "invocation": "codex --skill recipe-create-events-from-sheet \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
