{
  "name": "suppressing-noisy-errors",
  "version": "0.1.0",
  "category": "data",
  "description": "Agent skill for suppressing noisy errors workflows.",
  "intent_triggers": [
    "use the suppressing noisy errors agent skill",
    "run a workflow for suppressing noisy errors",
    "help me with suppressing noisy errors",
    "我需要處理 suppressing noisy errors 相關工作",
    "幫我啟用 suppressing noisy errors 這個 skill",
    "用 agent skill 完成 suppressing noisy errors"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the suppressing noisy errors skill for this task.",
      "invocation": "codex --skill suppressing-noisy-errors \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mit",
    "skill"
  ]
}
