{
  "name": "react",
  "version": "0.1.0",
  "category": "data",
  "description": "React renderer for json-render that turns JSON specs into React components.",
  "intent_triggers": [
    "use the react agent skill",
    "run a workflow for react",
    "help me with react",
    "我需要處理 react 相關工作",
    "幫我啟用 react 這個 skill",
    "用 agent skill 完成 react",
    "use react from json-render"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the react skill for this task.",
      "invocation": "codex --skill react \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
