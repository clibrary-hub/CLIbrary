{
  "name": "upstash-vector-js",
  "version": "0.1.0",
  "category": "data",
  "description": "Skills for Upstash Vector features, TypeScript/JavaScript SDK usage, and integrations.",
  "intent_triggers": [
    "use the upstash vector js agent skill",
    "run a workflow for upstash vector js",
    "help me with upstash vector js",
    "我需要處理 upstash vector js 相關工作",
    "幫我啟用 upstash vector js 這個 skill",
    "用 agent skill 完成 upstash vector js",
    "use upstash vector js from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the upstash vector js skill for this task.",
      "invocation": "codex --skill upstash-vector-js \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
