{
  "name": "spark-consumption-cli",
  "version": "0.1.0",
  "category": "data",
  "description": "Agent skill for spark consumption cli workflows.",
  "intent_triggers": [
    "use the spark consumption cli agent skill",
    "run a workflow for spark consumption cli",
    "help me with spark consumption cli",
    "我需要處理 spark consumption cli 相關工作",
    "幫我啟用 spark consumption cli 這個 skill",
    "用 agent skill 完成 spark consumption cli",
    "use spark consumption cli from skills-for-fabric"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the spark consumption cli skill for this task.",
      "invocation": "codex --skill spark-consumption-cli \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
