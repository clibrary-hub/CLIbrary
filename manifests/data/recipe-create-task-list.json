{
  "name": "recipe-create-task-list",
  "version": "0.1.0",
  "category": "data",
  "description": "Set up a new Google Tasks list with initial tasks.",
  "intent_triggers": [
    "use the recipe create task list agent skill",
    "run a workflow for recipe create task list",
    "help me with recipe create task list",
    "我需要處理 recipe create task list 相關工作",
    "幫我啟用 recipe create task list 這個 skill",
    "用 agent skill 完成 recipe create task list",
    "use recipe create task list from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recipe create task list skill for this task.",
      "invocation": "codex --skill recipe-create-task-list \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
