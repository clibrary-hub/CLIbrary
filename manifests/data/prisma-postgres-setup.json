{
  "name": "prisma-postgres-setup",
  "version": "0.1.0",
  "category": "data",
  "description": "Set up a new Prisma Postgres database and connect it to a local project using the Management API.",
  "intent_triggers": [
    "use the prisma postgres setup agent skill",
    "run a workflow for prisma postgres setup",
    "help me with prisma postgres setup",
    "我需要處理 prisma postgres setup 相關工作",
    "幫我啟用 prisma postgres setup 這個 skill",
    "用 agent skill 完成 prisma postgres setup",
    "use prisma postgres setup from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the prisma postgres setup skill for this task.",
      "invocation": "codex --skill prisma-postgres-setup \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
