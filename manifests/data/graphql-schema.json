{
  "name": "graphql-schema",
  "version": "0.1.0",
  "category": "data",
  "description": "Agent skill for graphql schema workflows.",
  "intent_triggers": [
    "use the graphql schema agent skill",
    "run a workflow for graphql schema",
    "help me with graphql schema",
    "我需要處理 graphql schema 相關工作",
    "幫我啟用 graphql schema 這個 skill",
    "用 agent skill 完成 graphql schema",
    "use graphql schema from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the graphql schema skill for this task.",
      "invocation": "codex --skill graphql-schema \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
