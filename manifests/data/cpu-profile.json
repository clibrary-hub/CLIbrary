{
  "name": "cpu-profile",
  "version": "0.1.0",
  "category": "data",
  "description": "Profile a ClickHouse query using the sampling query profiler and system.trace_log.",
  "intent_triggers": [
    "use the cpu profile agent skill",
    "run a workflow for cpu profile",
    "help me with cpu profile",
    "我需要處理 cpu profile 相關工作",
    "幫我啟用 cpu profile 這個 skill",
    "用 agent skill 完成 cpu profile",
    "use cpu profile from clickhouse"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the cpu profile skill for this task.",
      "invocation": "codex --skill cpu-profile \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
