{
  "name": "bigquery-pipeline-audit",
  "version": "0.1.0",
  "category": "data",
  "description": "Audits Python + BigQuery pipelines for cost safety, idempotency, and production readiness.",
  "intent_triggers": [
    "use the bigquery pipeline audit agent skill",
    "run a workflow for bigquery pipeline audit",
    "help me with bigquery pipeline audit",
    "我需要處理 bigquery pipeline audit 相關工作",
    "幫我啟用 bigquery pipeline audit 這個 skill",
    "用 agent skill 完成 bigquery pipeline audit",
    "use bigquery pipeline audit from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the bigquery pipeline audit skill for this task.",
      "invocation": "codex --skill bigquery-pipeline-audit \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "data",
    "mcpservers",
    "mit"
  ]
}
