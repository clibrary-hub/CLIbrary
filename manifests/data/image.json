{
  "name": "image",
  "version": "0.1.0",
  "category": "data",
  "description": "Image renderer for json-render that turns JSON specs into SVG and PNG images via Satori.",
  "intent_triggers": [
    "use the image agent skill",
    "run a workflow for image",
    "help me with image",
    "我需要處理 image 相關工作",
    "幫我啟用 image 這個 skill",
    "用 agent skill 完成 image",
    "use image from json-render"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the image skill for this task.",
      "invocation": "codex --skill image \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "apache-2.0",
    "data",
    "mcpservers"
  ]
}
