{
  "name": "qdrant-minimize-latency",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Guides Qdrant query latency optimization.",
  "intent_triggers": [
    "use the qdrant minimize latency agent skill",
    "run a workflow for qdrant minimize latency",
    "help me with qdrant minimize latency",
    "我需要處理 qdrant minimize latency 相關工作",
    "幫我啟用 qdrant minimize latency 這個 skill",
    "用 agent skill 完成 qdrant minimize latency"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the qdrant minimize latency skill for this task.",
      "invocation": "codex --skill qdrant-minimize-latency \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mit",
    "skill"
  ]
}
