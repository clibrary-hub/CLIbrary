{
  "name": "swarm",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Dispatch a batch of tasks to subagents in parallel with bounded concurrency.",
  "intent_triggers": [
    "use the swarm agent skill",
    "run a workflow for swarm",
    "help me with swarm",
    "我需要處理 swarm 相關工作",
    "幫我啟用 swarm 這個 skill",
    "用 agent skill 完成 swarm",
    "use swarm from deepagents"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the swarm skill for this task.",
      "invocation": "codex --skill swarm \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mcpservers",
    "mit"
  ]
}
