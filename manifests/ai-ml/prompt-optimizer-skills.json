{
  "name": "prompt-optimizer-skills",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Creates, optimizes, and iteratively refines agent prompts, system prompts, developer prompts, and reusable prompt templates.",
  "intent_triggers": [
    "use the prompt optimizer agent skill",
    "run a workflow for prompt optimizer",
    "help me with prompt optimizer",
    "我需要處理 prompt optimizer 相關工作",
    "幫我啟用 prompt optimizer 這個 skill",
    "用 agent skill 完成 prompt optimizer",
    "use prompt optimizer from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the prompt optimizer skill for this task.",
      "invocation": "codex --skill prompt-optimizer \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "apache-2.0",
    "mcpservers"
  ]
}
