{
  "name": "azure-identity-rust",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Agent skill for azure identity rust workflows.",
  "intent_triggers": [
    "use the azure identity rust agent skill",
    "run a workflow for azure identity rust",
    "help me with azure identity rust",
    "我需要處理 azure identity rust 相關工作",
    "幫我啟用 azure identity rust 這個 skill",
    "用 agent skill 完成 azure identity rust",
    "use azure identity rust from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the azure identity rust skill for this task.",
      "invocation": "codex --skill azure-identity-rust \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mcpservers",
    "mit"
  ]
}
