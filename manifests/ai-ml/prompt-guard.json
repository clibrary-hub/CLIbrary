{
  "name": "prompt-guard",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Meta's 86M prompt injection and jailbreak detector.",
  "intent_triggers": [
    "use the prompt guard agent skill",
    "run a workflow for prompt guard",
    "help me with prompt guard",
    "我需要處理 prompt guard 相關工作",
    "幫我啟用 prompt guard 這個 skill",
    "用 agent skill 完成 prompt guard",
    "use prompt guard from ai-research-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the prompt guard skill for this task.",
      "invocation": "codex --skill prompt-guard \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mcpservers",
    "mit"
  ]
}
