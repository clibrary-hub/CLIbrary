{
  "name": "pi-agent-integration",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Integrate `@mariozechner/pi-agent-core` as the agent abstraction inside another library or runtime.",
  "intent_triggers": [
    "use the pi agent integration agent skill",
    "run a workflow for pi agent integration",
    "help me with pi agent integration",
    "我需要處理 pi agent integration 相關工作",
    "幫我啟用 pi agent integration 這個 skill",
    "用 agent skill 完成 pi agent integration",
    "use pi agent integration from junior"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the pi agent integration skill for this task.",
      "invocation": "codex --skill pi-agent-integration \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "apache-2.0",
    "mcpservers"
  ]
}
