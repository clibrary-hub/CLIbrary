{
  "name": "azure-resource-manager-postgresql-dotnet-skills",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Agent skill for azure resource manager postgresql dotnet workflows.",
  "intent_triggers": [
    "use the azure resource manager postgresql dotnet agent skill",
    "run a workflow for azure resource manager postgresql dotnet",
    "help me with azure resource manager postgresql dotnet",
    "我需要處理 azure resource manager postgresql dotnet 相關工作",
    "幫我啟用 azure resource manager postgresql dotnet 這個 skill",
    "用 agent skill 完成 azure resource manager postgresql dotnet",
    "use azure resource manager postgresql dotnet from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the azure resource manager postgresql dotnet skill for this task.",
      "invocation": "codex --skill azure-resource-manager-postgresql-dotnet \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mcpservers",
    "mit"
  ]
}
