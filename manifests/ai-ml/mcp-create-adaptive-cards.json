{
  "name": "mcp-create-adaptive-cards",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Skill converted from mcp-create-adaptive-cards.prompt.md",
  "intent_triggers": [
    "use the mcp create adaptive cards agent skill",
    "run a workflow for mcp create adaptive cards",
    "help me with mcp create adaptive cards",
    "我需要處理 mcp create adaptive cards 相關工作",
    "幫我啟用 mcp create adaptive cards 這個 skill",
    "用 agent skill 完成 mcp create adaptive cards",
    "use mcp create adaptive cards from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mcp create adaptive cards skill for this task.",
      "invocation": "codex --skill mcp-create-adaptive-cards \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mcpservers",
    "mit"
  ]
}
