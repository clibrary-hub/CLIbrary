{
  "name": "rw-generate-video",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Generate videos directly using the Runway API via runnable scripts.",
  "intent_triggers": [
    "use the rw generate video agent skill",
    "run a workflow for rw generate video",
    "help me with rw generate video",
    "我需要處理 rw generate video 相關工作",
    "幫我啟用 rw generate video 這個 skill",
    "用 agent skill 完成 rw generate video",
    "use rw generate video from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the rw generate video skill for this task.",
      "invocation": "codex --skill rw-generate-video \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mcpservers",
    "mit"
  ]
}
