{
  "name": "bootstrap-realtime-eval",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Bootstrap a new realtime eval folder inside this cookbook repo by choosing the right harness from examples/evals/realtime_evals, scaffolding prompt/tools/data files, generating a useful README, and...",
  "intent_triggers": [
    "use the bootstrap realtime eval agent skill",
    "run a workflow for bootstrap realtime eval",
    "help me with bootstrap realtime eval",
    "我需要處理 bootstrap realtime eval 相關工作",
    "幫我啟用 bootstrap realtime eval 這個 skill",
    "用 agent skill 完成 bootstrap realtime eval",
    "use bootstrap realtime eval from openai-cookbook"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the bootstrap realtime eval skill for this task.",
      "invocation": "codex --skill bootstrap-realtime-eval \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mcpservers",
    "mit"
  ]
}
