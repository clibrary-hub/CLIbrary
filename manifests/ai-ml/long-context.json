{
  "name": "long-context",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Extend context windows of transformer models using RoPE, YaRN, ALiBi, and position interpolation techniques.",
  "intent_triggers": [
    "use the long context agent skill",
    "run a workflow for long context",
    "help me with long context",
    "我需要處理 long context 相關工作",
    "幫我啟用 long context 這個 skill",
    "用 agent skill 完成 long context",
    "use long context from ai-research-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the long context skill for this task.",
      "invocation": "codex --skill long-context \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mcpservers",
    "mit"
  ]
}
