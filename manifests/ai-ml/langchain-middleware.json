{
  "name": "langchain-middleware",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "INVOKE THIS SKILL when you need human-in-the-loop approval, custom middleware, or structured output.",
  "intent_triggers": [
    "use the langchain middleware agent skill",
    "run a workflow for langchain middleware",
    "help me with langchain middleware",
    "我需要處理 langchain middleware 相關工作",
    "幫我啟用 langchain middleware 這個 skill",
    "用 agent skill 完成 langchain middleware"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the langchain middleware skill for this task.",
      "invocation": "codex --skill langchain-middleware \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mit",
    "skill"
  ]
}
