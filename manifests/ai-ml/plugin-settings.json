{
  "name": "plugin-settings",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "This skill should be used when the user asks about \"plugin settings\", \"store plugin configuration\", \"user-configurable plugin\", \".local.md files\", \"plugin state files\", \"read YAML frontmatter\", \"pe...",
  "intent_triggers": [
    "use the plugin settings agent skill",
    "run a workflow for plugin settings",
    "help me with plugin settings",
    "我需要處理 plugin settings 相關工作",
    "幫我啟用 plugin settings 這個 skill",
    "用 agent skill 完成 plugin settings",
    "use plugin settings from claude-plugins-official"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the plugin settings skill for this task.",
      "invocation": "codex --skill plugin-settings \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "apache-2.0",
    "mcpservers"
  ]
}
