{
  "name": "slang",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "SLang language reference for defining Sutro backends — entities, relations, actions, triggers, queues, security, files, and AI.",
  "intent_triggers": [
    "use the slang agent skill",
    "run a workflow for slang",
    "help me with slang",
    "我需要處理 slang 相關工作",
    "幫我啟用 slang 這個 skill",
    "用 agent skill 完成 slang",
    "use slang from sutro-mcp-server"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the slang skill for this task.",
      "invocation": "codex --skill slang \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mit",
    "skills-rest"
  ]
}
