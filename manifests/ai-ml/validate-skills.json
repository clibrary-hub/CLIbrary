{
  "name": "validate-skills",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Validates skills in this repo against agentskills.io spec and Claude Code best practices.",
  "intent_triggers": [
    "use the validate skills agent skill",
    "run a workflow for validate skills",
    "help me with validate skills",
    "我需要處理 validate skills 相關工作",
    "幫我啟用 validate skills 這個 skill",
    "用 agent skill 完成 validate skills",
    "use validate skills from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the validate skills skill for this task.",
      "invocation": "codex --skill validate-skills \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mcpservers",
    "mit"
  ]
}
