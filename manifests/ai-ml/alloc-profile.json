{
  "name": "alloc-profile",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Analyze a jemalloc (or other) allocation profile in collapsed stack format.",
  "intent_triggers": [
    "use the alloc profile agent skill",
    "run a workflow for alloc profile",
    "help me with alloc profile",
    "我需要處理 alloc profile 相關工作",
    "幫我啟用 alloc profile 這個 skill",
    "用 agent skill 完成 alloc profile",
    "use alloc profile from clickhouse"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the alloc profile skill for this task.",
      "invocation": "codex --skill alloc-profile \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "apache-2.0",
    "mcpservers"
  ]
}
