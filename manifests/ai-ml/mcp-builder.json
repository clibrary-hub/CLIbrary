{
  "name": "mcp-builder",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Build Model Context Protocol (MCP) servers with mcp-use framework.",
  "intent_triggers": [
    "use the mcp builder agent skill",
    "run a workflow for mcp builder",
    "help me with mcp builder",
    "我需要處理 mcp builder 相關工作",
    "幫我啟用 mcp builder 這個 skill",
    "用 agent skill 完成 mcp builder",
    "use mcp builder from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mcp builder skill for this task.",
      "invocation": "codex --skill mcp-builder \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "apache-2.0",
    "mcpservers"
  ]
}
