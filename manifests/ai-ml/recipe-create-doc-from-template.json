{
  "name": "recipe-create-doc-from-template",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Copy a Google Docs template, fill in content, and share with collaborators.",
  "intent_triggers": [
    "use the recipe create doc from template agent skill",
    "run a workflow for recipe create doc from template",
    "help me with recipe create doc from template",
    "我需要處理 recipe create doc from template 相關工作",
    "幫我啟用 recipe create doc from template 這個 skill",
    "用 agent skill 完成 recipe create doc from template",
    "use recipe create doc from template from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recipe create doc from template skill for this task.",
      "invocation": "codex --skill recipe-create-doc-from-template \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "apache-2.0",
    "mcpservers"
  ]
}
