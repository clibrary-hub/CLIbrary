{
  "name": "legacy-circuit-mockups",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Generate breadboard circuit mockups and visual diagrams using HTML5 Canvas drawing techniques.",
  "intent_triggers": [
    "use the legacy circuit mockups agent skill",
    "run a workflow for legacy circuit mockups",
    "help me with legacy circuit mockups",
    "我需要處理 legacy circuit mockups 相關工作",
    "幫我啟用 legacy circuit mockups 這個 skill",
    "用 agent skill 完成 legacy circuit mockups",
    "use legacy circuit mockups from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the legacy circuit mockups skill for this task.",
      "invocation": "codex --skill legacy-circuit-mockups \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mcpservers",
    "mit"
  ]
}
