{
  "name": "eval-generator",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Stage 2 standalone — turns an eval plan (output of `/eval-suite-planner`) into concrete test cases grouped by quality signal.",
  "intent_triggers": [
    "use the eval generator agent skill",
    "run a workflow for eval generator",
    "help me with eval generator",
    "我需要處理 eval generator 相關工作",
    "幫我啟用 eval generator 這個 skill",
    "用 agent skill 完成 eval generator",
    "use eval generator from eval-guide"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the eval generator skill for this task.",
      "invocation": "codex --skill eval-generator \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mcpservers",
    "mit"
  ]
}
