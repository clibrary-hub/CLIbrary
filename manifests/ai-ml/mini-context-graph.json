{
  "name": "mini-context-graph",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Agent skill for mini context graph workflows.",
  "intent_triggers": [
    "use the mini context graph agent skill",
    "run a workflow for mini context graph",
    "help me with mini context graph",
    "我需要處理 mini context graph 相關工作",
    "幫我啟用 mini context graph 這個 skill",
    "用 agent skill 完成 mini context graph",
    "use mini context graph from awesome-copilot"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the mini context graph skill for this task.",
      "invocation": "codex --skill mini-context-graph \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mcpservers",
    "mit"
  ]
}
