{
  "name": "apollo-router",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Agent skill for apollo router workflows.",
  "intent_triggers": [
    "use the apollo router agent skill",
    "run a workflow for apollo router",
    "help me with apollo router",
    "我需要處理 apollo router 相關工作",
    "幫我啟用 apollo router 這個 skill",
    "用 agent skill 完成 apollo router",
    "use apollo router from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the apollo router skill for this task.",
      "invocation": "codex --skill apollo-router \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mcpservers",
    "mit"
  ]
}
