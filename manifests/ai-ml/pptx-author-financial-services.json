{
  "name": "pptx-author-financial-services",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Produce a .pptx file on disk (headless) instead of driving a live PowerPoint document — for managed-agent sessions with no open Office app.",
  "intent_triggers": [
    "use the pptx author agent skill",
    "run a workflow for pptx author",
    "help me with pptx author",
    "我需要處理 pptx author 相關工作",
    "幫我啟用 pptx author 這個 skill",
    "用 agent skill 完成 pptx author",
    "use pptx author from financial-services"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the pptx author skill for this task.",
      "invocation": "codex --skill pptx-author \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "apache-2.0",
    "mcpservers"
  ]
}
