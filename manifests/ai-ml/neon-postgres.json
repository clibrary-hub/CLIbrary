{
  "name": "neon-postgres",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Agent skill for neon postgres workflows.",
  "intent_triggers": [
    "use the neon postgres agent skill",
    "run a workflow for neon postgres",
    "help me with neon postgres",
    "我需要處理 neon postgres 相關工作",
    "幫我啟用 neon postgres 這個 skill",
    "用 agent skill 完成 neon postgres",
    "use neon postgres from agent-skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the neon postgres skill for this task.",
      "invocation": "codex --skill neon-postgres \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "apache-2.0",
    "mcpservers"
  ]
}
