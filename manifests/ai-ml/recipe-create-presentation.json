{
  "name": "recipe-create-presentation",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Create a new Google Slides presentation and add initial slides.",
  "intent_triggers": [
    "use the recipe create presentation agent skill",
    "run a workflow for recipe create presentation",
    "help me with recipe create presentation",
    "我需要處理 recipe create presentation 相關工作",
    "幫我啟用 recipe create presentation 這個 skill",
    "用 agent skill 完成 recipe create presentation",
    "use recipe create presentation from cli"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the recipe create presentation skill for this task.",
      "invocation": "codex --skill recipe-create-presentation \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "apache-2.0",
    "mcpservers"
  ]
}
