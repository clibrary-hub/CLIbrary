{
  "name": "apollo-federation",
  "version": "0.1.0",
  "category": "ai-ml",
  "description": "Agent skill for apollo federation workflows.",
  "intent_triggers": [
    "use the apollo federation agent skill",
    "run a workflow for apollo federation",
    "help me with apollo federation",
    "我需要處理 apollo federation 相關工作",
    "幫我啟用 apollo federation 這個 skill",
    "用 agent skill 完成 apollo federation",
    "use apollo federation from skills"
  ],
  "input_schema": {
    "task": {
      "type": "string",
      "required": true,
      "description": "Natural-language task or instruction for the agent skill."
    },
    "context_path": {
      "type": "string",
      "required": false,
      "description": "Optional workspace path or file context for the skill."
    }
  },
  "output_schema": {
    "format": "text",
    "fields": [
      "result",
      "notes"
    ]
  },
  "examples": [
    {
      "intent": "Use the apollo federation skill for this task.",
      "invocation": "codex --skill apollo-federation \"<task>\""
    }
  ],
  "error_codes": {
    "1": "Skill invocation failed or required context was missing."
  },
  "tags": [
    "agent-skill",
    "ai-ml",
    "mcpservers",
    "mit"
  ]
}
